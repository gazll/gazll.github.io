import { createCollection } from './collection.js';

/* The numbered bilingual case-study library. Shape and loading rules live in
   lib/collection.js, which Other Knowledge uses over its own directory. */
export const CaseStudies = createCollection('data/case-studies/');
