{
  "sections": [
    {
      "title": "Web protocols",
      "items": [
        {
          "id": "15-network-i-o-models.web-protocols.q1",
          "q": "How do HTTP and HTTPS differ? How does a TLS 1.3 handshake work?",
          "a": "HTTPS = HTTP over [[g:TLS]] → encryption (confidentiality), integrity, and server authentication (via a certificate).\n\nWith plain HTTP, packets can be read or modified in transit — [[r:unacceptable for fintech/OTA systems]].\n\n**TLS 1.3 (1-RTT):**\n- ClientHello (key share, cipher list) → ServerHello + certificate + key share\n- both sides derive session keys (ECDHE, forward secrecy) → Finished → encrypted data\n- compared with TLS 1.2 (2-RTT): faster and removes weak ciphers; [[o:0-RTT]] supports session resumption (replay risk → never use it for non-idempotent requests)\n\n:::tip Key takeaway\nThe certificate authenticates the server (PKI/CA); ECDHE provides *forward secrecy*. mTLS adds a client certificate for mutual authentication (service-to-service).\n:::\n\n:::deep\n- [[g:forward secrecy]] (ECDHE): even if the long-term private key is exposed, previously captured sessions still cannot be decrypted — critical for payment data\n- certificate chain: server certificate → intermediate CA → root CA; [[r:a common failure is omitting the intermediate certificate]] (Java clients may fail). OCSP stapling checks revocation; HSTS enforces HTTPS\n- where TLS terminates (LB/gateway/mesh) determines where certificates live &amp; how internal mTLS is handled; handshakes consume CPU → reuse sessions/keep-alive connections; PCI DSS prohibits TLS &lt; 1.2\n\nOTA: card data/PII requires TLS 1.2+ end-to-end; consider mTLS with PSPs/banks.\n:::",
          "difficulty": "core"
        },
        {
          "id": "15-network-i-o-models.web-protocols.q2",
          "q": "How do HTTP/1.1, HTTP/2, and HTTP/3 differ, and why was HTTP/3 introduced?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 280' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10.5'>\n<g text-anchor='middle'>\n<rect x='20' y='30' width='200' height='210' rx='8' fill='#FBFCFE' stroke='#42536C'/>\n<rect x='240' y='30' width='200' height='210' rx='8' fill='#FBFCFE' stroke='#0B6E4F'/>\n<rect x='460' y='30' width='200' height='210' rx='8' fill='#FBFCFE' stroke='#3D4E8F'/>\n<text x='120' y='52' fill='#42536C' font-weight='600'>HTTP/1.1</text>\n<text x='340' y='52' fill='#085239' font-weight='600'>HTTP/2</text>\n<text x='560' y='52' fill='#3D4E8F' font-weight='600'>HTTP/3</text>\n</g>\n<g font-size='10' text-anchor='middle' fill='#42536C'>\n<text x='120' y='78'>1 request / connection</text><text x='120' y='96'>(or multiple TCP conns)</text>\n<text x='120' y='128' fill='#AE4536'>Head-of-line blocking</text><text x='120' y='146' fill='#AE4536'>at the HTTP layer</text>\n<text x='120' y='178'>text headers</text>\n<text x='120' y='210' fill='#7E8DA3'>TCP</text>\n<text x='340' y='78'>multiplex many</text><text x='340' y='96'>streams / 1 TCP</text>\n<text x='340' y='128'>header compression</text><text x='340' y='146'>(HPACK), server push</text>\n<text x='340' y='178' fill='#AE4536'>still HOL at TCP layer</text>\n<text x='340' y='210' fill='#7E8DA3'>TCP + TLS</text>\n<text x='560' y='78'>QUIC: independent streams</text><text x='560' y='96'>loss on one stream</text><text x='560' y='114'>does not block others</text>\n<text x='560' y='146' fill='#085239'>0-RTT / 1-RTT, fast</text><text x='560' y='164' fill='#085239'>setup, smooth migration</text>\n<text x='560' y='210' fill='#7E8DA3'>QUIC over UDP</text>\n</g>\n<text x='340' y='266' text-anchor='middle' font-size='11' fill='#42536C'>HTTP/2 removes application-layer HOL; HTTP/3 (QUIC/UDP) removes transport-layer HOL</text>\n</svg></figure>\n\n- [[b:HTTP/1.1]]: each connection handles one request at a time; pipelining proved impractical → browsers open multiple TCP connections. [[r:HOL blocking]] at the application layer\n- [[b:HTTP/2]]: *multiplexes* many streams over *one* TCP connection, compresses headers (HPACK), and supports server push. But it still has [[r:TCP-layer HOL]] (one lost packet stalls every stream)\n- [[b:HTTP/3]]: runs over [[g:QUIC]] (over UDP). Each stream is independent → packet loss on one stream does not block the others; fast connection establishment (combined transport+TLS, 0/1-RTT) + *connection migration* (an IP/network change does not break the connection — useful on mobile)\n\n:::tip Key takeaway\nHTTP/2 removes application-layer HOL but retains TCP-layer HOL; HTTP/3 (QUIC/UDP) removes the latter. gRPC relies on HTTP/2 for multiplexing + streaming.\n:::\n\n:::deep\n- HTTP/2 multiplexing provides clear benefits for pages with many small resources &amp; for gRPC; it offers less benefit for APIs dominated by a single large request. In practice, server push is rarely used (Chrome removed it)\n- HTTP/3 (QUIC) performs especially well on lossy/mobile networks (switching between Wi-Fi↔4G without disconnecting — connection migration) &amp; high-latency links; however, firewalls may block UDP → provide HTTP/2 fallback (Alt-Svc)\n- operations: enable HTTP/2 at the LB/Ingress for client-facing traffic (the upstream hop may remain HTTP/1.1); HTTP/3 requires edge/CDN support\n\n[[r:gRPC + L4 LB pitfall]]: HTTP/2 pins all RPCs to one backend → use an L7/client-side LB or service mesh. OTA: internal gRPC benefits from HTTP/2; HTTP/3 at the CDN improves latency for mobile users on weak networks.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "15-network-i-o-models.web-protocols.q3",
          "q": "What makes a well-designed RESTful API: Richardson maturity, idempotency, status codes, and versioning?",
          "a": "[[b:Richardson Maturity Model]]:\n- L0 (RPC over HTTP), L1 (resources), L2 (HTTP verbs + correct status codes — where most production APIs land), L3 (HATEOAS, rarely used)\n\nPrinciples: model resources as nouns, let verbs carry semantics, and remain stateless.\n\n- [[b:Idempotency]]: GET/PUT/DELETE are idempotent; POST is not → creation requests (booking, payment) need an `Idempotency-Key`\n- [[b:Status codes]]: 201 (created + Location), 409 (conflict), 422 (validation), 429 (rate limit), 503 + Retry-After (degraded service)\n- [[b:Versioning]]: bump the version only for breaking changes; adding an optional field is backward-compatible\n\n:::warn Production pitfall\nReturning 200 for everything with an 'error' field in the body makes client behavior, retries, and monitoring harder to implement correctly. The HTTP status code is part of the contract.\n:::\n\n:::deep\n- most good APIs stop at [[g:Level 2]]; HATEOAS (L3) is rarely worth the cost\n- more important is *consistency* (naming, error format, and pagination conventions across the platform) — what partners and frontends actually need; use ETag/If-Match for optimistic concurrency\n- actions that do not map cleanly to CRUD ('cancel', 'refund') → use a sub-resource (`POST /bookings/{id}/cancellation`) or an RPC-style endpoint ([[r:do not force everything into doctrinaire REST]])\n\nOTA: issue/void/refund/reissue do not map cleanly to CRUD → design them explicitly, make them idempotent, return business status in the body, and use the correct HTTP status so partners can automate reliably.\n:::",
          "difficulty": "core"
        },
        {
          "id": "15-network-i-o-models.web-protocols.q4",
          "q": "How do you handle a large HTTP request end to end without turning it into a memory incident?",
          "a": "Treat the body as a resource budget at every hop: client, CDN, gateway, service, proxy, parser, temporary store, and downstream call.\n\nReject before expensive parsing when the declared or observed size exceeds the policy, and use <code>413 Content Too Large</code> with a useful limit or upload contract. Do not trust <code>Content-Length</code> alone: chunked or HTTP/2 bodies still need byte and time limits, and decompression can expand a small compressed input into a dangerous output.\n\nStream bytes to a bounded sink when incremental processing is safe; otherwise use a direct multipart/resumable object-storage session. Avoid copying the whole body through every layer, bound in-memory buffers, enforce upload rate/deadline, validate authorization and checksum, and clean up abandoned temporary data. A proxy retry is safe only when the body is replayable and the business operation has a stable idempotency identity.\n\n:::deep\nThe API contract owns upload-session state, completion, and asynchronous processing; see 17-rest-api-design.contracts-errors-lifecycle.q8-q9. This item owns the transport and memory boundary.\n:::",
          "difficulty": "advanced"
        }
      ]
    },
    {
      "title": "Realtime & event-driven",
      "items": [
        {
          "id": "15-network-i-o-models.realtime-event-driven.q1",
          "q": "How do polling, SSE, and WebSocket compare, and how does event-driven architecture differ from realtime delivery to clients?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 240' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10.5' text-anchor='middle'>\n<g>\n<rect x='20' y='34' width='200' height='150' rx='8' fill='#FBFCFE' stroke='#42536C'/>\n<rect x='240' y='34' width='200' height='150' rx='8' fill='#FBFCFE' stroke='#3D4E8F'/>\n<rect x='460' y='34' width='200' height='150' rx='8' fill='#FBFCFE' stroke='#0B6E4F'/></g>\n<text x='120' y='54' fill='#42536C' font-weight='600'>Polling</text>\n<text x='340' y='54' fill='#3D4E8F' font-weight='600'>SSE</text>\n<text x='560' y='54' fill='#085239' font-weight='600'>WebSocket</text>\n<g font-size='9.5' fill='#42536C'>\n<text x='120' y='80'>client asks repeatedly</text><text x='120' y='98'>req/res each time</text><text x='120' y='124' fill='#AE4536'>latency + request cost</text><text x='120' y='150'>simplest option</text>\n<text x='340' y='80'>one-way server&#8594;client</text><text x='340' y='98'>over HTTP, auto-reconnect</text><text x='340' y='124' fill='#085239'>good for feeds/notices</text><text x='340' y='150'>lightweight, easy to run</text>\n<text x='560' y='80'>bidirectional full-duplex</text><text x='560' y='98'>persistent connection</text><text x='560' y='124' fill='#085239'>chat/trading/games</text><text x='560' y='150'>heavier infrastructure</text></g>\n<text x='340' y='212' text-anchor='middle' font-size='11' fill='#42536C'>Event-driven (Kafka) = communication INSIDE the system (service&#8596;service); WebSocket/SSE = realtime push TO clients</text>\n</svg></figure>\n\n- [[b:Polling/long polling]]: the client asks repeatedly — simple, but adds latency and request overhead\n- [[b:SSE]]: one-way server→client over HTTP with automatic reconnection — suitable for feeds, notifications, and price updates\n- [[b:WebSocket]]: bidirectional full-duplex over a persistent connection — suitable for chat, trading, and realtime booking tracking\n\n[[g:Important distinction]]: [[b:event-driven architecture]] (Kafka/RabbitMQ) is how services communicate *INSIDE* the system (asynchronously and loosely coupled); WebSocket/SSE is how the system pushes updates *TO clients in realtime*.\n\nRealtime systems commonly combine both: an internal event updates state → a WebSocket gateway fans it out to clients.\n\n:::tip Key takeaway\nDo not confuse event-driven architecture (internal architecture) with realtime client delivery (transport). \"WebSocket vs event-driven\" compares two different, complementary layers.\n:::\n\n:::deep\n- prefer the simplest option that meets the requirement — push notifications or SSE cost less to operate than WebSocket\n- WebSocket and SSE both maintain [[r:stateful connections]] → scaling is harder than for stateless HTTP (requires stickiness/affinity or a connection registry + pub/sub fan-out; see 16.9); the LB must support long timeouts and disable buffering\n- use heartbeat/ping-pong to detect dead connections; support reconnect + resume (Last-Event-ID for SSE)\n\nOTA: push \"price/seat changed\" while the user is on the selection screen (SSE is sufficient), stream booking status in realtime, and use WebSocket for customer-support chat. Backpressure must prevent a slow client from taking down the gateway.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "15-network-i-o-models.realtime-event-driven.q2",
          "q": "How does event-driven programming differ from conventional sequential programming in mindset and code?",
          "a": "- [[b:Sequential/imperative]]: call A, wait for its result, then call B — the flow is linear and easy to read, but a thread remains occupied while I/O is pending\n- [[b:Event-driven]]: react to events/callbacks; instead of \"waiting,\" register what should happen on completion → a small number of threads can handle many operations, but the flow is split across handlers, making ordering and failures harder to reason about ([[r:callback hell]], lost stack traces)\n\n[[g:Core tradeoff]]: event-driven programming trades *straightforward sequential readability* for *resource efficiency under I/O-heavy workloads*.\n\nWith Java 21 virtual threads, teams can regain sequential code while still scaling I/O, so many no longer need event-driven programming solely to support high I/O concurrency.\n\n:::tip Key takeaway\nEvent-driven programming excels when you need backpressure, streaming, or decoupling. If the only goal is \"more concurrent I/O with fewer threads,\" virtual threads provide the same benefit with simpler code.\n:::\n\n:::deep\n- losing linear flow makes \"what happens after what\" harder to reason about (logic is distributed across consumers); debugging requires *distributed tracing* with trace context propagated in events, plus acceptance of eventual consistency and idempotent design\n- in return: decoupling (add a consumer without changing the producer), resilience (the broker buffers work), independent scaling, and auditability (event log)\n\nOTA: use events so that after PAID, ticketing + loyalty + notification + analytics react independently (real decoupling); keep the synchronous flow inside one request (validate→hold→charge) sequential, using virtual threads where appropriate. [[r:Do not turn everything into events]] (over-engineering).\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "15-network-i-o-models.realtime-event-driven.q3",
          "q": "How do the I/O models of Spring MVC, WebFlux, Vert.x, and Go compare?",
          "a": "<figure class='dgm'><svg viewBox='0 0 700 320' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10' text-anchor='middle'>\n<g>\n<rect x='14' y='30' width='160' height='240' rx='8' fill='#FBFCFE' stroke='#AE4536'/>\n<rect x='184' y='30' width='160' height='240' rx='8' fill='#FBFCFE' stroke='#3D4E8F'/>\n<rect x='354' y='30' width='168' height='240' rx='8' fill='#FBFCFE' stroke='#0B6E4F'/>\n<rect x='532' y='30' width='156' height='240' rx='8' fill='#FBFCFE' stroke='#9A7A41'/>\n</g>\n<g font-weight='600'>\n<text x='94' y='50' fill='#7d2f24'>Thread-per-request</text>\n<text x='264' y='50' fill='#3D4E8F'>Event loop</text>\n<text x='438' y='50' fill='#085239'>Virtual threads</text>\n<text x='610' y='50' fill='#9A7A41'>Goroutines</text>\n</g>\n<g font-size='9.5' fill='#42536C'>\n<text x='94' y='70'>Spring MVC / Tomcat</text>\n<text x='264' y='70'>WebFlux·Vert.x·Node</text>\n<text x='438' y='70'>Java 21 (Loom)</text>\n<text x='610' y='70'>Go runtime</text>\n</g>\n<g font-size='9.5' text-anchor='middle'>\n<text x='94' y='100' fill='#AE4536'>1 request = 1 OS thread</text>\n<text x='94' y='118' fill='#AE4536'>blocking I/O &#8594; thread waits</text>\n<text x='94' y='140'>pool of ~200 threads</text>\n<text x='94' y='158'>simple, easy to debug</text>\n<text x='94' y='182' fill='#7d2f24'>exhausted under heavy I/O</text>\n<text x='264' y='100' fill='#3D4E8F'>few threads = #cores</text>\n<text x='264' y='118' fill='#3D4E8F'>non-blocking + callbacks</text>\n<text x='264' y='140'>high throughput, low RAM</text>\n<text x='264' y='158' fill='#7d2f24'>harder code; one blocker</text>\n<text x='264' y='176' fill='#7d2f24'>&#8594; stalls the entire loop</text>\n<text x='438' y='100' fill='#085239'>millions of VTs on ~#cores</text>\n<text x='438' y='118' fill='#085239'>carriers; block &#8594; unmount</text>\n<text x='438' y='140'>blocking style, high scale</text>\n<text x='438' y='158'>reuse JDBC/Hibernate</text>\n<text x='438' y='182' fill='#7d2f24'>pinning with synchronized</text>\n<text x='610' y='100' fill='#9A7A41'>M:N, growable 2KB stack</text>\n<text x='610' y='118' fill='#9A7A41'>channels for coordination</text>\n<text x='610' y='140'>low RAM, fast startup</text>\n<text x='610' y='158'>mature scheduler</text>\n</g>\n<text x='350' y='300' text-anchor='middle' font-size='11' fill='#42536C'>Shared goal: high I/O concurrency with few OS threads · VTs &amp; goroutines keep blocking-style code scalable</text>\n</svg></figure>\n\n- [[b:Spring MVC (thread-per-request)]]: each request occupies one OS thread from the Tomcat pool (~200); blocking I/O leaves that thread waiting. It is simple and easy to debug, but [[r:threads are exhausted when I/O is slow or highly concurrent]]\n- [[b:WebFlux (Reactor)]]: a Netty event loop with roughly one thread per core; non-blocking I/O + backpressure. It delivers high throughput with little RAM, but reactive code is harder and [[r:one accidental blocking call stalls the entire loop]]\n- [[b:Vert.x]]: also event-loop based (verticles), with the golden rule \"do not block the event loop\"; a worker pool handles blocking work. It is lightweight and low-latency\n- [[b:Go]]: M:N goroutines let you write natural blocking-style code while the runtime yields on I/O; channels coordinate work. Goroutines use little RAM and start quickly\n\n:::tip Key takeaway\nAll four approaches solve the same problem: high I/O concurrency with few OS threads. MVC + virtual threads (Java 21) offers an experience similar to Go: blocking-style code that still scales. WebFlux/Vert.x are justified when you genuinely need backpressure or streaming.\n:::\n\n:::deep\n- with thread-per-request, each OS thread consumes roughly 1 MB of stack plus context-switching overhead → a pool of ~200 can block on only ~200 concurrent I/O-bound requests (and exhausts quickly when a downstream slows)\n- an event loop uses few threads, but [[r:one accidental blocking call is catastrophic]] (it stalls the loop → total throughput collapses) — the most common WebFlux/Node failure mode\n- virtual threads store and restore continuations with growable heap-backed stacks → millions of VTs are feasible; watch for *pinning* (synchronized/native code) &amp; heavy ThreadLocal use (→ ScopedValue)\n\n[[g:VT is the \"new default\"]] for I/O-bound JVM workloads (Java 21+); reactive remains valuable for true streaming/backpressure. In every model, [[r:protect downstream systems (DB/PSP) with pools/semaphores]] — the concurrency model does NOT limit downstream load by itself.\n:::",
          "difficulty": "advanced"
        }
      ]
    },
    {
      "title": "Threading & network programming",
      "items": [
        {
          "id": "15-network-i-o-models.threading-network-programming.q1",
          "q": "How does multithreading work in Spring Boot, from an incoming request to the thread pools?",
          "a": "In the conventional Spring MVC model, the embedded servlet container accepts a connection, parses HTTP, and dispatches a request to a worker. One platform worker normally stays associated with the synchronous filter → controller → service path until the response completes.\n\nExact connector, worker, accept-count, and queue defaults depend on the Spring Boot and container version, so inspect the effective configuration rather than memorizing “200 threads.”\n\nOther concurrency domains are independent:\n\n- `@Async` uses a `TaskExecutor`; its bounds and rejection policy come from the actual Boot auto-configuration or application bean.\n- `@Scheduled` needs an explicit overlap/concurrency policy for long jobs.\n- `CompletableFuture` uses the executor supplied to each stage, otherwise often the common pool.\n- HikariCP bounds database connections, which are usually scarcer than request threads.\n\nSpring Boot 3.2+ can enable virtual threads with `spring.threads.virtual.enabled=true`. This changes the cost of a waiting request, not the capacity of the database or a partner API.\n\n:::warn Protect resource hold time\nIncreasing request concurrency while keeping a small DB pool moves waiting to `getConnection()`. A worse failure mode is calling a slow remote service inside a transaction: the request holds a connection while waiting on unrelated I/O.\n:::\n\nKeep transactions short, set deadlines at every remote boundary, and reject or shed excess work before queues exceed the SLO.\n:::deep\nBuild a capacity map instead of tuning one pool in isolation: request admission → servlet/virtual thread → executor handoff → partner semaphore → Hikari connection → database worker. For each boundary record active, queued, rejected/timed-out, service time, and configured maximum.\n\nThe smallest effective capacity plus its hold time controls throughput.\n\nThread handoffs can lose MDC, tracing, security, and cancellation unless context is propagated deliberately. `TaskDecorator` can help for Spring executors; OpenTelemetry instrumentation and framework-specific security context strategies should be tested end to end.\n\nOn JDK 21–23, blocking under `synchronized` could pin a virtual-thread carrier. JDK 24 removed monitor-related pinning; native/foreign calls can still hold carriers. Regardless of JDK, bound downstream concurrency and observe pending connections plus request latency.\n\nFor within-request fan-out, compare `CompletableFuture` in (23-java-concurrency-coding.async-the-modern-toolkit.q1) with virtual threads and structured concurrency in (23-java-concurrency-coding.async-the-modern-toolkit.q4).\n\n**Runnable JDK 21+ proof that virtual threads still need a downstream bulkhead:**\n<pre><code>import java.util.concurrent.*;\nimport java.util.concurrent.atomic.AtomicInteger;\nimport java.util.stream.IntStream;\npublic class BoundedIo {\n  static final Semaphore SLOTS = new Semaphore(32);\n  static final AtomicInteger ACTIVE = new AtomicInteger(), PEAK = new AtomicInteger();\n  static void call() {\n    boolean acquired = false;\n    try {\n      acquired = SLOTS.tryAcquire(50, TimeUnit.MILLISECONDS);\n      if (!acquired) return;                 // map to controlled overload\n      int n = ACTIVE.incrementAndGet();\n      PEAK.accumulateAndGet(n, Math::max);\n      Thread.sleep(20);                      // stand-in for JDBC/HTTP\n    } catch (InterruptedException e) { Thread.currentThread().interrupt(); }\n    finally { if (acquired) { ACTIVE.decrementAndGet(); SLOTS.release(); } }\n  }\n  public static void main(String[] args) throws Exception {\n    try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {\n      var jobs = IntStream.range(0, 1000).mapToObj(i -&gt; executor.submit(BoundedIo::call)).toList();\n      for (var job : jobs) job.get();\n    }\n    System.out.println(\"peak downstream concurrency=\" + PEAK.get()); // never above 32\n  }\n}</code></pre>\n\n**Impact:** a thousand cheap request threads cannot turn into a thousand simultaneous database calls.\n\n- **Trade-off:** the bulkhead rejects or delays work; `32` is illustrative, not a default.\n- **Sizing rule:** derive it from measured downstream capacity, expose wait/rejection metrics and bound acquisition by the request deadline.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "15-network-i-o-models.threading-network-programming.q2",
          "q": "In network programming, how do TCP and UDP, blocking and non-blocking I/O, and the C10K problem relate?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>TCP</th><th>UDP</th></tr></thead><tbody>\n<tr><td>Reliability</td><td>ordered, reliable delivery</td><td>no delivery guarantee</td></tr>\n<tr><td>Connection</td><td>three-way handshake</td><td>connectionless</td></tr>\n<tr><td>Used for</td><td>HTTP/DB</td><td>DNS, streaming, QUIC</td></tr>\n</tbody></table>\n\n- [[b:Blocking I/O]] (one thread per connection): simple, but does not scale to tens of thousands of connections → the [[r:C10K problem]]\n- [[b:Non-blocking I/O / I/O multiplexing]]: one thread monitors many sockets through `epoll` (Linux)/`kqueue`; Java NIO exposes `Selector`. This underpins Netty/event loops → a small number of threads can serve tens of thousands of connections\n\nThat is the technical reason event loops are efficient: epoll can \"wait on many sockets at once\" without assigning one thread to each socket.\n\n**The same server implemented with two I/O models:**\n<pre><code><span class=\"c\">// Blocking: one thread per connection — 10k connections means 10k threads</span>\nServerSocket server = <span class=\"k\">new</span> ServerSocket(<span class=\"n\">8080</span>);\n<span class=\"k\">while</span> (<span class=\"k\">true</span>) {\n    Socket s = server.<span class=\"f\">accept</span>();          <span class=\"c\">// blocks until a client connects</span>\n    pool.<span class=\"f\">submit</span>(() -&gt; <span class=\"f\">handle</span>(s));       <span class=\"r\">// thread stays occupied even while the client is idle</span>\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// Non-blocking: one thread monitors tens of thousands of sockets via epoll/kqueue</span>\nSelector sel = Selector.<span class=\"f\">open</span>();\nchannel.<span class=\"f\">configureBlocking</span>(<span class=\"k\">false</span>);\nchannel.<span class=\"f\">register</span>(sel, SelectionKey.OP_READ);\n<span class=\"k\">while</span> (<span class=\"k\">true</span>) {\n    sel.<span class=\"f\">select</span>();                        <span class=\"c\">// blocks ONCE for all sockets</span>\n    <span class=\"k\">for</span> (SelectionKey k : sel.<span class=\"f\">selectedKeys</span>()) {\n        <span class=\"k\">if</span> (k.<span class=\"f\">isReadable</span>()) <span class=\"f\">read</span>((SocketChannel) k.<span class=\"f\">channel</span>());\n    }\n}</code></pre>\n\n:::deep\n- `epoll` (Linux) uses a *readiness* model (reports which sockets are ready) — one thread in `epoll_wait` can monitor tens of thousands of sockets → the foundation of Netty/Nginx/event loops and a solution to C10K; edge-triggered vs level-triggered\n- io_uring (newer) provides true asynchronous I/O and reduces syscalls. Java NIO `Selector` wraps epoll/kqueue\n- TCP: three-way handshake + slow start + Nagle (disable with `TCP_NODELAY` for low latency) + congestion control; UDP applications must implement reliability themselves when required (QUIC does so in user space)\n\nUnderstanding epoll explains how Netty serves hundreds of thousands of connections with few threads. Zero-copy (`sendfile`) avoids copies between kernel↔user space (Kafka). [[r:Ephemeral port exhaustion]] from missing connection pooling is a common failure at scale.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "15-network-i-o-models.threading-network-programming.q3",
          "q": "Why are keep-alive and connection pooling critical in microservices?",
          "a": "Opening a new TCP+TLS connection for every request is expensive (handshake, slow start).\n- [[g:Keep-alive]] reuses a connection across multiple requests\n- clients calling other services should use a [[g:connection pool]] (HTTP client pool, gRPC channel) → avoids ephemeral-port exhaustion and reduces latency\n\nConfigure maximum connections per route and idle timeout; keep [[o:client idle < server idle]] (to avoid a 'connection reset' when the server closes a keep-alive connection first).\n\n[[r:Missing pooling]] is a common cause of high p99 latency and 'too many open files' errors.\n\n:::deep\n- a TCP+TLS handshake costs roughly 1–2 RTTs + CPU for asymmetric cryptography → at scale, disabling keep-alive wastes CPU and latency\n- size the downstream pool's `maxConnPerRoute` for expected concurrency (too small → queueing; too large → overloads the downstream and consumes ports/RAM)\n- set `idleTimeout` &lt; the server/LB idle timeout ([[r:avoid reusing a connection the peer has already closed → intermittent 'connection reset'/502 errors]])\n- HTTP/2 multiplexing reduces the need for many connections, but long-lived gRPC connections require L7 load distribution; creating too many short-lived connections exhausts ephemeral ports\n\nOTA: services make frequent GDS/PSP calls, so correct pooling + keep-alive + timeouts determine p99 latency; missing pooling is an often-overlooked cause of high latency &amp; 'too many open files'.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "15-network-i-o-models.threading-network-programming.q4",
          "q": "Why is gRPC over HTTP/2 efficient for internal service communication?",
          "a": "gRPC leverages HTTP/2:\n- multiplexes many RPCs over one connection (no new connection per call)\n- compressed headers and four streaming modes\n- compact binary Protobuf payloads + generated stubs from `.proto` files (contract-first, versioned through field numbers)\n\nIt is well suited to low-latency, high-throughput service-to-service communication.\n\n[[r:Limitations]]: browsers cannot call it directly without gRPC-Web/a proxy, and binary payloads are harder to inspect manually → a common pattern is REST at the edge and gRPC internally.\n\n:::deep\n- gRPC uses HTTP/2 flow control for streaming backpressure and supports [[g:deadline propagation]] (the client sets a deadline → it propagates across every hop → downstream services can stop unnecessary work once the deadline expires)\n- Protobuf remains forward/backward-compatible through field numbers (adding optional fields is safe; never reuse a deleted field number); interceptors handle authentication, logging, metrics, and retries\n- [[r:Load-balancing pitfall]]: a long-lived HTTP/2 connection is pinned to one backend → use an L7/lookaside LB/service mesh, or client-side round-robin across multiple subchannels; debug with grpcurl/reflection\n\nOTA: use internal gRPC between search/pricing/inventory/booking services (low latency, strict contracts, and deadline propagation for a time-budgeted flight search), with REST/GraphQL at the edge. [[r:Do not expose gRPC publicly to partners unfamiliar with it]] (REST is easier to integrate).\n:::",
          "difficulty": "extra"
        },
        {
          "id": "15-network-i-o-models.threading-network-programming.q5",
          "q": "How should you combine idempotency, retries, timeouts, backoff, and circuit breakers at the network boundary?",
          "a": "Networks fail, so design for failure:\n- set reasonable [[g:timeouts]] for both connect and read\n- [[g:retry]] only idempotent operations, with *exponential backoff + jitter* (to avoid retry storms)\n- use a [[g:circuit breaker]] to stop calling a failing service (CLOSED→OPEN→HALF_OPEN)\n- use a [[g:bulkhead]] (separate pools per dependency) so one slow downstream cannot consume every thread\n\n[[r:Critical]]: retrying a non-idempotent operation duplicates its side effects (double charge) → every outbound command to a PSP/GDS must carry an idempotency key.\n\n:::deep\n- *timeouts* (connect + read) should be shorter than the upstream timeout so failures surface quickly\n- *retry* only idempotent work, with backoff + [[r:jitter being essential]] (without jitter, every client retries together and creates a thundering herd)\n- cap attempts and enforce a retry budget (do not retry while the downstream is collapsing)\n- a *circuit breaker* stops calls and fails fast (CLOSED→OPEN once failures cross the threshold→HALF_OPEN); a *bulkhead* isolates a pool/semaphore per dependency\n\nFintech/OTA: [[r:retrying NON-idempotent work = double charge/double issue]] → every outbound PSP/GDS command must carry an idempotency key. Combine all four patterns with resilience4j.\n\nBottom line: \"networks fail — design so failures do not propagate &amp; retries do not duplicate side effects.\"\n:::",
          "difficulty": "extra"
        },
        {
          "id": "15-network-i-o-models.threading-network-programming.q6",
          "q": "What is the end-to-end lifecycle of an HTTP request, from DNS resolution until the client renders the response?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 418' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10' text-anchor='middle'>\n<defs><marker id='e1' markerWidth='8' markerHeight='8' refX='6' refY='3' orient='auto'><path d='M0 0 L6 3 L0 6 z' fill='#42536C'/></marker><marker id='e2' markerWidth='8' markerHeight='8' refX='1' refY='3' orient='auto'><path d='M6 0 L0 3 L6 6 z' fill='#0B6E4F'/></marker></defs>\n<text x='6' y='49' fill='#3D4E8F' font-size='9' font-weight='600' text-anchor='start'>1 CLIENT</text>\n<rect x='72' y='26' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='140' y='43' fill='#085239' font-size='9.5' font-weight='600'>DNS resolution</text><text x='140' y='58' fill='#42536C' font-size='8'>browser→OS→recursive</text>\n<path d='M210 46 L220 46' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='222' y='26' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='290' y='43' fill='#085239' font-size='9.5' font-weight='600'>TCP 3-way</text><text x='290' y='58' fill='#42536C' font-size='8'>SYN · SYN-ACK · ACK</text>\n<path d='M360 46 L370 46' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='372' y='26' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='440' y='43' fill='#085239' font-size='9.5' font-weight='600'>TLS handshake</text><text x='440' y='58' fill='#42536C' font-size='8'>hello · key exchange</text>\n<path d='M510 46 L520 46' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='522' y='26' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='590' y='43' fill='#085239' font-size='9.5' font-weight='600'>HTTP request</text><text x='590' y='58' fill='#42536C' font-size='8'>GET /api/v1 + body</text>\n<path d='M340 67 L340 77' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<text x='6' y='101' fill='#3D4E8F' font-size='9' font-weight='600' text-anchor='start'>2 EDGE*</text>\n<rect x='72' y='78' width='136' height='40' rx='7' fill='#F6E8E5' stroke='#AE4536' stroke-dasharray='4 3'/><text x='140' y='95' fill='#7d2f24' font-size='9.5' font-weight='600'>CDN</text><text x='140' y='110' fill='#42536C' font-size='8'>static / cache</text>\n<path d='M210 98 L220 98' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='222' y='78' width='136' height='40' rx='7' fill='#F6E8E5' stroke='#AE4536' stroke-dasharray='4 3'/><text x='290' y='95' fill='#7d2f24' font-size='9.5' font-weight='600'>WAF</text><text x='290' y='110' fill='#42536C' font-size='8'>attack filtering</text>\n<path d='M360 98 L370 98' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='372' y='78' width='136' height='40' rx='7' fill='#F6E8E5' stroke='#AE4536' stroke-dasharray='4 3'/><text x='440' y='95' fill='#7d2f24' font-size='9.5' font-weight='600'>Load Balancer</text><text x='440' y='110' fill='#42536C' font-size='8'>ALB · Nginx · F5</text>\n<path d='M510 98 L520 98' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='522' y='78' width='136' height='40' rx='7' fill='#F6E8E5' stroke='#AE4536' stroke-dasharray='4 3'/><text x='590' y='95' fill='#7d2f24' font-size='9.5' font-weight='600'>Reverse proxy</text><text x='590' y='110' fill='#42536C' font-size='8'>SSL termination</text>\n<path d='M340 119 L340 129' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<text x='6' y='153' fill='#3D4E8F' font-size='9' font-weight='600' text-anchor='start'>3 KERNEL</text>\n<rect x='72' y='130' width='136' height='40' rx='7' fill='#FFFFFF' stroke='#42536C'/><text x='140' y='147' fill='#14233A' font-size='9.5' font-weight='600'>NIC</text><text x='140' y='162' fill='#42536C' font-size='8'>HW IRQ / NAPI</text>\n<path d='M210 150 L220 150' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='222' y='130' width='136' height='40' rx='7' fill='#FFFFFF' stroke='#42536C'/><text x='290' y='147' fill='#14233A' font-size='9.5' font-weight='600'>Ring buffer</text><text x='290' y='162' fill='#42536C' font-size='8'>SoftIRQ</text>\n<path d='M360 150 L370 150' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='372' y='130' width='136' height='40' rx='7' fill='#FFFFFF' stroke='#42536C'/><text x='440' y='147' fill='#14233A' font-size='9.5' font-weight='600'>TCP stack</text><text x='440' y='162' fill='#42536C' font-size='8'>SYN + accept queue</text>\n<path d='M510 150 L520 150' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='522' y='130' width='136' height='40' rx='7' fill='#FFFFFF' stroke='#42536C'/><text x='590' y='147' fill='#14233A' font-size='9.5' font-weight='600'>Socket buffer</text><text x='590' y='162' fill='#42536C' font-size='8'>recv buffer (OS)</text>\n<path d='M340 171 L340 181' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<text x='6' y='205' fill='#3D4E8F' font-size='9' font-weight='600' text-anchor='start'>4 TOMCAT</text>\n<rect x='72' y='182' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='140' y='199' fill='#3D4E8F' font-size='9.5' font-weight='600'>Acceptor</text><text x='140' y='214' fill='#42536C' font-size='8'>accept() socket</text>\n<path d='M210 202 L220 202' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='222' y='182' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='290' y='199' fill='#3D4E8F' font-size='9.5' font-weight='600'>Poller</text><text x='290' y='214' fill='#42536C' font-size='8'>Selector / epoll</text>\n<path d='M360 202 L370 202' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='372' y='182' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='440' y='199' fill='#3D4E8F' font-size='9.5' font-weight='600'>Worker pool</text><text x='440' y='214' fill='#42536C' font-size='8'>maxThreads = 200</text>\n<path d='M510 202 L520 202' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='522' y='182' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='590' y='199' fill='#3D4E8F' font-size='9.5' font-weight='600'>Coyote HTTP11</text><text x='590' y='214' fill='#42536C' font-size='8'>parse → Servlet</text>\n<path d='M340 223 L340 233' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<text x='6' y='257' fill='#3D4E8F' font-size='9' font-weight='600' text-anchor='start'>5 SPRING</text>\n<rect x='72' y='234' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='140' y='251' fill='#3D4E8F' font-size='9.5' font-weight='600'>Filter chain</text><text x='140' y='266' fill='#42536C' font-size='8'>Cors→Security→MDC</text>\n<path d='M210 254 L220 254' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='222' y='234' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='290' y='251' fill='#3D4E8F' font-size='9.5' font-weight='600'>DispatcherServlet</text><text x='290' y='266' fill='#42536C' font-size='8'>mapping + adapter</text>\n<path d='M360 254 L370 254' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='372' y='234' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='440' y='251' fill='#3D4E8F' font-size='9.5' font-weight='600'>Controller</text><text x='440' y='266' fill='#42536C' font-size='8'>@RestController</text>\n<path d='M510 254 L520 254' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='522' y='234' width='136' height='40' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='590' y='251' fill='#3D4E8F' font-size='9.5' font-weight='600'>Service</text><text x='590' y='266' fill='#42536C' font-size='8'>@Transactional</text>\n<path d='M340 275 L340 285' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<text x='6' y='309' fill='#3D4E8F' font-size='9' font-weight='600' text-anchor='start'>6 DATA</text>\n<rect x='72' y='286' width='136' height='40' rx='7' fill='#FFFBF2' stroke='#9A7A41'/><text x='140' y='303' fill='#9A7A41' font-size='9.5' font-weight='600'>HikariCP</text><text x='140' y='318' fill='#42536C' font-size='8'>pool of 10–20 conns</text>\n<path d='M210 306 L220 306' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='222' y='286' width='136' height='40' rx='7' fill='#FFFBF2' stroke='#9A7A41'/><text x='290' y='303' fill='#9A7A41' font-size='9.5' font-weight='600'>JDBC driver</text><text x='290' y='318' fill='#42536C' font-size='8'>statement / network</text>\n<path d='M360 306 L370 306' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='372' y='286' width='136' height='40' rx='7' fill='#FFFBF2' stroke='#9A7A41'/><text x='440' y='303' fill='#9A7A41' font-size='9.5' font-weight='600'>Database</text><text x='440' y='318' fill='#42536C' font-size='8'>MySQL / PostgreSQL</text>\n<path d='M510 306 L520 306' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<rect x='522' y='286' width='136' height='40' rx='7' fill='#FFFBF2' stroke='#9A7A41'/><text x='590' y='303' fill='#9A7A41' font-size='9.5' font-weight='600'>Redis</text><text x='590' y='318' fill='#42536C' font-size='8'>cache (in parallel)</text>\n<path d='M340 327 L340 337' stroke='#42536C' stroke-width='1.6' fill='none' marker-end='url(#e1)'/>\n<text x='6' y='361' fill='#085239' font-size='9' font-weight='600' text-anchor='start'>7 RESPONSE</text>\n<rect x='72' y='338' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='140' y='355' fill='#085239' font-size='9.5' font-weight='600'>Client render</text><text x='140' y='370' fill='#42536C' font-size='8'>JSON → DOM</text>\n<path d='M220 358 L210 358' stroke='#0B6E4F' stroke-width='1.6' fill='none' marker-end='url(#e2)'/>\n<rect x='222' y='338' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='290' y='355' fill='#085239' font-size='9.5' font-weight='600'>TLS encrypt</text><text x='290' y='370' fill='#42536C' font-size='8'>if HTTPS</text>\n<path d='M370 358 L360 358' stroke='#0B6E4F' stroke-width='1.6' fill='none' marker-end='url(#e2)'/>\n<rect x='372' y='338' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='440' y='355' fill='#085239' font-size='9.5' font-weight='600'>Socket write</text><text x='440' y='370' fill='#42536C' font-size='8'>flush send buffer</text>\n<path d='M520 358 L510 358' stroke='#0B6E4F' stroke-width='1.6' fill='none' marker-end='url(#e2)'/>\n<rect x='522' y='338' width='136' height='40' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='590' y='355' fill='#085239' font-size='9.5' font-weight='600'>Filter unwind</text><text x='590' y='370' fill='#42536C' font-size='8'>converter → JSON</text>\n<text x='340' y='394' fill='#3D4E8F' font-size='9'>ONE worker thread is held from steps 4 → 7 · returned only after flush completes (keep-alive retains the connection)</text>\n<text x='340' y='410' fill='#7E8DA3' font-size='9'>* EDGE is optional — it may not exist in dev/local environments</text>\n</svg></figure>\n\nUse this map to answer \"where is it slow?\" — each layer has its own bottlenecks and metrics.\n\n- [[b:1 Client]] — DNS (browser cache → OS cache → recursive resolver) → TCP three-way handshake → TLS handshake → send request. Keep-alive lets subsequent requests skip all three setup stages\n- [[b:2 Edge]] *(optional)* — CDN → WAF → Load Balancer → reverse proxy/API gateway: SSL termination, path/host routing, health checks\n- [[b:3 Kernel]] — NIC (hardware interrupt/NAPI) → ring buffer & SoftIRQ → TCP stack (SYN queue, accept queue) → socket receive buffer\n- [[b:4 Tomcat]] — Acceptor `accept()` → Poller (Selector/epoll) → worker thread pool → Coyote HTTP11 parses the request into an `HttpServletRequest`\n- [[b:5 Spring]] — filter chain (`doFilter`) → DispatcherServlet → controller → service (`@Transactional`)\n- [[b:6 Data]] — HikariCP → JDBC driver → DB; Redis cache access may run in parallel\n- [[b:7 Response]] — unwind through the filter chain → socket write → TCP ACK → TLS encryption → client parse/render\n\n[[g:Only ONE worker thread is involved from step 4 through step 7]] — it returns to the pool only after the response has been flushed. Any delay between those points increases *thread hold time*.\n\n<table class='cmp'><thead><tr><th>Layer</th><th>Bottleneck</th><th>Metric / signal</th></tr></thead><tbody>\n<tr><td>1 Client</td><td>DNS latency, TLS handshake</td><td>first request is unusually slow; high CPU without keep-alive</td></tr>\n<tr><td>2 Edge</td><td>LB saturation</td><td>502/504 at the LB with no corresponding application log</td></tr>\n<tr><td>3 Kernel</td><td>accept queue full</td><td>connection refused/timeout, <code>ss -ltn</code> Recv-Q reaches max</td></tr>\n<tr><td>4 Tomcat</td><td>thread pool exhausted</td><td><code>tomcat_threads_busy</code> = max</td></tr>\n<tr><td>5 Spring</td><td>large-payload serialization, GC pause</td><td>high Jackson CPU; p99 spikes in step with the GC cycle</td></tr>\n<tr><td>6 Data</td><td>HikariCP exhausted, slow query/lock, slow Redis</td><td><code>hikaricp_connections_pending</code> &gt; 0</td></tr>\n</tbody></table>\n\n:::warn Pitfall often misattributed to the application\nA full accept queue (`server.tomcat.accept-count` + `net.core.somaxconn`) → the kernel [[r:silently drops SYNs]]: the client sees a timeout/connection refused, but [[r:application logs are completely clean]] and no Spring metric reflects the failure. Inspect `ss -ltn` (Recv-Q/Send-Q) and `netstat -s | grep -i listen`.\n:::\n\n:::deep\n- NIO decouples [[b:connections]] from [[b:threads]]: `maxConnections` (8192) ≫ `maxThreads` (200) → the server can retain many idle connections and consumes a worker thread only while a socket actually has data\n- the practical exhaustion order is usually HikariCP (10–20) first, then Tomcat threads (200), and finally the accept queue (100) → [[r:increasing `maxThreads` when HikariCP is the bottleneck only lengthens the queue and worsens p99]]\n- timeouts must decrease at each layer: client &gt; LB &gt; Tomcat `connection-timeout` &gt; HTTP client timeout &gt; query timeout. Reversing that order stacks retries and multiplies load during an incident\n- the client-side pool/keep-alive `idle-timeout` must be [[o:shorter than]] the LB/DB idle timeout; otherwise the client reuses a connection the peer has already closed → intermittent 502/'connection reset' errors\n- `@Transactional` opens at step 5, but JSON serialization occurs at the end of step 5 (after commit) → lazy loading there throws `LazyInitializationException`; moreover, [[r:calling an external API while holding a DB connection]] turns network latency into connection hold time\n- Java 21 virtual threads (`spring.threads.virtual.enabled=true`) remove the `maxThreads` ceiling at step 4 but [[r:do not remove the HikariCP ceiling at step 6]] — concurrency to the DB/downstream must still be bounded\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "15-network-i-o-models.threading-network-programming.q7",
          "q": "How do deadlines and retries behave when a request body or mutation may have partially completed?",
          "a": "Use one absolute deadline for the caller's intent, then allocate smaller budgets for connection acquisition, connect, request headers, upload or response body, and downstream work. A timeout is not proof that the server did nothing: the peer may have accepted all bytes, committed the mutation, or failed before execution.\n\nRetry only when the bytes can be replayed, the error is classified as transient, and the operation has a stable idempotency key or equivalent deduplication identity. For a non-replayable stream, use one attempt or a resumable protocol with a part/session identity. After an ambiguous timeout, query the operation or provider status before creating a new intent.\n\nA circuit breaker can stop additional calls; it cannot decide whether an already-sent mutation committed. Keep retry ownership at one layer, budget attempts and bytes, propagate cancellation, and record an UNKNOWN outcome for reconciliation rather than silently converting it to failure.\n\n:::tip Boundary\nUse q5 for the general retry composition. This item adds the large-body and partial-completion rule that prevents a retry from duplicating a side effect.\n:::",
          "difficulty": "advanced"
        }
      ]
    }
  ]
}
