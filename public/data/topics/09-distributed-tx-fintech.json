{
  "sections": [
    {
      "title": "Distributed transaction patterns",
      "items": [
        {
          "id": "09-distributed-tx-fintech.distributed-transaction-patterns.q1",
          "q": "Why avoid 2PC in microservices?",
          "a": "2PC (prepare → commit via coordinator) suffers from:\n- [[r:blocking]]: participants hold resource locks from prepare through commit; if the coordinator dies, they're stuck in-doubt indefinitely\n- [[r:poor availability]]: one slow participant stalls the whole transaction\n- [[r:doesn't scale]]: holding locks across the network, tight coupling\n\n:::tip Bottom line\nmodern distributed systems trade 'distributed atomicity' for [[g:controlled eventual consistency]] via Saga + Outbox + idempotency.\n:::\n\n:::deep\n- the prepare phase holds *resource locks* until commit (blocking other transactions); if the coordinator dies after prepare, the participant is stuck [[r:in-doubt]] (holding the lock indefinitely, unable to decide on its own) — needs manual intervention\n- one slow/failing participant stalls the whole transaction (poor availability, doesn't scale)\n\n2PC is only appropriate in a narrow, homogeneous scope (XA between DB+JMS within a single service), [[r:not for long-running business flows spanning multiple services]] like OTA booking.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.distributed-transaction-patterns.q2",
          "q": "Saga: choreography vs orchestration? Compensating transaction?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 260' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11' text-anchor='middle'>\n<text x='170' y='20' fill='#085239' font-weight='600'>ORCHESTRATION</text>\n<rect x='110' y='34' width='120' height='34' rx='6' fill='#E7EAF6' stroke='#3D4E8F'/><text x='170' y='55' fill='#3D4E8F'>Orchestrator</text>\n<g><rect x='30' y='110' width='90' height='30' rx='5' fill='#fff' stroke='#42536C'/><text x='75' y='130'>Debit</text>\n<rect x='130' y='110' width='90' height='30' rx='5' fill='#fff' stroke='#42536C'/><text x='175' y='130'>PSP</text>\n<rect x='230' y='110' width='80' height='30' rx='5' fill='#fff' stroke='#42536C'/><text x='270' y='130'>Credit</text></g>\n<g stroke='#3D4E8F' stroke-width='1.4' marker-end='url(#ar6)'><line x1='150' y1='68' x2='80' y2='108'/><line x1='170' y1='68' x2='175' y2='108'/><line x1='195' y1='68' x2='265' y2='108'/></g>\n<text x='170' y='165' fill='#42536C'>one conductor orchestrates &#8594; clear flow, easy to monitor</text>\n<line x1='350' y1='30' x2='350' y2='190' stroke='#C6CFDC' stroke-dasharray='4 4'/>\n<text x='520' y='20' fill='#7d2f24' font-weight='600'>CHOREOGRAPHY</text>\n<g><rect x='400' y='80' width='80' height='30' rx='5' fill='#F6E8E5' stroke='#AE4536'/><text x='440' y='100'>Debit</text>\n<rect x='500' y='80' width='80' height='30' rx='5' fill='#F6E8E5' stroke='#AE4536'/><text x='540' y='100'>PSP</text>\n<rect x='600' y='80' width='60' height='30' rx='5' fill='#F6E8E5' stroke='#AE4536'/><text x='630' y='100'>Credit</text></g>\n<g stroke='#AE4536' stroke-width='1.4' marker-end='url(#ar6b)'><line x1='480' y1='95' x2='498' y2='95'/><line x1='580' y1='95' x2='598' y2='95'/></g>\n<text x='440' y='128' fill='#AE4536'>event</text><text x='540' y='128' fill='#AE4536'>event</text>\n<text x='525' y='165' fill='#42536C'>services react to events &#8594; loose coupling</text>\n<text x='340' y='220' fill='#085239'>A failed step &#8594; runs a COMPENSATING tx (a reversing entry, must be idempotent) instead of a real rollback</text>\n<defs><marker id='ar6' markerWidth='8' markerHeight='8' refX='7' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#3D4E8F'/></marker>\n<marker id='ar6b' markerWidth='8' markerHeight='8' refX='7' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#AE4536'/></marker></defs>\n</svg></figure>\n\n[[g:Saga]]: split a long transaction into a chain of local transactions; if a step fails, run a [[g:compensating transaction]] (a business-level undo, e.g. a reversing entry — not an actual DB rollback).\n\n- [[b:Choreography]]: services publish/listen to events, no conductor — loose coupling, but the overall flow is hard to see at a glance\n- [[b:Orchestration]]: an orchestrator drives each step — clear flow, easier to monitor/handle errors, at the cost of a central point\n\nCompensation must be [[o:idempotent]] and safe to run multiple times.\n\n:::warn Note\nSaga = eventual consistency → there's a window of 'imbalance'. The UI/business logic must accept a PENDING state, [[r:never act on a balance that hasn't settled]].\n:::\n\n:::deep\n- needs a [[g:saga log/state store]] (the orchestrator tracks which step it's on) to recover after a crash and resume; every step + its compensation must be idempotent\n- compensation isn't always feasible (an email already sent, a real ticket already issued) → use a [[o:pivot transaction]]: order the 'irreversible' steps last, so after the pivot point only forward progress is possible, no going back\n\nOTA: hold a seat (reversible) → charge payment (reversible via refund) → issue the ticket through the GDS (hard to reverse → placed last). The intermediate state exposed to the UI is PENDING/PROCESSING.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.distributed-transaction-patterns.q3",
          "q": "The Outbox pattern, and why is dual-write dangerous?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 210' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11' text-anchor='middle'>\n<rect x='30' y='40' width='190' height='110' rx='8' fill='#E4F1EB' stroke='#0B6E4F'/>\n<text x='125' y='32' fill='#085239' font-weight='600'>1 LOCAL TRANSACTION</text>\n<rect x='48' y='58' width='154' height='36' rx='5' fill='#fff' stroke='#42536C'/><text x='125' y='80' fill='#14233A'>UPDATE wallet</text>\n<rect x='48' y='102' width='154' height='36' rx='5' fill='#fff' stroke='#42536C'/><text x='125' y='124' fill='#14233A'>INSERT outbox(event)</text>\n<rect x='290' y='70' width='120' height='50' rx='6' fill='#E7EAF6' stroke='#3D4E8F'/><text x='350' y='90' fill='#3D4E8F'>Publisher</text><text x='350' y='106' fill='#42536C'>poll / CDC</text>\n<rect x='470' y='70' width='90' height='50' rx='6' fill='#fff' stroke='#42536C'/><text x='515' y='98' fill='#14233A'>Broker</text>\n<rect x='590' y='70' width='80' height='50' rx='6' fill='#F6E8E5' stroke='#AE4536'/><text x='630' y='90' fill='#7d2f24'>Consumer</text><text x='630' y='106' fill='#AE4536'>idempotent</text>\n<g stroke='#42536C' stroke-width='1.6' marker-end='url(#ar7_93)'>\n<line x1='220' y1='95' x2='288' y2='95'/><line x1='410' y1='95' x2='468' y2='95'/><line x1='560' y1='95' x2='588' y2='95'/></g>\n<text x='340' y='180' fill='#AE4536'>Atomic DB&#8596;event (no dual-write) &#183; at-least-once &#8594; consumer must dedup</text>\n<defs><marker id='ar7_93' markerWidth='9' markerHeight='9' refX='7' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#42536C'/></marker></defs>\n</svg></figure>\n\n[[r:Dual write]] (writing to the DB and publishing to the broker as separate steps) → if either side fails they drift out of sync: the DB says the money was transferred but the other service never received the event.\n\n[[g:Outbox]]: write the event to an outbox table *in the same transaction* as the business change → atomic; a publisher (CDC/polling) reads the outbox and publishes (at-least-once → consumer must be idempotent).\n\n:::tip Tie to your background\nPublisher Confirms (broker acknowledges receipt) + Outbox (atomic DB↔publish) together close the gap.\n:::\n\n:::deep\n- dual-write is dangerous because the DB and the broker/HTTP call don't share a transaction → they fall out of sync when one side fails, and the bug is hard to catch (only surfaces on rare network failures)\n- Outbox turns it into *one* atomic local DB write (state + event in the same tx), pushing the 'publish externally' part to an async publisher (retries until delivery succeeds, then marks it SENT)\n\nOTA: [[r:avoid `save(booking); kafka.send(event)`]] — if the send fails after commit, ticketing never learns the booking is PAID. Outbox + CDC (Debezium) is the standard way to 'reliably publish events' without blocking the business flow waiting on the broker.\n\n- Decide *what* is allowed into the outbox: an integration event with a public, versioned contract — not an internal domain event (24-domain-driven-design.tactical-modelling-it.q3).\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.distributed-transaction-patterns.q4",
          "q": "How does TCC (Try-Confirm-Cancel) differ from Saga?",
          "a": "TCC splits each operation into 3 phases:\n- [[b:Try]]: reserve/hold a resource in advance (e.g. freeze an amount of money)\n- [[b:Confirm]]: actually apply the change on top of what was held\n- [[b:Cancel]]: release the held resource if something fails\n\n<table class=\"cmp\"><thead><tr><th></th><th>Saga</th><th>TCC</th></tr></thead><tbody>\n<tr><td>Approach</td><td>do it for real, then compensate</td><td>reserve first, confirm later</td></tr>\n<tr><td>Intermediate state</td><td>visible externally</td><td>hidden (just 'held')</td></tr>\n<tr><td>Isolation</td><td>weaker</td><td>better</td></tr>\n<tr><td>Cost</td><td>1 op + 1 compensation</td><td>3 idempotent phases per service</td></tr>\n</tbody></table>\n\n:::deep\n- Saga *compensates after the fact* (money is deducted first, then refunded if needed); TCC *reserves up front* → avoids exposing intermediate state, better isolation\n- trade-off: every service must implement all 3 idempotent phases plus manage the 'reservation' (auto-release via TTL if it hangs)\n\nOTA: seat-hold is exactly TCC (Try=hold the seat with a TTL, Confirm=issue the ticket, Cancel=release); payment = Try=authorize (hold the funds), Confirm=capture, Cancel=void — this matches the PSP authorize/capture model exactly.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.distributed-transaction-patterns.q5",
          "q": "Two services need to stay consistent without a distributed transaction — what are the options?",
          "a": "In order of preference:\n- [[g:Merge into one bounded context]]: if the two pieces of data need strong consistency (both legs of a ledger entry) → put them in the same service/DB, local ACID. [[r:Don't split into microservices just because it's fashionable.]]\n- [[b:Saga + Outbox]] if a split is unavoidable: accept eventual consistency + compensation\n- [[b:Idempotency + retry]] on every step\n- [[b:Reconciliation]] jobs to periodically detect & fix drift\n\n:::tip Key takeaway\n'service boundaries should follow consistency boundaries'. If two things must be atomic with each other, that's a signal they belong in the same service.\n:::\n\n:::deep\n- (1) merge the bounded context if strong consistency is required (both legs of a ledger entry, holding a seat + decrementing inventory) → local ACID\n- (2) if a split is unavoidable → Saga + Outbox + eventual consistency + compensation\n- (3) idempotency + retry on every step; (4) periodic reconciliation to fix drift\n\nOTA: 'hold seat' + 'decrement inventory' should live in the same Inventory service (atomic); 'booking' + 'payment' can be split (Saga).\n\n[[r:A misplaced boundary is the root cause of a distributed monolith.]]\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.distributed-transaction-patterns.q6",
          "q": "Distributed lock (Redis/Redlock) — when to use it, and what are the risks?",
          "a": "Used to serialize an operation across instances (only one worker runs the job): `SET key val NX PX ttl`.\n\n[[r:Risk]]: the TTL expires before processing finishes → two holders enter at once.\n\nMitigations:\n- [[g:fencing token]] (a monotonically increasing number; the resource rejects an older token)\n- keep the critical section short\n\n:::warn Fintech\ndon't rely SOLELY on a distributed lock for money correctness. Let the DB be the final arbiter (unique constraint, optimistic locking, conditional update); [[o:the lock only reduces contention]].\n:::\n\n:::deep\n- `SET key val NX PX ttl` for mutual exclusion across instances\n- [[r:fatal risk]]: the TTL expires before processing finishes → two holders enter the critical section at once → [[g:fencing token]] (the resource rejects a token older than one it has already seen)\n- Redlock (multi-node) is controversial (Kleppmann) — clock skew/GC pauses can still break its safety guarantees\n\nFintech: [[r:do NOT rely solely on a lock for money/seat correctness]] — let the DB be the final arbiter (unique constraint, optimistic version, conditional update); the lock only *reduces contention*. Lock for 'coordination', DB for 'correctness'.\n:::",
          "difficulty": "core"
        }
      ]
    },
    {
      "title": "Correctness where money is involved",
      "items": [
        {
          "id": "09-distributed-tx-fintech.correctness-where-money-is-involved.q1",
          "q": "Real-world scenario: a call to a PSP/bank times out and you don't know whether the money was already deducted?",
          "a": "[[r:Unknown state]] — never default to assuming failure or success.\n\n- send every PSP call with an [[g:idempotency key]] from the start → safe retries, no double-charging\n- on timeout → mark it `UNKNOWN/PENDING`, [[r:do NOT tell the user it succeeded]]\n- [[g:status query/inquiry]] to the PSP, or wait for the webhook, to learn the real outcome\n- end-of-day [[g:reconciliation]] against the settlement file to close out pending transactions; have a business-level timeout + auto-reversal\n\n:::tip Bottom line\n'when unsure, don't guess — query again and reconcile'. Idempotency + reconciliation are the safety net of every payment system.\n:::\n\n:::deep\n- calls to the GDS (Amadeus/Sabre) or a PSP can both time out in an indeterminate state\n- invariant rule: every outbound call carries an idempotency key (or reuses booking_id/order_id), stored as `PENDING`, [[r:never infer success on your own]]\n- after a timeout: call a *retrieve/status API* (GDS PNR lookup; PSP inquiry) or wait for the webhook; run reconciliation against the end-of-day settlement file; have a *business-level timeout* + auto-void/refund once it expires\n\nThis is exactly the kind of 'real-world challenge' that fits well into a 'most challenging project' story.\n\n**Concrete unknown-outcome state machine (PostgreSQL-flavoured):**\n<pre><code>CREATE TABLE payment_attempt(\n  id uuid PRIMARY KEY,\n  idempotency_key text UNIQUE NOT NULL,\n  provider_ref text UNIQUE,\n  status text NOT NULL CHECK (status IN\n    ('CREATED','SUBMITTED','UNKNOWN','SUCCEEDED','FAILED'))\n);\n-- transaction 1: claim one execution\nUPDATE payment_attempt SET status='SUBMITTED'\n WHERE id=:id AND status='CREATED';  -- 1 row owns the provider call\nCOMMIT;\n-- call the PSP with the SAME idempotency_key, outside the DB transaction\n-- timeout: do not mark FAILED\nUPDATE payment_attempt SET status='UNKNOWN'\n WHERE id=:id AND status='SUBMITTED';\n-- reconciler queries PSP by key/reference, then applies one legal transition\nUPDATE payment_attempt SET status='SUCCEEDED', provider_ref=:ref\n WHERE id=:id AND status IN ('SUBMITTED','UNKNOWN');</code></pre>\n\nThe client receives `PENDING/UNKNOWN`, not a fabricated failure, until webhook/status-query reconciliation proves the outcome.\n\n- **Benefit:** a timeout cannot trigger a second logical charge.\n- **Cost:** more states, a reconciliation worker and longer user-visible completion.\n- **Invariant:** the unique key and conditional transitions guard every callback, retry and manual repair.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.correctness-where-money-is-involved.q2",
          "q": "Idempotency in payments: how do you implement it airtight?",
          "a": "The client sends an [[g:Idempotency-Key]] with every payment intent. The server stores key→result; a duplicate request returns the stored result instead of executing again.\n\n<pre><code><span class=\"k\">INSERT INTO</span> idempotency_keys(key,status,response)\n<span class=\"k\">VALUES</span>(?, <span class=\"s\">'IN_PROGRESS'</span>, <span class=\"k\">NULL</span>);  <span class=\"c\">-- unique violation =&gt; retry</span></code></pre>\n\nStore the key + result in the *same tx* as the business change (or via outbox) → no gap where 'the action ran but the key wasn't saved'. Handle the IN_PROGRESS state when a duplicate request arrives early.\n\n:::deep\n- server, within *one transaction*: INSERT the key (unique, IN_PROGRESS) → a unique violation means a duplicate request → return the stored result (or 409 if still IN_PROGRESS)\n- perform the business logic + store the response tied to the key in the same tx (or via outbox) → [[g:no gap where 'done but key not saved']]\n- keys have a sensible TTL (scoped per endpoint + user)\n\nOTA: every call out to a PSP/GDS carries an idempotency key (or order_id) → safe retries, no double-charge/double-issue. Race where two duplicate requests arrive nearly simultaneously: the second one sees IN_PROGRESS → poll/wait or return 409, [[r:never execute in parallel]].\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.correctness-where-money-is-involved.q3",
          "q": "Double-entry bookkeeping — why is it the gold standard for a ledger?",
          "a": "Every transaction has ≥2 entries: debit + credit, [[g:sum = 0]]. Money is never created or destroyed.\n\nThe ledger is [[g:append-only/immutable]]:\n- never UPDATE/DELETE a past entry\n- fix mistakes with a *reversing entry* → the audit trail is baked into the design\n\nBalance = sum of entries (or a materialized value reconciled periodically). Enforce the invariant `SUM(amount)=0` per transaction.\n\n:::tip Bottom line\n'the ledger is an immutable book of record — every change is a new entry' — a strong closing line for a fintech system.\n:::\n\n:::deep\n- table `ledger_entry(id, txn_id, account_id, direction, amount, currency, created_at)`, append-only; the constraint `SUM(signed_amount) per txn_id = 0` is checked both in the app and by a reconciliation job\n- balance: compute on-the-fly (`SUM` per account) or keep a materialized `account_balance` updated in the same tx + reconciled periodically\n- [[r:never UPDATE/DELETE an entry]] — fix it with a *reversing entry*; add a unique `idempotency_key` to prevent duplicate writes\n- multi-currency: each entry carries its currency + the exchange rate at that point in time; [[r:never mix currencies in a single sum]]\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.correctness-where-money-is-involved.q4",
          "q": "Why not use float for money? What's the correct way to store it?",
          "a": "Binary float/double cannot represent 0.1/0.2 exactly → [[r:accumulated rounding drift]] as values add up.\n\nUse instead:\n- `DECIMAL(p,s)` or minor units (cents) as a BIGINT\n- Java `BigDecimal` constructed from a [[o:String]] (never from a double!)\n- an explicit rounding mode (HALF_EVEN)\n- store the currency code alongside the amount, [[r:never add amounts of different currencies]]\n\n<pre><code><span class=\"k\">new</span> BigDecimal(<span class=\"s\">\"0.1\"</span>).add(<span class=\"k\">new</span> BigDecimal(<span class=\"s\">\"0.2\"</span>))\n    .setScale(<span class=\"n\">2</span>, RoundingMode.HALF_EVEN); <span class=\"c\">// 0.30</span></code></pre>\n\n:::deep\n- binary double/float cannot represent 0.1/0.2 exactly (IEEE 754) → drift accumulates across many transactions → mismatches at reconciliation time → violates the ledger invariant\n- `BigDecimal` constructed from a *String* (never a double!), an explicit rounding mode (`HALF_EVEN`, 'banker's rounding', reduces bias), scale set per currency\n- [[r:`new BigDecimal(0.1)` (from a double) still carries the error]] — it must be `new BigDecimal(\"0.1\")`\n\nOTA multi-currency: every `ledger_entry` carries a currency + a minor-unit amount; totals are checked per currency; rounding on currency conversion must be consistent &amp; auditable.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.correctness-where-money-is-involved.q5",
          "q": "How do you prevent double-spend / balance races under high load?",
          "a": "[[r:Never 'read the balance then subtract in the app']] (a race). Let the DB make it atomic:\n\n- [[g:conditional update]]: `UPDATE acc SET balance=balance-? WHERE id=? AND balance>=?` — 0 rows affected = insufficient funds\n- [[b:optimistic locking]] (a version column) + retry; or [[b:pessimistic]] `SELECT ... FOR UPDATE` under high contention\n- append-only ledger: balance is derived from entries, consistency is enforced at the DB layer\n\n:::tip Bottom line\nmoney correctness must be anchored to DB-level constraints (atomic, unique, conditional), not app logic or a distributed lock.\n:::\n\n:::deep\nClassify the business case:\n- 'countable, must not go negative' (balance, seats, ticket inventory) → conditional update / optimistic\n- 'must be unique' (1 seat, 1 person) → unique constraint on (flight, seat, status)\n\nOTA seat: `UPDATE seat SET status='HELD', hold_by=?, hold_exp=now()+'7m' WHERE id=? AND status='FREE'` — atomic, 0 rows = lost the race. [[r:Avoid 'SELECT to check, then INSERT']] (a TOCTOU race).\n\nExtremely high load on a hot flight → serialize per-flight via a queue, or bucket seats to reduce contention. An idempotency key on the booking request prevents a double 'book' click.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "09-distributed-tx-fintech.correctness-where-money-is-involved.q6",
          "q": "Eventual consistency in fintech: where is it acceptable, and where is it forbidden?",
          "a": "- [[r:FORBIDDEN for eventual]]: available balance used to decide spending, both legs of one ledger entry, limit checks, anti-double-spend\n- [[g:ACCEPTABLE for eventual]]: displayed transaction history, notifications, reports/analytics, reward points\n\nPrinciple: anywhere data is used to *make a decision about money* → must be [[g:strong consistency]].\n\n:::deep\n- FORBIDDEN for eventual: available balance used to decide spending, both legs of one ledger entry, limit checks/seat inventory checks before finalizing, anti-double-spend\n- ACCEPTABLE for eventual: displayed history, notifications, analytics, reward points, displayed search/availability\n\nOTA: showing '3 seats left' can be slightly stale (eventual, cached), but the *seat-hold/payment* step must [[g:re-validate atomically against inventory]] (strong) — [[r:never let a user pay based on stale 'seats left' data]]. Design the UI around this boundary (PENDING, re-validate at checkout).\n:::",
          "difficulty": "extra"
        }
      ]
    }
  ]
}
