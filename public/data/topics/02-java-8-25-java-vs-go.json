{
  "sections": [
    {
      "title": "Release timeline",
      "items": [
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q1",
          "q": "How did Java 8 change the way Java is written?",
          "a": "Java 8's biggest turning point:\n- [[g:lambda]] + [[g:Stream API]] (functional, collection-processing pipeline)\n- [[g:Optional]] (reduces NPEs)\n- default methods on interfaces\n- [[g:java.time]] (immutable Date/Time API replacing the old bug-ridden Date/Calendar)\n\nJava 8 is the longest-running LTS; many legacy systems still sit here.\n\n:::deep\n- Streams are [[b:lazy]] (only execute when there's a terminal op); `parallelStream()` uses the common ForkJoinPool — [[r:be careful]] with I/O tasks or when running inside a web container\n- `Optional` is meant for *return types* to express 'may be absent' — [[r:should NOT]] be used as a field/param (anti-pattern)\n- `java.time` is immutable and timezone-explicit (`Instant`/`ZonedDateTime`) — critical for multi-timezone OTA (local departure time vs UTC)\n- default methods: evolve an interface without breaking implementers\n\n[[r:Pitfall]]: overusing nested streams makes code harder to read/debug than a plain loop.\n\nTrivia: lambdas do NOT compile to an anonymous class but to `invokedynamic` + LambdaMetafactory (class generated at runtime) &#8594; fewer generated classes.\n:::",
          "difficulty": "core"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q2",
          "q": "What does Java 11 bring that matters (compared to 8)?",
          "a": "- `var` for local variables (type inference)\n- standard [[g:HttpClient]] (HTTP/2, async) replacing the old HttpURLConnection\n- run a single `.java` file without manual compilation\n- removes the Java EE modules (JAXB, JAX-WS) from the JDK; G1 becomes default; adds ZGC/Epsilon (experimental)\n\nThis is the milestone many enterprises jump from 8 to in order to escape end-of-life.\n\n:::deep\n- `var` is just [[b:compile-time type inference]] (not dynamic typing) — still static, still type-safe\n- new `HttpClient`: HTTP/2, async (`sendAsync` returns a CompletableFuture)\n- [[r:this is usually where 8→11 migrations break]]: removed Java EE modules (JAXB, JAX-WS, CORBA) → must add the dependency yourself\n- also introduces Epsilon GC (no-op, for benchmarking) + experimental ZGC\n\nDeeper: the pain is mostly in removed modules/APIs and tightened reflection (illegal access), not syntax.\n\n- Number worth remembering: 8 to 11 is the [[o:most painful]] step (Java EE modules removed, illegal-access tightened); 11 to 17 to 21 is mostly just bumping dependencies. Anyone still stuck on 8 should target 17 or 21 directly rather than stopping at 11 (02-java-8-25-java-vs-go.release-timeline.q7).\n:::",
          "difficulty": "core"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q3",
          "q": "How did Java 13–16 lay the groundwork for 17 (text blocks, switch, records)?",
          "a": "The [[o:incubation]] phase — previews to gather feedback before finalizing in 17:\n- [[g:text blocks]] (multi-line strings)\n- [[g:switch expression]] (returns a value, no fall-through)\n- previews of records, sealed classes, pattern matching for instanceof\n\n:::deep\n- [[g:switch expression]]: returns a value, arrow syntax, no fall-through, exhaustive → fewer forgotten-`break` bugs\n- [[g:text block]] (triple double-quote): clean multi-line SQL/JSON/HTML\n- records &amp; sealed &amp; pattern matching for instanceof went through preview in 14–16\n- Helpful NullPointerException (14) points out exactly which variable was null\n\nDeeper: the *preview feature* mechanism (`--enable-preview`) lets Java evolve quickly without locking in an API that isn't mature yet.\n\n- Concretely: `switch` returns a value (`case A -> 1;`) so you can no longer forget `break`; text blocks let multi-line SQL skip string concatenation; `if (o instanceof Booking b)` skips manual casting.\n- This is also why 17 (02-java-8-25-java-vs-go.release-timeline.q4) has no truly 'brand-new' feature: 13–16 already ran everything through preview, 17 just stamps it final.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q4",
          "q": "Java 17 (LTS) — what's the key feature set?",
          "a": "- [[g:Records]]: immutable data classes, with constructor/equals/hashCode/toString generated for you\n- [[g:Sealed classes]]: restrict the set of subclasses — tight domain modeling, enables exhaustive switch\n- [[g:Pattern matching for instanceof]]: less manual casting\n- strongly encapsulated JDK internals; numerous performance improvements\n\nJava 17 was the recommended LTS throughout 2021–2023, and is still very popular.\n\n:::deep\n[[g:sealed + record + pattern matching switch]] = *algebraic data type*:\n- enumerates every variant (compiler catches missing branches)\n- immutable, exhaustive handling\n- a great fit for state machines (booking: `Held | Paid | Issued | Cancelled`)\n\n- Records generate equals/hashCode/toString for you, but remember to defensively copy mutable fields (List) in the compact constructor\n- Java 17 tightens JDK internals encapsulation → 'illegal' reflection into sun.* is blocked, [[r:some older libs break]]\n- the foundation for Spring Boot 3 (requires Java 17 minimum)\n:::",
          "difficulty": "core"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q5",
          "q": "Java 21 (LTS) — why is it a 'game-changer' for concurrency?",
          "a": "- [[g:Virtual Threads]] became final: thread-per-request code can handle much higher I/O concurrency without a large platform-thread pool.\n- [[g:Pattern matching for `switch`]] and record patterns make closed data models concise and type-safe.\n- Sequenced Collections standardize first/last/reversed operations.\n- Generational ZGC became available; Structured Concurrency and Scoped Values were still preview APIs in Java 21.\n\n**An exhaustive Java 21 `switch` must cover every guarded case:**\n\n<pre><code><span class=\"k\">sealed interface</span> Event <span class=\"k\">permits</span> Refund, Charge {}\n<span class=\"k\">record</span> Refund(String id, <span class=\"k\">long</span> amountCents) <span class=\"k\">implements</span> Event {}\n<span class=\"k\">record</span> Charge(String id, <span class=\"k\">long</span> amountCents) <span class=\"k\">implements</span> Event {}\n<span class=\"c\">    </span>\n<span class=\"k\">switch</span> (event) {\n    <span class=\"k\">case</span> Refund(<span class=\"k\">var</span> id, <span class=\"k\">var</span> amount) <span class=\"k\">when</span> amount &gt; <span class=\"n\">0</span> -&gt; <span class=\"f\">refund</span>(id, amount);\n    <span class=\"k\">case</span> Refund(<span class=\"k\">var</span> id, <span class=\"k\">var</span> amount)                 -&gt; <span class=\"k\">throw new</span> IllegalArgumentException(<span class=\"s\">\"amount\"</span>);\n    <span class=\"k\">case</span> Charge c                                      -&gt; <span class=\"f\">charge</span>(c);\n    <span class=\"k\">case null</span>                                          -&gt; <span class=\"k\">throw new</span> IllegalArgumentException(<span class=\"s\">\"event\"</span>);\n}</code></pre>\n\n:::deep\nThe migration value is larger than a feature checklist:\n\n- Virtual threads let blocking JDBC/HTTP code scale without callback conversion, but CPU-bound work still needs bounded parallelism and downstream capacity remains finite.\n- On JDK 21–23, blocking under `synchronized` could pin a carrier. JDK 24 removed monitor-related pinning, so audit by target runtime rather than applying that workaround forever.\n- Record patterns plus a sealed hierarchy allow compile-time exhaustiveness. A guarded pattern covers only values satisfying its guard; the second `Refund` arm above is required.\n- Scoped Values became final in JDK 25; Structured Concurrency remains preview in JDK 25. Keep preview APIs behind a deliberate build/runtime flag and expect source migration.\n\nFor a Spring Boot blocking service, enabling virtual threads can be a small configuration change, but capacity tests must still cover DB connections, partner quotas, queue depth, deadlines, and memory per waiting request.\n:::",
          "difficulty": "core"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q6",
          "q": "Java 25 (GA 9/2025) — what matters most for production and high-load systems?",
          "a": "JDK 25 shipped 18 JEPs and is treated as an LTS release by most major vendors; the actual support duration, patch channel, and cost are [[o:vendor-specific]]. Important production-facing changes include:\n\n- [[g:Scoped Values]] became final for immutable, bounded context propagation.\n- [[g:Compact Object Headers]] became a product feature, reducing 64-bit object headers to 8 bytes when explicitly enabled; it is not the default layout.\n- [[g:Generational Shenandoah]] became a product feature, but Shenandoah still defaults to its single-generation mode.\n- Ahead-of-Time Command-Line Ergonomics and Method Profiling extend the AOT cache workflow to improve startup and warmup when the training run represents production.\n- JFR gained cooperative sampling and method timing/tracing; CPU-time profiling is experimental.\n- The KDF API, module imports, compact source files, and flexible constructor bodies graduated, while Structured Concurrency and several other features remained preview/incubator/experimental.\n\n:::tip Production positioning\nJava 25 improves memory density, observability, and warmup options, but none is a free universal win. Enable product features in a canary, compare application SLOs and resource use, and keep rollback-compatible flags. The Foreign Function &amp; Memory API was already final in Java 22.\n:::\n\n:::deep\nCompact headers reduce the header itself from 12 or 16 bytes to 8 on supported 64-bit configurations; application heap savings depend on object shape and alignment.\n\nGenerational Shenandoah can improve workloads with many short-lived objects, but it must be selected explicitly and benchmarked against G1/ZGC under the same traffic.\n\nAOT method profiles come from a training run. If production class paths, feature flags, or traffic differ, HotSpot still profiles and optimizes online; treat the cache as an optimization artifact tied to the build and workload, not as a replacement for JIT.\n\nStructured Concurrency's JDK 25 API uses `StructuredTaskScope.open(...)` and still requires `--enable-preview`. Scoped Values are final. Mixing those lifecycle statuses is a common interview and migration mistake.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q7",
          "q": "Why do enterprises stick to LTS releases instead of the latest version every 6 months?",
          "a": "“LTS” is a support commitment made by a JDK vendor, not a Java-language guarantee with one universal end date. Most vendors designate 8, 11, 17, 21, and 25 for long-term support, but free/commercial windows, platforms, update cadence, and backport policy differ.\n\nEnterprises often standardize on a vendor-supported LTS because it simplifies framework certification, vulnerability patching, compliance evidence, and fleet-wide testing. That does [[r:not]] mean freezing the runtime: security and CPU/PSU patch releases still need a tested rollout path.\n\n:::warn The migration risk is accumulated distance\nJumping from 8 directly to 21 or 25 combines language/runtime removals, strong encapsulation, library changes, TLS defaults, and build-tool upgrades into one programme. Regularly testing on the next JDK—even before adopting it—keeps the dependency graph and operational playbook ready.\n:::\n\nThere is no universal “never fall more than two LTS releases behind” rule; derive the deadline from vendor support, security exposure, framework support, and migration lead time.\n:::deep\nChoose an update policy explicitly:\n\n- pin the vendor/distribution, architecture, support channel, and end date rather than writing only “Java 25 LTS”;\n- apply quarterly security/critical updates through canary and rollback automation;\n- run CI on the current production JDK and the next target JDK to expose warnings and removed APIs early;\n- inventory preview/incubator flags separately because they offer no source-compatibility promise;\n- rehearse rollback for runtime, container base image, agents, and native libraries together.\n\nA non-LTS feature release can be reasonable when the team can upgrade every six months and needs a delivered feature. An LTS is a maintenance strategy, not proof that an application is secure or stable.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "02-java-8-25-java-vs-go.release-timeline.q8",
          "q": "How do you read Java release notes without accidentally shipping a preview or vendor-specific feature?",
          "a": "Classify every feature before using it in a production recommendation. A final Java SE API has a specification and compatibility contract; a preview feature requires the matching compiler/runtime preview flags and may change or disappear; an incubator module is an experimental API with a different adoption and migration risk; a HotSpot diagnostic flag or vendor default is not a Java language guarantee.\n\nFor a pinned service, record the JDK distribution, exact patch version, compiler and runtime flags, base image, architecture, and whether preview modules are allowed. Compile and run with the same policy in CI, verify the feature with a small compatibility test, and make the release gate fail when the runtime is older or preview flags are missing. Use the official JEP/API/release note rather than a blog title as the status source.\n\nFor example, StructuredTaskScope is documented as a preview API in Java 25, while JEP 491 is a delivered Java 24 HotSpot change. Those facts have different rollout rules. A release table should therefore say `final`, `preview`, `incubator`, or `implementation-specific` next to each feature, then link the migration and rollback test. “Java 25 supports it” is incomplete unless the exact feature status and target distribution are named.",
          "difficulty": "advanced"
        }
      ]
    },
    {
      "title": "Java vs Go (2025–2026)",
      "items": [
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q1",
          "q": "Virtual Threads vs Goroutines — how do the models differ?",
          "a": "Both are [[g:M:N schedulers]] that map many lightweight threads onto few OS threads, yielding automatically on blocking I/O.\n\n<table class=\"cmp\"><thead><tr><th></th><th>Goroutine</th><th>Virtual Thread</th></tr></thead><tbody>\n<tr><td>Introduced</td><td>2009, mature scheduler</td><td>Java 21 (Loom)</td></tr>\n<tr><td>Stack</td><td>~2KB growable</td><td>continuation on the heap</td></tr>\n<tr><td>Coordination</td><td>channels (message passing)</td><td>shared memory + StructuredTaskScope</td></tr>\n<tr><td>Ecosystem</td><td>written specifically for Go</td><td>reuses JDBC/Hibernate/Kafka</td></tr>\n</tbody></table>\n\nGo's channels ≈ data passing; StructuredTaskScope ≈ task lifecycle management — they complement each other rather than being direct substitutes.\n\n:::deep\n- goroutines have their own scheduler refined over ~15 years (work-stealing, GOMAXPROCS, preemption since Go 1.14)\n- VTs mount/unmount on a carrier (ForkJoinPool); a continuation saves/restores the stack on block\n- coordination: Go favors [[b:channels]] (CSP, 'share memory by communicating'); Java favors shared memory + locks/concurrent collections + StructuredTaskScope\n\n[[g:Where Java wins]]: reusing the entire blocking ecosystem without rewriting it.\n[[r:Watch out for]]: pinning (synchronized/native) and heavy ThreadLocal use when there are millions of VTs (→ ScopedValue).\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q2",
          "q": "How should Java and Go performance be compared for a real backend workload?",
          "a": "There is no defensible constant such as “Go uses 4–6× less memory” or “Java is 13% slower.” Runtime version, framework, GC, allocation rate, serialization, database latency, concurrency, warmup, container limit, and tuning can reverse a benchmark result.\n\n<table class=\"cmp\"><thead><tr><th>Measure</th><th>Control before comparing</th><th>Report</th></tr></thead><tbody>\n<tr><td>startup/readiness</td><td>same dependencies, image cache, probes, and cold/warm mode</td><td>distribution, not one run</td></tr>\n<tr><td>steady throughput</td><td>same response, dataset, connection limits, and target latency</td><td>RPS with p50/p95/p99 and errors</td></tr>\n<tr><td>memory/density</td><td>same container limit and load; include native/off-heap memory</td><td>RSS, heap/live set, OOM margin</td></tr>\n<tr><td>cost</td><td>same region, autoscaling policy, replicas, and SLO</td><td>cost per successful business operation</td></tr>\n</tbody></table>\n\nGo often starts quickly and can have a small idle footprint; a warmed-up Java service can deliver strong throughput and latency, and virtual threads simplify high blocking-I/O concurrency. Those are hypotheses to measure, not language guarantees.\n\n:::warn Benchmark validity\nDrive the real dependency shape or realistic fakes, warm JIT-based services to the state production reaches, avoid coordinated omission in the load generator, and repeat until confidence intervals are useful.\n:::\n\nA GC that reduces pause spikes does not “eliminate” tail latency; queueing, CPU throttling, safepoints, network, and dependencies remain.\n:::deep\nMake the decision from a bottleneck and total-cost model:\n\n- If compute cost is material, prototype the hottest representative service in both stacks and price the measured capacity plus operational headroom.\n- If database or partner latency dominates, language microbenchmarks may have no business impact; schema, caching, batching, and concurrency limits matter more.\n- Include team delivery speed, incident tooling, libraries, patching, and polyglot operational cost. A small runtime saving can be overwhelmed by a second build/observability/security ecosystem.\n\nPublish the harness, commit, configs, dataset, hardware, runtime flags, raw results, and date. Without that provenance, a number is an anecdote—not a reusable architectural fact.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q3",
          "q": "When do you pick Java, when do you pick Go? What's the polyglot model?",
          "a": "<table class=\"cmp\"><thead><tr><th>Choose Java when</th><th>Choose Go when</th></tr></thead><tbody>\n<tr><td>complex domain (many entities/rules)</td><td>edge proxy, sidecar, CLI, infra tooling</td></tr>\n<tr><td>need an ORM/mature ecosystem (Hibernate, Spring Data)</td><td>simple high-throughput API</td></tr>\n<tr><td>strong Java team</td><td>cost/container density matters</td></tr>\n<tr><td>AI/LLM/vector integration</td><td>need a single binary + fast startup</td></tr>\n</tbody></table>\n\n[[g:Polyglot]] is the dominant pattern in 2025–2026: Java for core business logic & stateful domains, Go for the edge/high-throughput layer; communicating over HTTP/gRPC (Google, Netflix, Cloudflare).\n\nThe question isn't 'which language wins' but [[g:'which tool is right for each layer']].\n\n:::tip Closing point\nAnswering 'it depends on the layer' backed with cost/density numbers shows architectural and operational thinking, not language fandom.\n:::\n\n:::deep\n- [[b:Java]]: complex domain, Java-first team (velocity &gt;&gt; the 13% gap), AI/vector integration\n- [[b:Go]]: simple HTTP+DB+cache at scale, single binary (sidecar/CLI/proxy), cost/service &gt;$50k/year\n- [[b:Native Java]]: scale-to-zero/fast startup (serverless)\n\n[[r:The cost of polyglot]]: operating multiple stacks (build, observability, hiring) — don't go multi-language just because you like it.\n:::",
          "difficulty": "core"
        },
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q4",
          "q": "GraalVM Native Image — which Java weakness does it address relative to Go?",
          "a": "[[g:Native Image]] AOT-compiles to a native binary:\n- millisecond-scale startup, small footprint, high density\n- narrows the gap with Go for serverless/scale-to-zero\n\nIn exchange:\n- peak throughput is usually [[r:lower than a warmed-up JIT]]\n- more complex build process\n- reflection/dynamic features need configuration\n\n'Native Java' sits in the middle: better density than the JVM, lower peak than JIT.\n\n:::deep\n**Mechanism & cost:** AOT compilation under a *closed-world assumption* (every class must be known at build time) → reflection/dynamic proxies/resources must be declared (reachability metadata); Spring Boot 3 + GraalVM support this via AOT processing.\n\n- [[g:Gains]]: millisecond-scale startup, small RSS, high density → great fit for serverless/scale-to-zero\n- [[r:Losses]]: peak throughput lower than a warm JIT (offset somewhat by PGO at build time), longer & more complex builds, some libraries not yet compatible\n\nDeeper: go Native when *autoscaling/startup is the SLO*; go JVM+ZGC when *p99.9 throughput* is the SLO.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q5",
          "q": "What's the future of Java: Project Loom, Valhalla, Panama?",
          "a": "- [[g:Loom]]: virtual threads + structured concurrency — lightweight concurrency\n- [[g:Valhalla]]: value/primitive classes — objects laid out 'flat' like primitives in memory, improving cache performance (data-intensive/AI)\n- [[g:Panama (FFM API)]]: clean native calls replacing JNI; the foundation for C/AI integration\n\nDirection: Java keeps its strong ecosystem while patching its historical weaknesses (concurrency, memory layout, native interop).\n\n:::deep\n- [[g:Loom]] (mostly final now): changes how I/O code is written\n- [[g:Valhalla]]: 'flat' value/primitive classes (no header, no indirection) → better cache locality, less RAM (data-intensive/AI, large numeric collections); Java 25 lays the groundwork (Compact Headers)\n- [[g:Panama (FFM)]]: clean, safe native calls (final since 22), the foundation for C/AI/SIMD (Vector API)\n\nDeeper: these three projects are Java 'fixing itself' on three weaknesses relative to Go/Rust (concurrency model, memory layout, native interop) while still keeping backward compatibility.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q6",
          "q": "Records, sealed classes, and pattern matching combine to create what programming style?",
          "a": "This trio brings the [[g:algebraic data types]] (functional) style to Java:\n- [[g:sealed]]: enumerate every variant\n- [[g:record]]: immutable data\n- [[g:pattern matching switch]]: exhaustive handling (compiler catches missing branches)\n\nA great fit for clearly modeling a domain/state machine with little boilerplate.\n\n:::deep\n**Practical application:** `sealed interface BookingState permits Held, Paid, Issued, Cancelled` + a record per state + exhaustive `switch` pattern matching:\n- the compiler forces you to handle every state\n- adding a new state and forgetting a branch → [[r:compile error]] (safe for a money/ticket state machine)\n\n- unlike an enum: each state carries its own data (record fields)\n- the [[g:'make illegal states unrepresentable']] style\n\nDeeper: combine with deconstruction (`case Paid(var amount, var ts)`) for code that's both short and type-safe — close to Rust/Scala while staying pure Java.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "02-java-8-25-java-vs-go.java-vs-go-2025-2026.q7",
          "q": "What must a fair Java-versus-Go backend benchmark hold constant?",
          "a": "Hold the question constant before comparing the languages. Use the same protocol, payloads, data distribution, client, TLS settings, serialization, retry/deadline policy, database and cache, connection-pool limits, container CPU/memory quotas, and deployment topology. Measure both cold start and warmed steady state; a JIT warm-up, a build-time native image, and a pre-warmed Go process answer different operational questions.\n\nReport throughput and p50/p95/p99 latency together with CPU, RSS/cgroup memory, allocation/GC or runtime pauses, startup/readiness, error/timeout rate, connection-pool wait, queue age, and downstream saturation. Run multiple trials with a declared ramp, duration, sample size, hardware, compiler/runtime version, and statistical treatment. A lower median with a worse tail or a faster server that overloads the database is not a win.\n\nVirtual-thread or goroutine count is not capacity. Bound CPU work, database connections, sockets, memory, external quota, and in-flight requests in both implementations. If Java Native Image is compared with a Go binary, label it as a separate deployment target and test reflection, resource loading, TLS, shutdown, observability, and patching. The result should select a workload and operating model, not declare a universal language winner.",
          "difficulty": "advanced"
        }
      ]
    }
  ]
}
