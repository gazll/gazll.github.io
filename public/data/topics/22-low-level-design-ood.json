{
  "sections": [
    {
      "title": "The LLD framework & classic problems",
      "items": [
        {
          "id": "22-low-level-design-ood.the-lld-framework-classic-problems.q1",
          "q": "How does a low-level design interview differ from system design, and what five-step framework should you use?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>System design (HLD)</th><th>Low-level design (LLD)</th></tr></thead><tbody>\n<tr><td>Unit</td><td>service, database, queue, cache</td><td>class, interface, method</td></tr>\n<tr><td>Focus</td><td>scale, consistency, partitioning</td><td>responsibilities, coupling, extensibility</td></tr>\n<tr><td>Deliverable</td><td>diagram + trade-offs</td><td><b>working code</b></td></tr>\n<tr><td>Follow-up</td><td>\"What happens at 10× traffic?\"</td><td>\"How many places change when type X is added?\"</td></tr>\n</tbody></table>\n\n**Five-step framework:**\n\n1. **Clarify the scope** (3–5 minutes) — LLD prompts are deliberately ambiguous. *\"Elevator: how many cars? Are some floors prioritized? Do we need only scheduling or a full simulation?\"* Agree on a scope that fits 45 minutes.\n2. **List use cases** — identify what each actor can do. These become the methods later.\n3. **Identify entities and responsibilities** — nouns become classes and verbs become methods. State explicitly: *\"This class has the single responsibility of…\"*\n4. **Sketch the API before implementing it** — write method signatures and interfaces, [[g:confirm them]], then fill in method bodies. Avoid writing 40 lines before discovering that the prompt was misunderstood.\n5. **Code the core** — implement the most interesting part as working code; describe repetitive parts as *\"analogous\"* and move on.\n\n:::tip What the interviewer actually evaluates\nThe [[o:extensibility of the design]]. They will almost certainly ask *\"What if we add another vehicle, fee, or strategy type?\"* If the answer is \"add one class implementing the existing interface\", the design passes. If it is \"edit the `switch` in three places\", it does not.\n:::\n\n:::deep\n- [[r:do not draw an ERD]]. LLD asks for the in-memory *object* model, not a schema. Jumping to tables and foreign keys immediately leaves the problem's scope.\n- begin with interfaces and enums, and defer storage; an in-memory `Map` is more than enough unless the prompt says otherwise\n- state the system [[g:invariants]], such as *\"a seat cannot be both FREE and HELD\"*, then show where the code enforces them. This distinguishes a senior from a mid-level answer.\n- prefer [[o:composition over inheritance]]: a deep inheritance tree signals immature design. A four-level `Car extends Vehicle` hierarchy for a \"vehicle types\" problem almost always has a better composition-based alternative.\n- manage time: 5 minutes clarifying, 10 designing, 25 coding, and 5 discussing extensions. [[r:Running out of time without working code]] is the worst outcome, so implement the core early.\n- always ask *\"Does this need to be thread-safe?\"* If yes, explain the strategy (22-low-level-design-ood.patterns-in-interview-code.q3); if no, mention that concurrency was considered.\n:::",
          "difficulty": "core"
        },
        {
          "id": "22-low-level-design-ood.the-lld-framework-classic-problems.q2",
          "q": "How do you design an LRU cache with O(1) get and put, and why do you choose those data structures?",
          "a": "This is the most frequently asked LLD problem (LeetCode 146). Both `get` and `put` must be [[g:O(1)]].\n\n**Why two structures are required:** a `HashMap` provides O(1) lookup but no order; a list provides order but O(n) lookup. Combine them so the map points [[o:directly to a list node]], making lookup, removal, and insertion all O(1).\n\nIt must be a [[g:doubly]] linked list because removing a node requires its predecessor. Two sentinel nodes (`head`, `tail`) eliminate every special null case.\n\n<pre><code><span class=\"k\">class</span> LRUCache {\n    <span class=\"k\">private final</span> <span class=\"k\">int</span> cap;\n    <span class=\"k\">private final</span> Map&lt;Integer, Node&gt; map = <span class=\"k\">new</span> HashMap&lt;&gt;();\n    <span class=\"k\">private final</span> Node head = <span class=\"k\">new</span> Node(<span class=\"n\">0</span>, <span class=\"n\">0</span>), tail = <span class=\"k\">new</span> Node(<span class=\"n\">0</span>, <span class=\"n\">0</span>);\n<span class=\"c\">    </span>\n    <span class=\"f\">LRUCache</span>(<span class=\"k\">int</span> cap) {\n        <span class=\"k\">this</span>.cap = cap;\n        head.next = tail; tail.prev = head;      <span class=\"c\">// two sentinel nodes</span>\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> <span class=\"k\">int</span> <span class=\"f\">get</span>(<span class=\"k\">int</span> key) {\n        Node n = map.<span class=\"f\">get</span>(key);\n        <span class=\"k\">if</span> (n == <span class=\"k\">null</span>) <span class=\"k\">return</span> -<span class=\"n\">1</span>;\n        <span class=\"f\">moveToFront</span>(n);                          <span class=\"c\">// just used =&gt; most recent</span>\n        <span class=\"k\">return</span> n.val;\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">put</span>(<span class=\"k\">int</span> key, <span class=\"k\">int</span> val) {\n        Node n = map.<span class=\"f\">get</span>(key);\n        <span class=\"k\">if</span> (n != <span class=\"k\">null</span>) { n.val = val; <span class=\"f\">moveToFront</span>(n); <span class=\"k\">return</span>; }\n        <span class=\"k\">if</span> (map.<span class=\"f\">size</span>() == cap) {                  <span class=\"c\">// full =&gt; evict the last node</span>\n            Node lru = tail.prev;\n            <span class=\"f\">remove</span>(lru); map.<span class=\"f\">remove</span>(lru.key);\n        }\n        Node fresh = <span class=\"k\">new</span> Node(key, val);\n        map.<span class=\"f\">put</span>(key, fresh); <span class=\"f\">addFirst</span>(fresh);\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">private</span> <span class=\"k\">void</span> <span class=\"f\">moveToFront</span>(Node n) { <span class=\"f\">remove</span>(n); <span class=\"f\">addFirst</span>(n); }\n    <span class=\"k\">private</span> <span class=\"k\">void</span> <span class=\"f\">remove</span>(Node n) { n.prev.next = n.next; n.next.prev = n.prev; }\n    <span class=\"k\">private</span> <span class=\"k\">void</span> <span class=\"f\">addFirst</span>(Node n) {\n        n.next = head.next; n.prev = head;\n        head.next.prev = n; head.next = n;\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">private static class</span> Node {\n        <span class=\"k\">int</span> key, val; Node prev, next;\n        <span class=\"f\">Node</span>(<span class=\"k\">int</span> k, <span class=\"k\">int</span> v) { key = k; val = v; }\n    }\n}</code></pre>\n\nEach node must also store its `key`: during eviction you have the node but need the `key` to remove its map entry.\n\n:::deep\n- `LinkedHashMap` implements this in five lines with `accessOrder=true` and an overridden `removeEldestEntry`. [[o:Mentioning it is useful]] because it demonstrates library knowledge, but the interviewer almost always wants the manual implementation.\n- a common extension is **thread safety**. One global lock is simple but blocks all operations; real implementations use [[g:sharding]] such as Caffeine or Guava, with multiple segments and one lock per segment.\n- LRU is [[r:not always the best policy]]: one full-table scan can evict all hot data. [[o:LFU]] or [[o:W-TinyLFU]] can resist scans at the cost of frequency-tracking state; see the admission-versus-eviction design in (25-microservice.05-caching-pitfalls.q10).\n- adding TTL requires a third structure, such as an expiry min-heap or timing wheel; see 22.8\n- in practice, Redis uses [[o:approximate]] LRU by sampling several random keys and evicting the oldest, avoiding a linked list for millions of keys. Mentioning this earns points.\n- useful test cases include capacity = 1, overwriting an existing key, getting a missing key, and verifying eviction order after interleaved get/put operations\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "22-low-level-design-ood.the-lld-framework-classic-problems.q3",
          "q": "How do you design and code token-bucket and sliding-window rate limiters?",
          "a": "Topic 10 discusses the algorithms at system level; here you must [[o:write the code]].\n\n**Token bucket** — it permits bounded bursts, which is why it is the most widely used approach:\n\n<pre><code><span class=\"k\">class</span> TokenBucket {\n    <span class=\"k\">private final</span> <span class=\"k\">long</span> capacity;\n    <span class=\"k\">private final</span> <span class=\"k\">double</span> refillPerMs;\n    <span class=\"k\">private</span> <span class=\"k\">double</span> tokens;\n    <span class=\"k\">private</span> <span class=\"k\">long</span> lastRefill;\n<span class=\"c\">    </span>\n    <span class=\"f\">TokenBucket</span>(<span class=\"k\">long</span> capacity, <span class=\"k\">double</span> refillPerSec) {\n        <span class=\"k\">this</span>.capacity = capacity;\n        <span class=\"k\">this</span>.refillPerMs = refillPerSec / <span class=\"n\">1000.0</span>;\n        <span class=\"k\">this</span>.tokens = capacity;                   <span class=\"c\">// full at initialization</span>\n        <span class=\"k\">this</span>.lastRefill = System.<span class=\"f\">currentTimeMillis</span>();\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">synchronized</span> <span class=\"k\">boolean</span> <span class=\"f\">tryAcquire</span>(<span class=\"k\">int</span> n) {\n        <span class=\"f\">refill</span>();\n        <span class=\"k\">if</span> (tokens &lt; n) <span class=\"k\">return</span> <span class=\"k\">false</span>;\n        tokens -= n;\n        <span class=\"k\">return</span> <span class=\"k\">true</span>;\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">private</span> <span class=\"k\">void</span> <span class=\"f\">refill</span>() {\n        <span class=\"k\">long</span> now = System.<span class=\"f\">currentTimeMillis</span>();\n        tokens = Math.<span class=\"f\">min</span>(capacity, tokens + (now - lastRefill) * refillPerMs);\n        lastRefill = now;                          <span class=\"c\">// clamp at capacity: idle time cannot accumulate without bound</span>\n    }\n}</code></pre>\n\nUse [[o:lazy refill]], calculated when called, rather than a periodic refill thread. It requires no dedicated refill thread and remains accurate to the millisecond.\n\n**Sliding window log** — exact, at the cost of O(requests in the window) memory:\n\n<pre><code><span class=\"k\">boolean</span> <span class=\"f\">allow</span>(String key, <span class=\"k\">int</span> limit, <span class=\"k\">long</span> windowMs) {\n    <span class=\"k\">long</span> now = System.<span class=\"f\">currentTimeMillis</span>();\n    Deque&lt;Long&gt; q = log.<span class=\"f\">computeIfAbsent</span>(key, k -&gt; <span class=\"k\">new</span> ArrayDeque&lt;&gt;());\n    <span class=\"k\">synchronized</span> (q) {\n        <span class=\"k\">while</span> (!q.<span class=\"f\">isEmpty</span>() &amp;&amp; q.<span class=\"f\">peekFirst</span>() &lt;= now - windowMs) q.<span class=\"f\">pollFirst</span>();\n        <span class=\"k\">if</span> (q.<span class=\"f\">size</span>() &gt;= limit) <span class=\"k\">return</span> <span class=\"k\">false</span>;\n        q.<span class=\"f\">addLast</span>(now);\n        <span class=\"k\">return</span> <span class=\"k\">true</span>;\n    }\n}</code></pre>\n\n<table class=\"cmp\"><thead><tr><th>Algorithm</th><th>Memory</th><th>Allows bursts</th><th>Accuracy</th></tr></thead><tbody>\n<tr><td>Token bucket</td><td>O(1)</td><td>yes, up to capacity</td><td>high</td></tr>\n<tr><td>Fixed window</td><td>O(1)</td><td>yes, up to 2× at the boundary</td><td><b>low</b> — boundary error</td></tr>\n<tr><td>Sliding window log</td><td>O(n)</td><td>no</td><td>exact</td></tr>\n<tr><td>Sliding window counter</td><td>O(1)</td><td>limited</td><td>good approximation</td></tr>\n</tbody></table>\n\n:::deep\n- [[r:fixed-window boundary error]]: with a limit of 100 per minute, 100 requests at 10:00:59 and another 100 at 10:01:00 produce 200 requests in one second while remaining \"within the rules\". Interviewers frequently probe this case.\n- in a distributed system, state must live in [[g:Redis]], and the read-modify-write operation must be atomic through a Lua script or `INCR` plus `EXPIRE`. Per-instance in-memory limits become N times the intended limit across N instances.\n- a Redis token bucket stores `tokens` and `lastRefill` in a hash and wraps refill plus deduction in one Lua script\n- return the correct [[o:429]] status with `Retry-After` and `X-RateLimit-Limit/Remaining/Reset` headers (17-rest-api-design.contracts-errors-lifecycle.q2)\n- choose the key by layer: API key for partners, user for authenticated consumers, and IP for anonymous users. [[r:IP-only limiting is wrong]] when customers behind NAT or CGNAT share an address.\n- distinguish rate limiting, which prevents abuse, from [[o:load shedding]], which protects against overload using current load, and from concurrency limits. Real systems commonly need all three.\n- the hard testing problem is freezing time. Inject `Clock` or `Supplier&lt;Long&gt;` instead of calling `System.currentTimeMillis()` directly; this makes the code testable and demonstrates good design.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "22-low-level-design-ood.the-lld-framework-classic-problems.q4",
          "q": "How should classes be divided when designing a parking lot or elevator system?",
          "a": "These are two classic problems. Evaluation is not about one \"correct answer\" but about [[o:responsibility boundaries]] and the ability to add new types.\n\n**Parking lot — a compact model:**\n\n<pre><code><span class=\"k\">enum</span> SpotType { MOTORBIKE, COMPACT, LARGE }\n<span class=\"c\">//</span>\n<span class=\"k\">interface</span> Vehicle { SpotType <span class=\"f\">requiredSpot</span>(); String <span class=\"f\">plate</span>(); }\n<span class=\"c\">//</span>\n<span class=\"k\">interface</span> PricingStrategy { Money <span class=\"f\">price</span>(Ticket t, Instant out); }\n<span class=\"k\">interface</span> SpotAllocator { Optional&lt;Spot&gt; <span class=\"f\">allocate</span>(Vehicle v); }\n<span class=\"c\">//</span>\n<span class=\"k\">class</span> ParkingLot {\n    <span class=\"k\">private final</span> SpotAllocator allocator;      <span class=\"c\">// change the allocation policy</span>\n    <span class=\"k\">private final</span> PricingStrategy pricing;      <span class=\"c\">// change the pricing policy</span>\n    <span class=\"k\">public</span> Ticket <span class=\"f\">park</span>(Vehicle v) { ... }\n    <span class=\"k\">public</span> Money <span class=\"f\">unpark</span>(String ticketId) { ... }\n}</code></pre>\n\nThose two `interface` declarations answer the extension question *\"What if pricing changes during peak hours?\"*: add another `PricingStrategy` and [[g:do not modify `ParkingLot`]].\n\n**Elevator — the key distinction is separating *commands* from *scheduling*:**\n\n<pre><code><span class=\"k\">enum</span> Direction { UP, DOWN, IDLE }\n<span class=\"c\">//</span>\n<span class=\"k\">class</span> Request { <span class=\"k\">int</span> floor; Direction dir; }        <span class=\"c\">// hall call</span>\n<span class=\"k\">class</span> Elevator {                                <span class=\"c\">// only knows how to move ITSELF</span>\n    <span class=\"k\">int</span> current; Direction dir;\n    TreeSet&lt;Integer&gt; stops;\n    <span class=\"k\">void</span> <span class=\"f\">step</span>() { ... }                          <span class=\"c\">// move one floor</span>\n}\n<span class=\"k\">interface</span> Scheduler {                           <span class=\"c\">// choose which car serves the request</span>\n    Elevator <span class=\"f\">assign</span>(Request r, List&lt;Elevator&gt; cabins);\n}</code></pre>\n\nAn `Elevator` must not know about other cars. If it does, it violates single responsibility and cannot be tested independently.\n\n:::deep\n- the [[r:number-one mistake]] is putting everything in a 300-line `ParkingLot` class with `if (type == ...)` scattered throughout. An extension question exposes it immediately.\n- [[o:state the invariants aloud]]: one vehicle occupies one spot, one spot holds one vehicle, and a paid ticket cannot be paid again. Then show where the code enforces them.\n- allocate efficiently with `Map&lt;SpotType, PriorityQueue&lt;Spot&gt;&gt;` in O(log n) instead of scanning every spot. Complexity analysis earns points even in an LLD round.\n- for elevators, use the [[g:SCAN/elevator]] algorithm, traveling fully in one direction before reversing. Disk I/O scheduling uses the same idea, and connecting it to operating systems is valuable.\n- a `TreeSet` for `stops` turns \"the next stop in the current direction\" into O(log n) `ceiling()`/`floor()` calls, cleaner than manually sorting\n- concurrency matters when many people press buttons simultaneously (22-low-level-design-ood.patterns-in-interview-code.q3). At minimum say, *\"I will use a request queue and one dispatcher thread.\"*\n- the same template solves vending machines (State pattern), cinema reservations (seat locking), chat, and libraries. Mastering the template matters more than memorizing each problem.\n:::",
          "difficulty": "advanced"
        }
      ]
    },
    {
      "title": "Patterns in interview code",
      "items": [
        {
          "id": "22-low-level-design-ood.patterns-in-interview-code.q1",
          "q": "How do you reference SOLID naturally in an LLD interview without sounding formulaic?",
          "a": "Reciting the five letters from memory is [[r:counterproductive]]. Use them correctly by reaching a design decision, [[g:naming the relevant principle, and immediately moving on]].\n\n- [[b:S]] — Single Responsibility. *\"I separated `PricingStrategy` because its reason to change differs from `ParkingLot`.\"* The rule is \"one reason to change\", not \"one method\".\n- [[b:O]] — Open/Closed. *\"Adding a new vehicle type means adding an implementation, not editing existing classes.\"* This is the [[o:most frequently tested]] principle in extension questions.\n- [[b:L]] — Liskov. A subtype must replace its base type without violating expectations. The classic `Square extends Rectangle` fails because `setWidth` also changes the height.\n- [[b:I]] — Interface Segregation. Interfaces should be small and consumer-specific. A 20-method `Repository` forces every implementation to provide all 20 methods.\n- [[b:D]] — Dependency Inversion. `ParkingLot` depends on the `PricingStrategy` abstraction, not the concrete `WeekendPricing`.\n\n:::tip A strong interview line\n*\"I apply Open/Closed [[o:where I expect change]]—the pricing calculation. I keep unlikely-to-change areas simple because premature abstraction is also debt.\"* Explaining a principle's **limits** is what sounds senior.\n:::\n\n:::deep\n- the [[r:over-engineering trap]] is creating an interface for every class \"to follow SOLID\". An interface with one implementation that will never gain a second is [[o:noise]], not good design. YAGNI wins until a real axis of change appears.\n- Dependency Inversion is not Dependency Injection. DI is a common *mechanism* for achieving DIP; confusing the concepts is a frequent interview mistake.\n- SRP correctly means *\"group things that change for the same reason and separate things that change for different reasons\"*. It is based on **who or what requests the change**, not the number of tasks a class performs.\n- a common real-world Liskov violation is a subclass throwing `UnsupportedOperationException`, as with `Collections.unmodifiableList`. Recognizing that even standard libraries violate the rule shows understanding rather than memorisation.\n- SOLID arose around inheritance-heavy OOP; modern design leans toward [[o:composition, pure functions, and immutable data]]. Mentioning that perspective demonstrates maturity.\n- combine other principles naturally: DRY, while avoiding merging coincidentally similar code, KISS, YAGNI, and the Law of Demeter\n:::",
          "difficulty": "core"
        },
        {
          "id": "22-low-level-design-ood.patterns-in-interview-code.q2",
          "q": "When should you choose Strategy, State, or Chain of Responsibility?",
          "a": "The three patterns look similar because all of them plug in behavior, but they answer three different questions.\n\n<table class=\"cmp\"><thead><tr><th>Pattern</th><th>Answers</th><th>Who chooses the next behavior</th></tr></thead><tbody>\n<tr><td>Strategy</td><td><b>how should it be done?</b> — interchangeable algorithms</td><td>the client chooses; it does not change mid-operation</td></tr>\n<tr><td>State</td><td><b>where are we now?</b> — behavior changes with state</td><td>the current state transitions to the next state</td></tr>\n<tr><td>Chain</td><td><b>who handles it?</b> — try handlers in sequence until one accepts</td><td>each handler decides whether to handle or forward</td></tr>\n</tbody></table>\n\n**Strategy** — pricing, compression choice, or scheduling algorithm:\n\n<pre><code>lot = <span class=\"k\">new</span> ParkingLot(<span class=\"k\">new</span> WeekendPricing());   <span class=\"c\">// injected at construction</span></code></pre>\n\n**State** — booking lifecycle, vending machine, or order:\n\n<pre><code><span class=\"k\">interface</span> BookingState {\n    BookingState <span class=\"f\">pay</span>(Booking b);       <span class=\"c\">// return the next state</span>\n    BookingState <span class=\"f\">cancel</span>(Booking b);\n}\n<span class=\"c\">//</span>\n<span class=\"k\">class</span> Held <span class=\"k\">implements</span> BookingState {\n    <span class=\"k\">public</span> BookingState <span class=\"f\">pay</span>(Booking b) { b.<span class=\"f\">charge</span>(); <span class=\"k\">return</span> <span class=\"k\">new</span> Paid(); }\n    <span class=\"k\">public</span> BookingState <span class=\"f\">cancel</span>(Booking b) { b.<span class=\"f\">releaseSeat</span>(); <span class=\"k\">return</span> <span class=\"k\">new</span> Cancelled(); }\n}</code></pre>\n\nThe benefit is that [[g:invalid transitions become impossible]]: `Cancelled.pay()` can simply throw an error, with no scattered `if` statements.\n\n**Chain of Responsibility** — middleware, filters, or validation pipelines:\n\n<pre><code>auth.<span class=\"f\">setNext</span>(rateLimit).<span class=\"f\">setNext</span>(validate).<span class=\"f\">setNext</span>(handler);</code></pre>\n\n:::deep\n- the signal for State is [[r:the same `if (status == ...)` chain repeated across several methods]]. That is a state machine hidden inside `if` statements.\n- State [[o:holds context]] and transitions itself; Strategy is [[o:stateless]] and selected externally. This is the most common distinction interviewers ask for.\n- in modern Java, a Strategy is often just a [[g:lambda]] (`Function`/`Predicate`) rather than a class hierarchy. Saying so shows understanding rather than pattern worship.\n- an `enum` can implement State compactly in Java by overriding methods per constant, especially when states hold no private data\n- a long Chain is [[r:difficult to debug]]; add logs or traces at every link and define whether an unhandled input is an error or is silently ignored\n- patterns commonly paired in LLD include [[o:Factory]] (construct the correct type from input), [[o:Observer]] (notify changes), [[o:Builder]] (objects with many optional arguments), and [[o:Command]] (undo/redo or queued tasks)\n- [[r:do not force patterns]] into a design to show them off. A misplaced pattern is worse than none; the interviewer will ask *\"Why is it needed here?\"*, and the answer must be real.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "22-low-level-design-ood.patterns-in-interview-code.q3",
          "q": "What thread-safety questions arise in an LLD interview, and how should you answer them?",
          "a": "Almost every LLD problem ends with *\"What if multiple threads call it concurrently?\"* A prepared framework makes the answer quick.\n\n**Four levels, in preferred order:**\n\n1. [[g:Immutability]] — no shared mutable state means no concurrency problem. Mention this first.\n2. [[g:Single-thread confinement]] — funnel work through a queue to one coordinator thread, as in an elevator or actor model.\n3. [[g:Concurrent data structures]] — `ConcurrentHashMap`, `AtomicLong`, and `BlockingQueue`. These are sufficient for most problems.\n4. [[g:Explicit locking]] — `synchronized` or `ReentrantLock`. This is the most powerful and the easiest to get wrong.\n\n**The common interviewer trap** is a compound operation on `ConcurrentHashMap`:\n\n<pre><code><span class=\"c\">// WRONG: each call is atomic, but the PAIR is not</span>\n<span class=\"k\">if</span> (!map.<span class=\"f\">containsKey</span>(k)) map.<span class=\"f\">put</span>(k, v);\n<span class=\"c\">//</span>\n<span class=\"c\">// CORRECT: one atomic operation</span>\nmap.<span class=\"f\">putIfAbsent</span>(k, v);\nmap.<span class=\"f\">computeIfAbsent</span>(k, key -&gt; <span class=\"k\">new</span> Spot(key));</code></pre>\n\n*\"Using `ConcurrentHashMap` makes it safe\"* is [[r:incorrect]]. It guarantees individual operations, not sequences of operations.\n\n**A correct parking-space allocation example:**\n\n<pre><code><span class=\"k\">public</span> Optional&lt;Spot&gt; <span class=\"f\">allocate</span>(Vehicle v) {\n    Queue&lt;Spot&gt; free = freeByType.<span class=\"f\">get</span>(v.<span class=\"f\">requiredSpot</span>());\n    Spot s = free.<span class=\"f\">poll</span>();                  <span class=\"c\">// ConcurrentLinkedQueue: atomic poll</span>\n    <span class=\"k\">return</span> Optional.<span class=\"f\">ofNullable</span>(s);         <span class=\"c\">// one vehicle = one spot, no lock required</span>\n}</code></pre>\n\n:::deep\n- [[o:minimize lock scope]]: lock an individual spot or key rather than the entire lot. Using `synchronized` on every method of a singleton serializes the entire system.\n- consistent lock ordering is the cheapest deadlock prevention: when two locks are needed, always acquire them by increasing ID (see 5.10 and 23.3)\n- `ReentrantLock` versus `synchronized`: the former provides `tryLock(timeout)`, interruptible acquisition, and multiple `Condition`s (23-java-concurrency-coding.classic-synchronisation-problems.q1). Without those requirements, `synchronized` is simpler and well optimized by the JIT.\n- [[r:do not]] make outbound HTTP or database calls while holding a lock; doing so lengthens the critical section and increases deadlock risk\n- for read-heavy, write-light workloads, use `ReadWriteLock`, or copy-on-write when writes are extremely rare (23-java-concurrency-coding.classic-synchronisation-problems.q4)\n- a useful knowledge check: [[o:`volatile`]] guarantees *visibility*, not *atomicity*. `volatile int i; i++` is still broken. This is a classic follow-up question.\n- if the prompt does not require concurrency, say *\"I will design the single-threaded version first for clarity, then identify the synchronisation points.\"* Raising it proactively is better than waiting to be challenged.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "22-low-level-design-ood.patterns-in-interview-code.q4",
          "q": "How do you design an in-memory key-value store with TTL?",
          "a": "This natural extension of LRU is a miniature Redis, making it a favorite follow-up question.\n\n**Requirements:** `put(k, v, ttl)`, `get(k)`, automatic expiry, and no memory leaks.\n\n**Three expiry approaches — production systems use all three:**\n\n<table class=\"cmp\"><thead><tr><th>Approach</th><th>Mechanism</th><th>Trade-off</th></tr></thead><tbody>\n<tr><td>Lazy</td><td>check expiry during <code>get</code></td><td>cheap, but an unread key can <b>remain forever</b></td></tr>\n<tr><td>Active sampling</td><td>a periodic job samples random keys and deletes expired ones</td><td>steady low CPU cost; cleans cold keys</td></tr>\n<tr><td>Expiry min-heap</td><td>a job reads exactly the next key due to expire</td><td>precise, but the heap needs cleanup when keys are overwritten</td></tr>\n</tbody></table>\n\n<pre><code><span class=\"k\">class</span> TtlStore {\n    <span class=\"k\">private final</span> Map&lt;String, Entry&gt; map = <span class=\"k\">new</span> ConcurrentHashMap&lt;&gt;();\n    <span class=\"k\">private record</span> Entry(String val, <span class=\"k\">long</span> expireAt) {\n        <span class=\"k\">boolean</span> <span class=\"f\">alive</span>(<span class=\"k\">long</span> now) { <span class=\"k\">return</span> expireAt == <span class=\"n\">0</span> || now &lt; expireAt; }\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> <span class=\"k\">void</span> <span class=\"f\">put</span>(String k, String v, <span class=\"k\">long</span> ttlMs) {\n        <span class=\"k\">long</span> exp = ttlMs &lt;= <span class=\"n\">0</span> ? <span class=\"n\">0</span> : System.<span class=\"f\">currentTimeMillis</span>() + ttlMs;\n        map.<span class=\"f\">put</span>(k, <span class=\"k\">new</span> Entry(v, exp));\n    }\n<span class=\"c\">    </span>\n    <span class=\"k\">public</span> Optional&lt;String&gt; <span class=\"f\">get</span>(String k) {\n        <span class=\"k\">long</span> now = System.<span class=\"f\">currentTimeMillis</span>();\n        Entry e = map.<span class=\"f\">get</span>(k);\n        <span class=\"k\">if</span> (e == <span class=\"k\">null</span>) <span class=\"k\">return</span> Optional.<span class=\"f\">empty</span>();\n        <span class=\"k\">if</span> (!e.<span class=\"f\">alive</span>(now)) {\n            map.<span class=\"f\">remove</span>(k, e);                 <span class=\"c\">// remove(k,v): do not delete a newer entry</span>\n            <span class=\"k\">return</span> Optional.<span class=\"f\">empty</span>();\n        }\n        <span class=\"k\">return</span> Optional.<span class=\"f\">of</span>(e.<span class=\"f\">val</span>());\n    }\n}</code></pre>\n\nThe two-argument `map.remove(k, e)` is [[o:important]]: another thread may `put` a new value between the read and delete. Removing by key alone would [[r:delete newly written data]].\n\n:::deep\n- Redis uses exactly lazy expiry plus active sampling: every 100 ms it samples 20 keys with TTLs, deletes expired keys, and repeats if more than 25% were expired. [[g:Mentioning this is a major plus.]]\n- a [[o:timing wheel]], such as Netty's `HashedWheelTimer` or Kafka's implementation, outperforms a min-heap for millions of expirations with O(1) insertion/removal instead of O(log n)\n- combining TTL with LRU requires both a capacity limit and expiry. Caffeine already provides `expireAfterWrite` plus `maximumSize`; mention it to demonstrate library awareness.\n- `System.currentTimeMillis()` [[r:can move backwards]] when NTP adjusts the clock, so use `System.nanoTime()` for elapsed durations. This small detail is distinctly senior.\n- persistence requires a WAL or snapshots, such as Redis AOF and RDB. It is usually outside a 45-minute scope but worth identifying.\n- a common extension is `getOrLoad` using cache-aside plus [[o:cache-stampede]] protection through `computeIfAbsent` or single-flight, directly connecting to the Microservices caching chapter\n:::",
          "difficulty": "extra"
        },
        {
          "id": "22-low-level-design-ood.patterns-in-interview-code.q5",
          "difficulty": "advanced",
          "q": "How do you design an object model for testability, invariants, and deterministic concurrency tests?",
          "a": "Testability is an architectural property. Keep domain decisions in small objects or pure functions; put clocks, randomness, ID generation, persistence, messaging, and scheduling behind narrow injected ports.\n\nThis lets tests control inputs without exposing private fields or adding production-only switches.\n\n<pre><code><span class=\"k\">interface</span> Clock { Instant <span class=\"f\">now</span>(); }\n<span class=\"k\">interface</span> IdSource { String <span class=\"f\">next</span>(); }\n<span class=\"k\">final class</span> HoldService {\n    <span class=\"k\">private final</span> Clock clock;\n    <span class=\"k\">private final</span> HoldRepository holds;\n    <span class=\"c\">// constructor injection; no Instant.now() hidden in domain logic</span>\n}</code></pre>\n\nExpress each aggregate invariant and legal transition close to the state it protects: a hold cannot expire before creation, a released hold cannot be confirmed, balance-changing commands must preserve the ledger equation.\n\nReturn explicit outcomes instead of using exceptions for every expected rejection. Make side effects occur after a decision through ports, and define idempotency where commands can be retried.\n\nUse a layered test portfolio:\n\n- [[b:Example/unit tests]] cover boundaries and state transitions with a fake clock and deterministic IDs.\n- [[b:Property/model-based tests]] generate command sequences and assert invariants after every step; a simple reference model can catch optimized-implementation mistakes.\n- [[b:Contract/integration tests]] verify adapters against the real database, broker, serializer, or HTTP contract.\n- [[b:Concurrency tests]] first identify the linearization point, then coordinate targeted interleavings with barriers/latches and bounded timeouts. Add sustained stress or OpenJDK jcstress for Java Memory Model questions; passing one stress run is evidence, not a proof.\n\n:::deep\nDo not use `Thread.sleep` as synchronization. Inject virtual time for TTL and refill logic; use explicit signals to know when a worker reached the intended point. Record seeds and shrink failing property cases. Assert final state and externally visible history, not implementation timing.\n\nThread safety is part of the public contract: document which methods are safe concurrently, compound-operation semantics, callback/reentrancy rules, fairness, blocking, cancellation, and ownership of returned mutable data.\n\nPrefer designs with a single owner or immutable snapshots when they make illegal interleavings unrepresentable. The concurrency reasoning framework is covered in (16-project-concurrency-whiteboard.technical-storytelling-fundamentals.q3).\n:::"
        }
      ]
    }
  ]
}
