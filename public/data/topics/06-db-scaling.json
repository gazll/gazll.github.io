{
  "sections": [
    {
      "title": "Replication & consistency",
      "items": [
        {
          "id": "06-db-scaling.replication-consistency.q1",
          "q": "What's the difference between async/semi-sync/sync replication? What problems does replication lag cause?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 220' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11.5' text-anchor='middle'>\n<rect x='270' y='30' width='140' height='44' rx='6' fill='#E4F1EB' stroke='#0B6E4F'/><text x='340' y='50' fill='#085239' font-weight='600'>PRIMARY</text><text x='340' y='66' fill='#42536C'>receives every WRITE</text>\n<rect x='60' y='140' width='150' height='44' rx='6' fill='#fff' stroke='#42536C'/><text x='135' y='160' fill='#14233A'>Replica 1</text><text x='135' y='176' fill='#7E8DA3'>READ</text>\n<rect x='265' y='140' width='150' height='44' rx='6' fill='#fff' stroke='#42536C'/><text x='340' y='160' fill='#14233A'>Replica 2</text><text x='340' y='176' fill='#7E8DA3'>READ</text>\n<rect x='470' y='140' width='150' height='44' rx='6' fill='#fff' stroke='#42536C'/><text x='545' y='160' fill='#14233A'>Replica 3</text><text x='545' y='176' fill='#7E8DA3'>READ</text>\n<g stroke='#3D4E8F' stroke-width='1.6' stroke-dasharray='5 4' marker-end='url(#ar4)'>\n<line x1='300' y1='74' x2='150' y2='138'/><line x1='340' y1='74' x2='340' y2='138'/><line x1='380' y1='74' x2='530' y2='138'/></g>\n<text x='200' y='112' fill='#3D4E8F'>async replicate (has LAG)</text>\n<defs><marker id='ar4' markerWidth='9' markerHeight='9' refX='7' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#3D4E8F'/></marker></defs>\n<text x='340' y='208' text-anchor='middle' fill='#AE4536'>Read-your-writes: route reads to primary for a few seconds after writing · semi-sync: wait for 1 replica ack</text>\n</svg></figure>\n\n- [[b:Async]]: primary commit returns immediately, replica applies later → has [[r:lag]] (reading from the replica shows stale data)\n- [[b:Semi-sync]]: primary waits for ≥1 replica ack before committing → reduces lag/increases durability, trades off write latency\n- [[b:Sync]]: waits for every replica → safest, slowest\n\n[[r:Lag consequence]]: 'I just transferred money, why hasn't my balance changed?' — from reading a lagging replica by mistake.\n\n:::deep\nTwo kinds of lag:\n- [[b:apply lag]]: replica replays slowly\n- [[b:transport lag]]: network\n\nMeasured with `Seconds_Behind_Master` (coarse) or a heartbeat table (more accurate).\n\n- [[r:Semi-sync trap]]: if the only ack-ing replica dies, the primary can *degrade to async* → thresholds and alerting must be configured\n- Aurora separates storage/compute → a 'replica' reads the same volume → millisecond lag (very different from binlog replication)\n\nOTA: 'history/loyalty' can read from a replica freely; 'wallet balance / just-created booking' must use [[g:read-your-writes]] back to the writer.\n**Measuring lag honestly:**\n<pre><code><span class=\"c\">-- Postgres: bytes behind, per replica. write/flush/replay are NOT the same;</span>\n<span class=\"c\">-- replay_lag is the one a stale read actually sees.</span>\n<span class=\"k\">SELECT</span> client_addr, state, sync_state,\n       <span class=\"f\">pg_wal_lsn_diff</span>(<span class=\"f\">pg_current_wal_lsn</span>(), replay_lsn) <span class=\"k\">AS</span> bytes_behind,\n       write_lag, flush_lag, replay_lag\n<span class=\"k\">FROM</span> pg_stat_replication;\n<span class=\"c\">--</span>\n<span class=\"c\">-- Heartbeat table: the only lag number that stays honest when the</span>\n<span class=\"c\">-- primary is idle (Seconds_Behind_Master reads 0 with no traffic).</span>\n<span class=\"k\">SELECT</span> <span class=\"f\">NOW</span>() - ts <span class=\"k\">AS</span> real_lag <span class=\"k\">FROM</span> repl_heartbeat <span class=\"k\">WHERE</span> id = <span class=\"n\">1</span>;</code></pre>\n\n`Seconds_Behind_Master` lies in two directions: it shows [[r:0 on an idle primary]] even when the replica is far behind, and it measures the *apply* delay only, missing transport lag entirely.\n\nA heartbeat row updated once a second on the primary makes lag a wall-clock number you can alert on and put in an SLO.\n:::",
          "difficulty": "core"
        },
        {
          "id": "06-db-scaling.replication-consistency.q2",
          "q": "How do you guarantee read-after-write consistency when reading from a replica?",
          "a": "- [[g:Read-your-writes]]: after a write, route that user's reads to the primary for a few seconds\n- [[g:Version/LSN check]]: only read from a replica that has applied up to the user's commit position (Aurora supports this)\n- [[g:Sticky/monotonic reads]]: keep the same replica as long as it's recent enough\n\n:::tip Fintech\nbalances and core data read from the primary; only reports/older history/analytics get pushed to a replica.\n:::\n\n:::deep\n- [[b:read-your-writes]]: route the just-written user's reads to the primary within a window of a few seconds (sticky by user/session)\n- [[b:LSN/GTID gating]]: after writing, get the log position, and only read from a replica that has applied up to it (Aurora/Postgres)\n- [[b:monotonic reads]]: use the same replica for one session so time doesn't 'go backwards'\n\n[[r:Trap]]: a load balancer round-robins across replicas with different lag → the user sees data jump around.\n\nRule: data the user expects to see immediately after writing → primary.\n\n**Runnable PostgreSQL lower-bound token — wait for replay, then fall back to primary:**\n<pre><code>-- same primary session, immediately after COMMIT\nSELECT pg_current_wal_flush_lsn() AS required_lsn;  -- e.g. 0/16B6C50\n-- on the chosen replica\nSELECT pg_last_wal_replay_lsn() &gt;= '0/16B6C50'::pg_lsn AS caught_up;\n-- f = wait with a short deadline, or route this read to primary\n-- t = the replica has replayed at least the required lower bound</code></pre>\n\nThe flush LSN can include unrelated concurrent WAL, so it is a safe but sometimes conservative lower bound—not an exact “my row” position. Carry it only for the workflow that needs monotonic reads.\n\n- **Benefit:** most later reads may still use replicas.\n- **Cost:** token propagation and waiting consume latency; primary fallback consumes write-node capacity.\n- **Safety:** never wait indefinitely or compare LSNs from unrelated timelines after failover.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "06-db-scaling.replication-consistency.q3",
          "q": "Master-slave vs multi-master — what's the trade-off?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>Single-primary</th><th>Multi-master</th></tr></thead><tbody>\n<tr><td>Writes</td><td>1 place</td><td>multiple places</td></tr>\n<tr><td>Write conflict</td><td>none</td><td>must be resolved (LWW/CRDT/app)</td></tr>\n<tr><td>Weakness</td><td>bottleneck + SPOF (needs failover)</td><td>complex, easy to lose consistency</td></tr>\n<tr><td>Good for</td><td>most systems (especially ledgers)</td><td>high-availability multi-region</td></tr>\n</tbody></table>\n\nFintech [[r:rarely uses multi-master]] for a ledger.\n\n:::deep\n- single-primary: simple, no write conflicts, but bottleneck + SPOF → needs automatic failover (Patroni/Orchestrator/RDS Multi-AZ) with the risk of [[r:split-brain]] (two primaries) if fencing is weak → needs quorum/STONITH\n- multi-master: writes in multiple places (low latency across regions) but must resolve *write conflicts* (LWW loses data, CRDT works for mergeable types, or the app resolves it)\n\nFintech/OTA: a ledger is almost never multi-master; multi-region is usually single-primary-per-region + partitioned by region (data residency) + replicated reads. Active-active is reserved for data that tolerates eventual consistency (catalog, session).\n:::",
          "difficulty": "extra"
        },
        {
          "id": "06-db-scaling.replication-consistency.q4",
          "difficulty": "advanced",
          "q": "Case: six nodes are added to a Cassandra/ScyllaDB cluster and some customers' balances start coming back empty. What are the candidates, and how do you separate them?",
          "a": "Start with the symptom, because it is the clue: [[r:a missing row is not an error]]. A read served by a replica that never received the write returns *no rows*, so the application renders an empty balance instead of failing.\n\nThe question is therefore always \"which replica answered, and why did it have nothing?\"\n\n**Candidates, cheapest to check first:**\n\n1. [[b:The consistency math]] — safety needs `R + W > RF`. At `RF = 3`, QUORUM on both sides overlaps (2 + 2 > 3); `ONE`/`ONE` does not, and a topology change widens the window in which that shows. Check the driver's default consistency level, not only the statement in front of you.\n2. [[r:A node that joined without streaming]] — `auto_bootstrap: false`, copied from a \"first node in a brand-new cluster\" runbook, lets a node claim its token ranges and serve reads with nothing behind them. The hole appears immediately and never heals on its own.\n3. [[r:Six at once]] — nodes are meant to bootstrap one at a time, each showing `UN` in `nodetool status` before the next begins. Forcing parallel joins past the consistent-range-movement guard lets ownership move before the data does.\n4. `nodetool cleanup` run too early — cleanup deletes what a node no longer owns, so running it before streaming and repair have converged removes rows the new owner never received.\n5. Expired hints — a replica down longer than the hint window (three hours by default) is not caught up by hinted handoff, and only a repair fixes it.\n\n**The decisive test is per key, not per cluster:**\n<pre><code><span class=\"c\">-- which replicas own this customer?</span>\nnodetool getendpoints ledger balances 8f3c1a2e\n<span class=\"c\">-- then ask with one replica, and watch the coordinator work</span>\nCONSISTENCY ONE; TRACING ON;\n<span class=\"k\">SELECT</span> * <span class=\"k\">FROM</span> ledger.balances <span class=\"k\">WHERE</span> customer_id = 8f3c1a2e;\n<span class=\"c\">-- ONE finds nothing but ALL returns the row =&gt; a replica is missing data,</span>\n<span class=\"c\">-- rather than a write that never happened</span></code></pre>\n\n:::warn Raise consistency before you debug\nMoving reads and writes to LOCAL_QUORUM costs latency and stops customers seeing an empty balance, so the investigation can proceed without the incident continuing underneath it.\n:::\n\n:::deep\n**Recovery order matters: repair first, cleanup after.** `nodetool repair -pr` on every node, or a scheduled repair service, rebuilds what the new replicas are missing; only then does `cleanup` remove what the old nodes no longer own.\n\nA node that joined with `auto_bootstrap: false` is not repaired but replaced — decommission it and let it bootstrap properly.\n\n**Know what your version promises about topology.** The one-node-at-a-time rule exists because ownership used to change before the data did.\n\nScyllaDB has moved topology changes onto Raft so the cluster coordinates joins itself, and Cassandra is moving the same way. Confirm what your release actually does rather than inheriting either rule from a blog post.\n\n**The real defect sits upstream of the database.** A mutable `balance` cell in an eventually consistent store has no safe read-modify-write: two writers at QUORUM still overwrite each other, and last-write-wins resolves that by timestamp, silently.\n\nMoney wants an append-only ledger with the balance derived from it (09-distributed-tx-fintech.correctness-where-money-is-involved.q3), or a lightweight transaction (Paxos, `IF` with SERIAL) paying for compare-and-set.\n\nEither way, the reconciliation that should have caught this before a customer did is not optional (09-distributed-tx-fintech.correctness-where-money-is-involved.q6).\n:::"
        }
      ]
    },
    {
      "title": "Sharding & partitioning",
      "items": [
        {
          "id": "06-db-scaling.sharding-partitioning.q1",
          "q": "What's the difference between partitioning and sharding? How do you choose a shard key?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 220' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11.5' text-anchor='middle'>\n<rect x='250' y='24' width='180' height='42' rx='6' fill='#E7EAF6' stroke='#3D4E8F'/><text x='340' y='44' fill='#3D4E8F' font-weight='600'>Router / shard map</text><text x='340' y='60' fill='#42536C'>hash(account_id)</text>\n<g stroke='#C6CFDC' stroke-width='1.6' marker-end='url(#ar5)'>\n<line x1='300' y1='66' x2='130' y2='130'/><line x1='340' y1='66' x2='340' y2='130'/><line x1='380' y1='66' x2='550' y2='130'/></g>\n<rect x='50' y='132' width='160' height='56' rx='6' fill='#E4F1EB' stroke='#0B6E4F'/><text x='130' y='154' fill='#085239' font-weight='600'>Shard 1</text><text x='130' y='172' fill='#42536C'>acct id % 3 == 0</text>\n<rect x='260' y='132' width='160' height='56' rx='6' fill='#E4F1EB' stroke='#0B6E4F'/><text x='340' y='154' fill='#085239' font-weight='600'>Shard 2</text><text x='340' y='172' fill='#42536C'>== 1</text>\n<rect x='470' y='132' width='160' height='56' rx='6' fill='#E4F1EB' stroke='#0B6E4F'/><text x='550' y='154' fill='#085239' font-weight='600'>Shard 3</text><text x='550' y='172' fill='#42536C'>== 2</text>\n<defs><marker id='ar5' markerWidth='9' markerHeight='9' refX='7' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#9AA6B8'/></marker></defs>\n<text x='340' y='208' text-anchor='middle' fill='#AE4536'>Every transaction for 1 account lives on the same shard &#8594; avoids cross-shard JOIN/transaction</text>\n</svg></figure>\n\n- [[b:Partitioning]]: splits 1 table into multiple partitions within the *same DB* (range/hash/list) → partition pruning, managing old data\n- [[b:Sharding]]: splits data across *multiple independent nodes* → scales writes & storage beyond a single machine\n\n[[g:A good shard key]]: high cardinality, evenly distributed (avoids hotspots), matches the primary query pattern.\n\nFintech typically shards by `account_id` so one user's transactions stay on the same shard (no cross-shard).\n\n:::deep\nThree concepts that often get asked together:\n- [[b:Partition]] = splitting a table within 1 node (range by month for `booking` to prune + drop old partitions fast)\n- [[b:Sharding]] = splitting across multiple nodes (by `user_id`)\n- [[b:Composite index]] = a multi-column index serving a query pattern (not splitting data)\n\nA table can *both* be range-partitioned *and* have a composite index within each partition.\n\n[[r:Shard trap]]: using `flight_id` as the shard key → hotspot on popular flights; `user_id` distributes evenly but 'all bookings for one flight' becomes scatter-gather. Bottom line: the shard key follows the *hottest query*, accepting a trade-off for secondary queries (push them to a separate read model).\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q2",
          "q": "Why is resharding painful? How does consistent hashing help?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 280' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11.5' text-anchor='middle'>\n<circle cx='250' cy='140' r='100' fill='none' stroke='#C6CFDC' stroke-width='2'/>\n<g fill='#0B6E4F'><circle cx='250' cy='40' r='9'/><circle cx='337' cy='190' r='9'/><circle cx='163' cy='190' r='9'/></g>\n<text x='250' y='28' fill='#085239'>Node A</text><text x='370' y='205' fill='#085239'>Node B</text><text x='130' y='205' fill='#085239'>Node C</text>\n<g fill='#AE4536'><circle cx='320' cy='75' r='6'/><circle cx='290' cy='235' r='6'/><circle cx='150' cy='95' r='6'/></g>\n<text x='345' y='72' fill='#AE4536'>k1</text><text x='300' y='255' fill='#AE4536'>k2</text><text x='128' y='92' fill='#AE4536'>k3</text>\n<text x='250' y='150' fill='#42536C'>key &#8594; next node</text><text x='250' y='168' fill='#42536C'>clockwise</text>\n<g font-size='11' text-anchor='start' fill='#42536C'>\n<text x='430' y='70' fill='#085239' font-weight='600'>Adding a new node:</text>\n<text x='430' y='92'>only remaps a slice of keys between the</text>\n<text x='430' y='110'>new node and the next one &#8212; does NOT</text>\n<text x='430' y='128'>reshuffle everything (unlike hash % N).</text>\n<text x='430' y='160' fill='#3D4E8F' font-weight='600'>Virtual nodes:</text>\n<text x='430' y='182'>each node maps to many points</text>\n<text x='430' y='200'>on the ring &#8594; more even distribution,</text>\n<text x='430' y='218'>avoids hotspots.</text></g>\n</svg></figure>\n\nResharding has to move data + change the mapping while the system is running.\n\n[[g:Consistent hashing]]: maps keys & nodes onto a hash ring.\n- adding/removing a node only remaps a small fraction of keys (instead of nearly all of them with modulo)\n- [[g:virtual nodes]] help balance the load\n\nTip: over-provision many 'logical shards' up front so scaling out later is easy.\n\n:::warn What you lose when you shard\nCross-shard JOIN/transaction, global AUTO_INCREMENT, aggregation requires scatter-gather. [[r:Don't shard early]] — optimize indexes/replicas/cache first.\n:::\n\n:::deep\n- each physical node gets only 1 point on the ring → skewed distribution, and losing a node dumps all its load onto exactly one neighboring node\n- assign each node ~100–200 vnodes spread around the ring → losing a node spreads the load, adding a node 'steals' a little evenly from every node (the Cassandra/DynamoDB technique)\n\nLimitations of plain consistent hashing:\n- still needs *rebalancing* of data + handling replicas (replicate to the next N nodes on the ring)\n- if [[r:range queries]] are a core requirement → hashing destroys locality → use range sharding + a directory instead of hashing\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q3",
          "q": "Generating global IDs once you've sharded: Snowflake vs UUID?",
          "a": "Sharding loses global auto-increment → you need a distributed, unique ID.\n\n<table class=\"cmp\"><thead><tr><th></th><th>Snowflake</th><th>UUIDv4</th><th>UUIDv7</th></tr></thead><tbody>\n<tr><td>Structure</td><td>timestamp+machine+seq (64-bit)</td><td>random 128-bit</td><td>time-ordered 128-bit</td></tr>\n<tr><td>Index-friendly</td><td>good (time-sequential)</td><td>poor (page splits)</td><td>good</td></tr>\n<tr><td>Needs coordination</td><td>machine id + clock sync</td><td>no</td><td>no</td></tr>\n</tbody></table>\n\n:::deep\n- [[b:Snowflake]] 64-bit = timestamp(41b)+machine(10b)+sequence(12b) → time-sequential (B+Tree-friendly, fewer splits), sortable, ~4096 ids/ms/node; needs clock sync + handling clock-skew/leap seconds\n- [[b:UUIDv4]] is random (no coordination needed, but destroys locality → splits, bloats secondary indexes); [[b:UUIDv7]] is time-ordered and fixes that\n- or a *ticket server*/*hi-lo* scheme handing out id ranges\n\nOTA: `booking_id`/`pnr` should be time-sortable (Snowflake/UUIDv7) for good write-index behavior + convenient keyset pagination + [[o:leaking less volume information]] (avoiding strictly sequential ids so competitors can't guess booking volume).\n:::",
          "difficulty": "core"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q4",
          "q": "Explain CAP and PACELC. Which side does fintech lean toward?",
          "a": "- [[b:CAP]]: when there's a Partition you must choose Consistency or Availability (P is unavoidable)\n- [[b:PACELC]]: if Partitioned, choose A/C; Else (normal operation) you still trade off Latency vs Consistency\n\nFintech:\n- core ledger/balance = [[g:CP]] (better to refuse than get the money wrong)\n- peripheral (feed, displayed history) = [[o:AP/eventual]]\n\n:::tip Bottom line\ndon't answer CAP in the abstract — say explicitly 'this part is CP, that part is AP'.\n:::\n\n:::deep\n- when [[b:P]]artitioned → choose A or C; [[b:E]]lse (no partition) → you still trade off Latency vs Consistency (reading the primary is strongly consistent but slower/farther away, reading a replica is fast but possibly stale)\n- examples: DynamoDB = PA/EL (favors availability+latency), Spanner = PC/EC (favors consistency, pays for it in latency via TrueTime)\n\nFintech/OTA states this *per component*:\n- ledger/balance/seat hold = [[g:CP/EC]] (better to refuse/be slow than get money wrong/double-sell)\n- search/availability/feed/loyalty = [[o:AP/EL]] (fast, tolerates staleness)\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q5",
          "q": "What is distributed consensus (Raft/Paxos) used for — at what depth do you need to know it?",
          "a": "[[g:Consensus]] (Raft/Paxos): multiple nodes agree on a value/ordered log despite node failures (leader election, replicated log).\n\nIt underlies etcd, the Kafka controller (KRaft), Consul, and distributed databases.\n\nA senior engineer needs to understand the [[o:concepts]] (quorum, leader, log replication, why an odd number of nodes) rather than implement it from scratch.\n\n:::deep\n- [[b:Raft]] (easier to understand than Paxos): leader election + replicated log + [[g:quorum]] (majority of N/2+1, hence an odd number: 3 tolerates losing 1, 5 tolerates losing 2)\n- an entry commits once a majority has written it → consistent despite losing a minority\n- underlies etcd (K8s), Consul, Kafka KRaft (replacing Zookeeper), CockroachDB/TiDB\n\n- why an [[o:odd number]]: avoids ties + optimizes fault tolerance/cost\n- why [[o:quorum]]: any two majorities always overlap (no two valid leaders at once)\n- consensus is often 'hidden' inside infrastructure (leader election for a scheduler/distributed lock) — knowing when it's in play is enough\n:::",
          "difficulty": "extra"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q6",
          "q": "N+1 in JPA/Hibernate — how do you detect and fix it?",
          "a": "[[r:N+1]]: 1 query fetches a list of N parents → each access to a lazy association fires 1 extra query.\n\n**Fix depending on context:**\n- `JOIN FETCH`/`@EntityGraph` (deliberate eager loading)\n- `default_batch_fetch_size`/`@BatchSize` (batches into IN → N/k+1)\n- [[g:DTO projection]] for read-heavy screens\n\nDon't go EAGER globally; watch out for `LazyInitializationException`.\n\n:::deep\n- detect: turn on `spring.jpa.show-sql`/Hibernate statistics, or a datasource-proxy that counts queries in dev — N+1 shows up as 'one list query → N child queries'\n- fix: `JOIN FETCH`/`@EntityGraph`, `@BatchSize`/`hibernate.default_batch_fetch_size` (N+1 → N/k+1), or [[g:DTO projection]] (fetch only the needed columns)\n\n[[r:Trap]]: `JOIN FETCH` on multiple collections at once → cartesian product bloats the result set; fetch one collection per query.\n\nOTA: a booking list screen with segments + passengers is prone to N+1 → use projection/2 batched queries instead of repeated lazy loads.\n\n- The same problem viewed from the application layer (batching, fetch size, statement timeout) is covered in 18.8\n- if the domain is separated from the JPA entity, most N+1 issues disappear along with lazy loading (12-architecture-patterns.clean-layered-building-it-for-real.q2).\n**The three fixes, and the one that throws at startup:**\n<pre><code><span class=\"c\">// 1. @EntityGraph - declarative, no JPQL, composes with Pageable</span>\n<span class=\"k\">@EntityGraph</span>(attributePaths = {<span class=\"s\">\"segments\"</span>})\nPage&lt;Booking&gt; <span class=\"f\">findByUserId</span>(Long userId, Pageable page);\n<span class=\"c\">    </span>\n<span class=\"c\">// 2. JOIN FETCH - explicit, but pagination happens IN MEMORY</span>\n<span class=\"k\">@Query</span>(<span class=\"s\">\"select distinct b from Booking b join fetch b.segments where b.userId = :u\"</span>)\nList&lt;Booking&gt; <span class=\"f\">findWithSegments</span>(<span class=\"k\">@Param</span>(<span class=\"s\">\"u\"</span>) Long u);\n<span class=\"c\">    </span>\n<span class=\"c\">// 3. Two List collections at once -&gt; MultipleBagFetchException, at STARTUP</span>\n<span class=\"k\">@Query</span>(<span class=\"s\">\"select b from Booking b join fetch b.segments join fetch b.passengers\"</span>)\n<span class=\"r\">// org.hibernate.loader.MultipleBagFetchException: cannot simultaneously fetch multiple bags</span></code></pre>\n\nFix 3 by fetching one collection per query and letting Hibernate stitch the second into the same persistence context, or by making the collections `Set` instead of `List`.\n\nPair `@EntityGraph` with `hibernate.default_batch_fetch_size=100` — the graph handles the association you know about, batch fetching covers the ones you forgot.\n:::",
          "difficulty": "core"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q7",
          "q": "What is CDC (Change Data Capture) / Debezium used for?",
          "a": "[[g:CDC]] reads a DB's *binlog/WAL* → emits a stream of 'data that changed' without the app needing to dual-write.\n\nDebezium pushes into Kafka, used for:\n- syncing to other systems\n- updating a cache/search index\n- implementing [[g:Outbox]] efficiently (reading the outbox table via binlog)\n\nGuarantees at-least-once → consumers must be [[o:idempotent]].\n\n:::deep\n- CDC reads the *transaction log* (MySQL binlog / Postgres WAL logical decoding) → emits a stream of 'row changed' events WITHOUT the app dual-writing (solves the atomic DB↔event problem)\n- Debezium → Kafka: syncs search/cache, data lake, implements Outbox (reading the outbox table via binlog instead of polling)\n- at-least-once + order preserved by PK → consumers must be idempotent\n\n[[r:Traps]]: schema changes, the initial snapshot (reading the whole table) is resource-intensive, 'tombstones' on delete.\n\nOTA: CDC from the booking DB to update the 'seats available' search index, sync loyalty/analytics, materialize a read model — decoupling services while staying eventually consistent.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "06-db-scaling.sharding-partitioning.q8",
          "difficulty": "advanced",
          "q": "How do you choose a partition or shard key, and migrate to a better key without losing correctness?",
          "a": "Start from the access pattern and invariant, not from the hash function. Score a candidate key against:\n\n- read locality: can the common query be answered in one partition, or will every request fan out?\n- write distribution: does one tenant, product, account, or time bucket become hot?\n- tenant isolation and privacy: can a large tenant consume the failure and capacity budget of smaller tenants?\n- growth and lifecycle: does the partition stay bounded, and can old data be archived or dropped?\n- cross-partition rules: which uniqueness, ordering, counters, or transactions become harder?\n- migration cost: can the key be changed with an online backfill and a repairable cutover?\n\nA time bucket can bound an ever-growing partition but creates boundary queries. A tenant key preserves locality but can concentrate a large tenant. A salted or bucketed key spreads writes but may require bounded fan-out and loses a simple total order. Consistent hashing helps placement changes; it does not solve a single hot key or a cross-shard invariant.\n\n**Safe migration:** `DISCOVER -> BACKFILL -> DUAL_VERIFY -> DUAL_READ/CUTOVER -> DRAIN -> RETIRE`. Record the source position/checkpoint, make copies idempotent, apply writes after the snapshot boundary, compare counts/checksums and semantic invariants, include deletes/tombstones, then ramp traffic by tenant or cell. Keep the old route for rollback until the new route has passed a measured stability window.\n\nGlobal uniqueness and ordering need an explicit owner: a database constraint on one authority, a namespace/ID strategy, or a workflow that accepts eventual reconciliation. A shard map that only answers “where is this key?” is not a correctness design.\n\n:::deep\nMeasure skew, p95/p99 partition size, hottest-key share, cross-partition fan-out, replication/repair lag, and migration error classes. Average load can look healthy while one tenant or key is already over its budget.\n:::"
        }
      ]
    }
  ]
}
