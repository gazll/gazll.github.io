{
  "sections": [
    {
      "title": "Memory & execution model",
      "items": [
        {
          "id": "01-java-core-jvm.memory-execution-model.q1",
          "q": "How does the Java Memory Model (JMM) work: happens-before, volatile, and visibility?",
          "a": "The [[b:Java Memory Model (JMM)]] defines which values a read is allowed to observe and which ordering guarantees exist between actions in different threads. Without a [[g:happens-before]] path, conflicting accesses form a data race; “it worked in a test” does not make their result portable.\n\nImportant happens-before edges include:\n\n- program order within one thread\n- monitor unlock → a subsequent lock of the same monitor\n- write to a `volatile` field → every subsequent read of that field in the synchronization order\n- actions before `Thread.start()` → actions in the started thread\n- all actions in a thread → another thread successfully detects its termination through `join()`\n\nHappens-before is transitive. It provides visibility and ordering for the actions before/after those edges; it is more precise than saying “flush the CPU cache.”\n\n<pre><code><span class=\"k\">class</span> Worker {\n    <span class=\"k\">private</span> <span class=\"k\">int</span> data;\n    <span class=\"k\">private volatile boolean</span> stop;\n    <span class=\"k\">void</span> <span class=\"f\">shutdown</span>() {\n        data = <span class=\"n\">42</span>;\n        stop = <span class=\"k\">true</span>;       <span class=\"c\">// release/publication edge</span>\n    }\n    <span class=\"k\">void</span> <span class=\"f\">run</span>() {\n        <span class=\"k\">while</span> (!stop) Thread.<span class=\"f\">onSpinWait</span>();\n        System.out.<span class=\"f\">println</span>(data); <span class=\"c\">// 42 after observing stop == true</span>\n    }\n}</code></pre>\n\nThe volatile read that observes the published write also observes `data = 42`, which precedes that write. “Latest value” means a value permitted by the volatile synchronization order—not an unsynchronized wall-clock promise while writes race.\n\nRemoving `volatile` permits the loop to keep reading an old value or be optimized in ways that never observe shutdown.\n\n:::warn Visibility is not compound atomicity\n`volatile int count; count++` is still a read, calculation and write that can lose updates. Use `AtomicInteger`/CAS, a lock, or ownership that removes the race. Volatile is appropriate for independent state such as a cancellation/config reference when all invariants fit one atomic read/write.\n:::\n\n:::deep\nJITs map JMM guarantees to architecture-specific barriers and instructions; portable code should not assert one fixed “store-store/store-load” recipe for every JVM/CPU. Use VarHandle acquire/release/opaque modes only when their weaker semantics are deliberately proven and benchmarked.\n\nFinal fields receive special initialization safety when construction completes without `this` escaping. That protects the initialized final state; it does not make later mutation of reachable objects thread-safe or safely publish arbitrary non-final state.\n\nKeep atomicity, visibility and ordering separate. CAS/ABA details are in (23-java-concurrency-coding.async-the-modern-toolkit.q3).\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "01-java-core-jvm.memory-execution-model.q2",
          "q": "Heap vs stack, escape analysis, and TLAB — why do they matter for performance?",
          "a": "- [[b:Stack]]: one stack per thread, holding frames (local variables, references).\n- [[b:Heap]]: shared objects, managed by the GC.\n\n**Escape analysis** (JIT): if an object never escapes its method/thread, the JVM can:\n- eliminate the allocation through [[g:scalar replacement]]; HotSpot C2 does not replace it with an ordinary stack allocation\n- or drop its lock through [[g:lock elision]]\n\n→ reduces GC pressure.\n\n**TLAB** (Thread-Local Allocation Buffer): each thread gets its own private allocation buffer inside Eden for fast, [[g:contention-free]] allocation.\n\n:::tip Takeaway\nmost objects \"die young\" (the weak generational hypothesis) — that's why the GC splits generations and allocation in Eden is extremely cheap.\n:::\n\n:::deep\nEscape analysis cannot eliminate the allocation when an object:\n- gets stored into another object's field\n- gets returned to the caller\n- is passed into a method that isn't inlined\n\n→ it then still goes to the heap. Enable `-XX:+PrintEscapeAnalysis`/`-XX:+EliminateAllocations` to check.\n\nAllocating in Eden is a *bump-the-pointer* operation (just moves a pointer), so it's nearly free; TLAB lets each thread bump its own pointer without CAS.\n\n**Real-world consequence:** hot loops that create garbage objects (autoboxed `Integer`, iterators, lambda captures) push the allocation rate up → more frequent minor GCs.\n- measure allocation rate (MB/s) in the GC log\n- reduce it by reusing buffers, using primitives, and avoiding boxing on the hot path\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.memory-execution-model.q3",
          "q": "Comparing GCs: Serial, Parallel, G1, ZGC, Shenandoah — when do you pick which?",
          "a": "Choose a collector from the workload's [[o:SLO]], allocation profile, heap size, and CPU budget—not from a universal ranking.\n\n<table class=\"cmp\"><thead><tr><th>Collector</th><th>Optimizes for</th><th>Typical fit</th><th>Main trade-off</th></tr></thead><tbody>\n<tr><td>Serial</td><td>simplicity and small footprint</td><td>small heaps, single-core utilities</td><td>stop-the-world collection</td></tr>\n<tr><td>Parallel</td><td>throughput</td><td>batch and offline jobs</td><td>longer stop-the-world pauses</td></tr>\n<tr><td>G1 (default)</td><td>balanced throughput and pause goals</td><td>the starting point for most services</td><td>pause target is a goal, not a guarantee</td></tr>\n<tr><td>ZGC</td><td>very low pause time</td><td>large heaps and tail-latency-sensitive services</td><td>concurrent-work CPU and memory overhead</td></tr>\n<tr><td>Shenandoah</td><td>very low pause time</td><td>latency-sensitive deployments whose JDK vendor ships it</td><td>concurrent-work overhead and vendor availability</td></tr>\n</tbody></table>\n\nStart with the JDK default, record allocation rate, live-set size, CPU, pause distribution, and end-to-end p99/p99.9, then load-test a candidate with production-shaped traffic.\n\nDo not compare only maximum GC pause: a collector can shorten pauses while consuming CPU that increases application latency.\n\n:::deep\nThe mechanisms matter:\n\n- **G1** divides the heap into regions and selects collection sets to pursue `-XX:MaxGCPauseMillis`. Humongous allocations, remembered-set work, and evacuation failure are useful investigation signals.\n- **ZGC** uses colored pointers plus load barriers so most marking and relocation work runs concurrently.\n- **Shenandoah** uses forwarding/indirection pointers and barriers for concurrent evacuation; it does [[r:not]] use ZGC's colored-pointer design.\n\nNeither “sub-millisecond” pauses nor throughput cost is a contractual constant across heaps, JDK builds, hardware, and workloads. Report a result as an [[b:example with provenance]]: workload, JDK/vendor, collector flags, heap, hardware, run duration, and latency/CPU before and after.\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.memory-execution-model.q4",
          "q": "GC tuning: heap generations, minor vs major/full GC, and signs of a memory leak?",
          "a": "<figure class='dgm'><svg viewBox='0 0 680 230' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11' text-anchor='middle'>\n<rect x='30' y='40' width='380' height='110' rx='8' fill='#E4F1EB' stroke='#0B6E4F'/><text x='220' y='32' fill='#085239' font-weight='600'>YOUNG GEN (minor GC, fast, frequent)</text>\n<rect x='48' y='60' width='200' height='72' rx='5' fill='#fff' stroke='#42536C'/><text x='148' y='100' fill='#14233A'>Eden</text>\n<rect x='262' y='60' width='62' height='72' rx='5' fill='#fff' stroke='#42536C'/><text x='293' y='100' fill='#42536C'>S0</text>\n<rect x='332' y='60' width='62' height='72' rx='5' fill='#fff' stroke='#42536C'/><text x='363' y='100' fill='#42536C'>S1</text>\n<rect x='440' y='40' width='210' height='110' rx='8' fill='#F6E8E5' stroke='#AE4536'/><text x='545' y='32' fill='#7d2f24' font-weight='600'>OLD GEN (major/full GC, expensive)</text>\n<text x='545' y='100' fill='#7d2f24'>long-lived objects</text>\n<path d='M394 96 L438 96' stroke='#3D4E8F' stroke-width='1.8' marker-end='url(#ar11)'/><text x='416' y='86' fill='#3D4E8F' font-size='9'>promote</text>\n<text x='340' y='180' fill='#42536C'>Most objects &#8220;die young&#8221; in Eden (weak generational hypothesis) &#8594; cheap to allocate, fast to clean up</text>\n<text x='340' y='204' fill='#085239'>GC: Parallel (throughput) · G1 (default, balanced) · ZGC/Shenandoah (pause &lt;1ms)</text>\n<defs><marker id='ar11' markerWidth='8' markerHeight='8' refX='6' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#3D4E8F'/></marker></defs>\n</svg></figure>\n\n**Generational heap:** Young (Eden + 2 Survivor spaces) and Old.\n- [[g:Minor GC]] cleans Young — fast, frequent\n- objects that live long enough get *promoted* to Old\n- [[o:Major/Full GC]] cleans Old — expensive, long pauses\n\n**Signs of a [[r:memory leak]]:**\n- repeated Full GCs where Old gen doesn't shrink\n- heap climbs steadily until OOM\n\nCommon causes: a static collection holding references forever, a `ThreadLocal` never removed, unbounded listeners/caches, a ClassLoader leak (redeploy).\n\nTools: heap dump + MAT, GC logs, `jcmd`.\n\n:::warn Fintech trap\nan unbounded cache is the most common source of OOM in high-traffic services — always set a max size + eviction policy.\n:::\n\n:::deep\n**Leak investigation workflow:**\n- enable `-XX:+HeapDumpOnOutOfMemoryError` + GC logging\n- open the heap dump in Eclipse MAT → check the *dominator tree* + \"Leak Suspects\"\n- trace the GC root holding the reference\n\n**Four classic culprits:**\n- unbounded cache (no max size/eviction)\n- `ThreadLocal` not `remove()`d in a thread pool (object lives as long as the thread does)\n- listener/callback never unregistered\n- ClassLoader leak on hot redeploy (Metaspace bloats)\n\nDistinguish a [[r:leak]] (Old gen climbs steadily, doesn't drop after a Full GC) from [[o:under-provisioning]] (frequent GC but reclamation still works). Citing \"allocation rate\" + \"promotion rate\" is a strong signal.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "01-java-core-jvm.memory-execution-model.q5",
          "q": "What are JIT compilation, the interpreter, C1/C2, tiered compilation, and warmup?",
          "a": "The JVM starts by *interpreting* bytecode, while also counting how \"hot\" each method is.\n\nHot methods get compiled to machine code by the JIT:\n- [[b:C1]]: fast compile, light optimization\n- [[b:C2]]: slower compile, deep optimization (inlining, escape analysis, loop unrolling)\n- [[g:Tiered compilation]] combines both\n\n**Warmup:** a JIT-based service changes performance as profiling and compilation progress.\n- a steady-state benchmark must control and report warmup; a startup/cold-path benchmark intentionally measures it\n- Native Image removes JIT warmup and often improves startup/footprint, while peak throughput and latency depend on workload, build options and profile-guided optimization—measure both deployment modes\n\n:::deep\nTiered compilation has 5 levels: 0 interpreter → 1/2/3 are C1 (different profiling levels) → 4 is C2.\n\nTwo mechanisms rarely mentioned:\n- [[o:OSR]] (On-Stack Replacement): compiling mid-flight while a long-running loop is already executing\n- [[o:deoptimization]]: when a speculative assumption (type profiling, a never-taken branch) is invalidated → falls back to the interpreter then recompiles (uncommon trap)\n\n[[r:A full code cache]] (`-XX:ReservedCodeCacheSize`) makes the JIT stop compiling → silent performance degradation.\n\nA *megamorphic* call site (many receiver types) can't be inlined → slower. Use JFR, compilation logs or `jcmd` evidence to inspect compilation; match the benchmark lifecycle to the production question instead of warming every benchmark by rule.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "01-java-core-jvm.memory-execution-model.q6",
          "difficulty": "advanced",
          "q": "JVM memory areas versus HotSpot process memory: why can RSS exceed -Xmx?",
          "a": "**The JVM specification describes logical run-time areas; the operating system measures an implementation process.** They answer different questions. `-Xmx` caps the Java heap, not every byte reserved, committed or resident by HotSpot and its native libraries.\n\n<figure class='dgm'><svg viewBox='0 0 680 296' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='10.5' text-anchor='middle'>\n<text x='18' y='20' text-anchor='start' fill='#14233A' font-weight='600'>JVM specification · logical areas</text>\n<rect x='18' y='42' width='310' height='92' rx='8' fill='#E7EAF6' stroke='#3D4E8F'/><text x='173' y='62' fill='#3D4E8F' font-weight='600'>per thread</text><text x='173' y='82' fill='#3D4E8F'>pc register · JVM stack · native method stack</text><text x='173' y='102' fill='#42536C'>frames: locals · operand stack · linkage</text><text x='173' y='120' fill='#42536C'>lifetime follows the thread</text>\n<rect x='352' y='42' width='310' height='92' rx='8' fill='#E4F1EB' stroke='#0B6E4F'/><text x='507' y='62' fill='#085239' font-weight='600'>shared</text><text x='507' y='82' fill='#085239'>heap · method area · runtime constant pools</text><text x='507' y='102' fill='#42536C'>the spec does not mandate HotSpot's layout</text><text x='507' y='120' fill='#42536C'>or one operating-system allocation</text>\n<line x1='18' y1='154' x2='662' y2='154' stroke='#D8DEE8'/><text x='18' y='178' text-anchor='start' fill='#14233A' font-weight='600'>HotSpot process · implementation memory</text>\n<rect x='18' y='198' width='122' height='60' rx='7' fill='#FFF4D9' stroke='#C58A19'/><text x='79' y='222' fill='#76500C' font-weight='600'>Java heap</text><text x='79' y='241' fill='#76500C'>bounded by -Xmx</text>\n<rect x='152' y='198' width='122' height='60' rx='7' fill='#F1EAF7' stroke='#7C5AA6'/><text x='213' y='218' fill='#5E3E80' font-weight='600'>Metaspace</text><text x='213' y='235' fill='#5E3E80'>class space</text><text x='213' y='250' fill='#5E3E80'>code cache</text>\n<rect x='286' y='198' width='122' height='60' rx='7' fill='#E7EAF6' stroke='#3D4E8F'/><text x='347' y='218' fill='#3D4E8F' font-weight='600'>Platform stacks</text><text x='347' y='235' fill='#3D4E8F'>GC / compiler</text><text x='347' y='250' fill='#3D4E8F'>JVM internals</text>\n<rect x='420' y='198' width='122' height='60' rx='7' fill='#E4F1EB' stroke='#0B6E4F'/><text x='481' y='218' fill='#085239' font-weight='600'>Direct / FFM</text><text x='481' y='235' fill='#085239'>JNI malloc</text><text x='481' y='250' fill='#085239'>native libraries</text>\n<rect x='554' y='198' width='108' height='60' rx='7' fill='#F6E8E5' stroke='#AE4536'/><text x='608' y='218' fill='#7D2F24' font-weight='600'>Mappings</text><text x='608' y='235' fill='#7D2F24'>JAR · libc</text><text x='608' y='250' fill='#7D2F24'>shared pages</text>\n<text x='340' y='284' fill='#AE4536' font-weight='600'>RSS ≠ heap used ≠ heap committed ≠ virtual address space</text>\n</svg><figcaption>The specification is portable semantics; the process map is vendor, collector, JDK and operating-system dependent.</figcaption></figure>\n\n<table><thead><tr><th>Number</th><th>What it means</th><th>Common mistake</th></tr></thead><tbody>\n<tr><td>heap used</td><td>live and garbage objects currently occupying committed heap</td><td>treating it as process RAM</td></tr>\n<tr><td>heap committed</td><td>memory currently available to the heap without growing</td><td>assuming all committed pages are resident</td></tr>\n<tr><td>heap reserved / <code>-Xmx</code></td><td>maximum heap address-space budget</td><td>using it as a container memory limit</td></tr>\n<tr><td>RSS / PSS</td><td>resident process pages; PSS apportions shared pages</td><td>expecting NMT totals to equal it exactly</td></tr>\n</tbody></table>\n\n**Thread type changes where the stack cost appears.** Platform threads have native stacks whose reservation is influenced by `-Xss`; reservation is not the same as committed or resident bytes.\n\nSince Java 21, virtual-thread stacks are growable stack-chunk objects in the GC-managed heap, not one native stack per virtual thread.\n\n**Escape analysis does not mean HotSpot moves ordinary objects onto a stack.** C2 can scalar-replace a non-escaping object, eliminating the allocation, and can elide an uncontended lock.\n\nCurrent HotSpot documentation explicitly distinguishes that from stack allocation; see the corrected treatment in (01-java-core-jvm.memory-execution-model.q2).\n\n**Direct and foreign memory are also different ownership models.** NIO direct buffers use native memory behind a heap wrapper and are associated with the direct-memory limit.\n\nThe Java 22 FFM API uses `Arena` lifetime to control native segments. Do not assume `-XX:MaxDirectMemorySize` caps arbitrary FFM or JNI allocations (01-java-core-jvm.language-fundamentals.q5).\n\n<pre><code><span class=\"c\"># Start with HotSpot's own native allocation accounting</span>\njava -XX:NativeMemoryTracking=summary -jar app.jar\njcmd &lt;pid&gt; VM.native_memory summary\njcmd &lt;pid&gt; VM.native_memory baseline\njcmd &lt;pid&gt; VM.native_memory summary.diff\n<span class=\"c\"># Then reconcile with the operating system</span>\ncat /proc/&lt;pid&gt;/smaps_rollup\npmap -x &lt;pid&gt;</code></pre>\n\nNMT tracks HotSpot/JVM native allocations, but not every allocation made outside the JVM—for example arbitrary JNI library memory. Diagnose a widening `RSS - NMT committed` gap with OS mappings and native tooling instead of increasing `-Xmx`.\n\n:::warn Container sizing is a sum, not one flag\nBudget heap plus metaspace, code cache, platform-thread stacks, direct/foreign buffers, GC and compiler structures, native libraries, mapped pages and safety headroom. Verify the sum under peak concurrency and the chosen collector; do not copy a fixed percentage across services.\n:::"
        }
      ]
    },
    {
      "title": "Concurrency",
      "items": [
        {
          "id": "01-java-core-jvm.concurrency.q1",
          "q": "Traditional thread pools vs Virtual Threads (Project Loom) — what's the core difference?",
          "a": "A platform thread is scheduled by the OS and normally maps 1:1 to it. A [[g:virtual thread]] is a Java `Thread` scheduled by the JVM over a smaller set of carrier threads. When supported blocking I/O parks a virtual thread, the JVM can unmount it so the carrier runs another task.\n\n<table class=\"cmp\"><thead><tr><th>Model</th><th>Programming style</th><th>What must be bounded</th></tr></thead><tbody>\n<tr><td>bounded platform-thread pool</td><td>simple blocking code</td><td>threads, queue, and downstream resources</td></tr>\n<tr><td>event loop/reactive</td><td>non-blocking pipeline</td><td>event-loop work and downstream concurrency</td></tr>\n<tr><td>virtual thread per task</td><td>simple blocking code at high I/O concurrency</td><td>downstream concurrency; virtual threads are not the scarce resource</td></tr>\n</tbody></table>\n\nVirtual threads, final since Java 21, improve the scalability of thread-per-request code; they do [[r:not]] make CPU work faster, make a blocking protocol non-blocking, or increase database capacity.\n\nCreate one per task rather than pooling them, and put explicit semaphores, connection pools, queues, or rate limits at scarce dependencies.\n\n:::warn Version-aware pinning guidance\nOn JDK 21–23, blocking while holding a `synchronized` monitor can pin a carrier; moving that I/O outside the critical section or using `ReentrantLock` can help. JDK 24's JEP 491 removed monitor-related pinning, so “replace every `synchronized`” is obsolete advice on JDK 24+.\n:::\n\nNative/foreign calls and class-initialization edge cases can still hold a carrier. The old `-Djdk.tracePinnedThreads` diagnostic was removed in JDK 24; use JFR, thread dumps, and carrier-utilization evidence instead.\n:::deep\nVirtual threads preserve ordinary stack traces, `try`/`catch`, `ThreadLocal` compatibility, and debugger behavior, which makes migration of blocking JDBC/HTTP code incremental.\n\nThat does not make reactive I/O obsolete: event loops remain useful for streaming, protocol engines, explicit backpressure, and ecosystems designed around non-blocking APIs.\n\nThe operational question is not “how many virtual threads can the JVM create?” but “how much concurrency can each downstream safely accept?” A virtual thread waiting for one of 20 database connections is cheap, yet 50,000 waiters still consume memory, deadlines, and queueing budget.\n\nSee the coding and diagnostic details in (23-java-concurrency-coding.async-the-modern-toolkit.q4).\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.concurrency.q2",
          "q": "What do structured concurrency and ScopedValue (Java 21/25) solve compared to ThreadLocal?",
          "a": "[[g:Structured concurrency]] makes concurrent subtasks share a lexical lifetime: the parent forks related work, joins it once, handles failure centrally, and cannot accidentally leave child tasks running after the scope closes.\n\n`StructuredTaskScope` is still a [[o:preview API in JDK 25]]; the JDK 25 shape uses `StructuredTaskScope.open(...)`, not the old public `ShutdownOnFailure`/`ShutdownOnSuccess` constructors.\n\n[[g:ScopedValue]], finalized in JDK 25, carries immutable context such as a request ID or principal through a bounded dynamic scope. Code can read a binding but cannot mutate it from arbitrary layers, and child tasks inherit the binding under the structured-concurrency rules.\n\n:::tip Separate responsibilities\n`StructuredTaskScope` controls task lifetime, cancellation, failure, and joining. `ScopedValue` carries context. Neither is a drop-in replacement for every `ThreadLocal`: keep thread-confined mutable state local, and use explicit method parameters when the dependency is part of the business API.\n:::\n\n:::deep\nIn JDK 25, `StructuredTaskScope.open()` uses a fail-fast policy: a failed subtask causes sibling cancellation and `join()` propagates failure.\n\nA custom `Joiner` expresses policies such as “first successful result.” Because this is preview, compile and run with `--enable-preview`, pin the target JDK in examples, and expect source changes between previews.\n\n`ScopedValue.where(KEY, value).call(...)` binds a value only while the operation runs. This avoids the `set()`/`remove()` discipline and thread-pool leakage risk of mutable `ThreadLocal`. It also makes ownership visible, but large objects and accidental retention can still be costly—scope values narrowly.\n\nCompare task coordination tools in (23-java-concurrency-coding.async-the-modern-toolkit.q2) and the JDK 25 API in (23-java-concurrency-coding.async-the-modern-toolkit.q4).\n\n**Runnable JDK 25 preview — two child tasks inherit one immutable trace context:**\n<pre><code>import java.util.concurrent.StructuredTaskScope;\npublic class FanOut {\n  private static final ScopedValue&lt;String&gt; TRACE = ScopedValue.newInstance();\n  static String fetch(String name) { return name + \":\" + TRACE.get(); }\n  public static void main(String[] args) {\n    ScopedValue.where(TRACE, \"trace-42\").run(() -&gt; {\n      try (var scope = StructuredTaskScope.open()) {\n        var user = scope.fork(() -&gt; fetch(\"user\"));\n        var order = scope.fork(() -&gt; fetch(\"order\"));\n        scope.join();\n        System.out.println(user.get() + \" | \" + order.get());\n      } catch (InterruptedException e) {\n        Thread.currentThread().interrupt();\n        throw new RuntimeException(e);\n      }\n    });\n  }\n}\n# save as FanOut.java\njavac --release 25 --enable-preview FanOut.java\njava --enable-preview FanOut\n# user:trace-42 | order:trace-42</code></pre>\n\n**Impact and trade-off:**\n\n- **Benefit:** lexical scope exposes child lifetime, failure propagation and context cleanup; leaving it cannot leak an orphan task.\n- **Preview cost:** `StructuredTaskScope` in JDK 25 needs `--enable-preview`, and its API may still change.\n- **Boundary:** `ScopedValue` replaces one-way immutable context, not a mutable `ThreadLocal` cache.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "01-java-core-jvm.concurrency.q3",
          "q": "How does ConcurrentHashMap work? Why is it faster than Hashtable/synchronizedMap?",
          "a": "Instead of locking the whole map, CHM locks at the [[g:bucket/bin]] level:\n- CAS when a bin is empty\n- locks the bin's first node on collision\n- reads are nearly [[g:lock-free]]\n\nJava 8+ converts long bins from a linked list to a red-black tree (O(log n)) past a threshold.\n\n**Result:** multiple threads writing to different bins don't block each other → much higher throughput than Hashtable's single global lock.\n\nNote: `size()` is only an estimate; compound operations need `compute()`/`merge()` to stay atomic.\n\n**Compound operations are where mistakes usually happen:**\n<pre><code>ConcurrentHashMap&lt;String, List&lt;Seat&gt;&gt; holds = <span class=\"k\">new</span> ConcurrentHashMap&lt;&gt;();\n<span class=\"c\">// WRONG: both operations are individually atomic, but the combination isn't</span>\n<span class=\"k\">if</span> (!holds.<span class=\"f\">containsKey</span>(id)) {           <span class=\"r\">// T1 and T2 both see false</span>\n    holds.<span class=\"f\">put</span>(id, <span class=\"k\">new</span> ArrayList&lt;&gt;());  <span class=\"r\">// T2 overwrites T1's list → the held seat is lost</span>\n}\nholds.<span class=\"f\">get</span>(id).<span class=\"f\">add</span>(seat);\n<span class=\"c\">    </span>\n<span class=\"c\">// RIGHT: a single call — CHM holds the bin lock while the function runs</span>\nholds.<span class=\"f\">computeIfAbsent</span>(id, k -&gt; Collections.<span class=\"f\">synchronizedList</span>(<span class=\"k\">new</span> ArrayList&lt;&gt;()))\n     .<span class=\"f\">add</span>(seat);</code></pre>\n\n:::deep\n- init is [[b:lazy]]; resize happens via *transfer* one bin at a time, with multiple threads helping resize concurrently (each thread claims a range of bins via `transferIndex`)\n- size counting uses `baseCount` + a `CounterCell` array, LongAdder-style, to reduce contention → `size()` is only approximate\n- treeify triggers when a bin has ≥ 8 elements *and* the table has ≥ 64 buckets (below 64, it resizes instead of treeifying)\n\n[[r:A common race]]: a compound \"check-then-put\" must use `computeIfAbsent`/`merge` to stay atomic — an easy mistake when using CHM as a cache.\n\n- This exact \"check-then-act\" trap is the classic thread-safety question in LLD rounds (22-low-level-design-ood.patterns-in-interview-code.q3).\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "01-java-core-jvm.concurrency.q4",
          "q": "synchronized vs ReentrantLock vs StampedLock — when do you pick which?",
          "a": "- [[b:`synchronized`]]: the simplest intrinsic monitor; automatic release and strong JVM optimization, but no timed/interruptible acquisition or multiple conditions.\n- [[b:`ReentrantLock`]]: `tryLock`, timeout, interruptible acquisition, optional fairness, and multiple `Condition`s; always unlock in `finally`.\n- [[b:`StampedLock`]]: optimistic reads plus read/write modes; not reentrant and deliberately lower-level.\n\n<pre><code><span class=\"c\">// 1. synchronized — prefer this for a short, unconditional critical section</span>\n<span class=\"k\">synchronized</span> (lock) { balance += amount; }\n<span class=\"c\">    </span>\n<span class=\"c\">// 2. ReentrantLock — timed admission and explicit failure policy</span>\n<span class=\"k\">if</span> (lock.<span class=\"f\">tryLock</span>(<span class=\"n\">200</span>, TimeUnit.MILLISECONDS)) {\n    <span class=\"k\">try</span> { balance += amount; } <span class=\"k\">finally</span> { lock.<span class=\"f\">unlock</span>(); }\n} <span class=\"k\">else</span> {\n    <span class=\"k\">throw</span> <span class=\"k\">new</span> BusyException();\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// 3. StampedLock — copy state, validate, then fall back</span>\n<span class=\"k\">long</span> stamp = sl.<span class=\"f\">tryOptimisticRead</span>();\n<span class=\"k\">long</span> snapshot = balance;\n<span class=\"k\">if</span> (!sl.<span class=\"f\">validate</span>(stamp)) {\n    stamp = sl.<span class=\"f\">readLock</span>();\n    <span class=\"k\">try</span> { snapshot = balance; } <span class=\"k\">finally</span> { sl.<span class=\"f\">unlockRead</span>(stamp); }\n}</code></pre>\n\n:::deep\nThese mechanisms do [[r:not]] share one implementation: `synchronized` is a JVM intrinsic monitor; `ReentrantLock` is built on `AbstractQueuedSynchronizer` (AQS); `StampedLock` has its own stamped read/write synchronizer. Choose semantics first and benchmark only when contention matters.\n\nBiased locking was disabled in JDK 15 and is no longer a modern selection criterion. The JIT can still coarsen or eliminate locks. Fair `ReentrantLock` reduces starvation risk but can materially reduce throughput.\n\n`StampedLock` optimistic reads must copy the fields needed, then call `validate(stamp)` and retry under a read lock. Do not dereference a mutable object graph before validation. It is not reentrant and has no `Condition`, so it should not be the default.\n\nOn JDK 21–23, blocking I/O inside `synchronized` could pin a virtual-thread carrier; JDK 24 removed monitor-related pinning. Use `ReentrantLock` when you need its semantics, not as a blanket JDK 24+ pinning workaround.\n\nSee the concrete coordination examples in (23-java-concurrency-coding.classic-synchronisation-problems.q1) and (23-java-concurrency-coding.classic-synchronisation-problems.q4).\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.concurrency.q5",
          "q": "How do you compose an async pipeline with CompletableFuture, and how is it different from Future?",
          "a": "`Future` only has `get()` (blocking).\n\n`CompletableFuture` lets you compose non-blocking pipelines:\n- `thenApply`/`thenCompose` (chaining)\n- `thenCombine` (merging two stages)\n- `allOf`/`anyOf`\n- `exceptionally`/`handle` (error handling)\n\nYou can specify an executor to control which thread runs it. With Java 21+, many cases are simplified with virtual threads + blocking code instead.\n\n:::deep\n[[r:Production trap]]: the non-Async variant of `thenApply` runs on whichever thread completed the *previous* stage — if that's an I/O/Netty thread, you accidentally block it → use `thenApplyAsync(fn, executor)` with an explicit executor.\n\nThe default `ForkJoinPool.commonPool` is shared JVM-wide → one slow task starves the whole app, and its size is #cores-1 (poor fit for I/O).\n\n- [[r:swallowed exceptions]]: you need an `exceptionally`/`handle` at the end of the chain, or errors silently \"disappear\"\n- Java 9+ adds `orTimeout`/`completeOnTimeout`\n- `cancel()` does NOT interrupt a task that's already running\n\nWith Java 21, many CF pipelines can be replaced with virtual threads + blocking code for easier reading/debugging.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "01-java-core-jvm.concurrency.q6",
          "difficulty": "core",
          "q": "Concurrency, multithreading and parallelism — what is the real difference, and how does it change the design you pick?",
          "a": "They answer three different questions, and an interview rewards keeping them apart:\n\n- [[b:Concurrency]] is a property of the *structure*: several tasks are in flight at once and their steps interleave, so correctness has to hold for every interleaving. It is about coordination.\n- [[g:Multithreading]] is one *mechanism* for that structure: the OS or the JVM schedules several threads of execution. Event loops, coroutines, virtual threads, separate processes and async I/O are other mechanisms for the same thing.\n- [[o:Parallelism]] is a property of *execution*: two things run in the same instant, which needs more than one core.\n\n<table class=\"cmp\"><thead><tr><th></th><th>Concurrency</th><th>Parallelism</th></tr></thead><tbody>\n<tr><td>Answers</td><td>how do overlapping tasks coordinate?</td><td>how fast can the work finish?</td></tr>\n<tr><td>Limited by</td><td>shared state, locks, ordering</td><td>cores, and the serial fraction (Amdahl)</td></tr>\n<tr><td>Fails as</td><td>race, deadlock, lost update</td><td>no speed-up, or negative scaling from contention</td></tr>\n<tr><td>Needs threads?</td><td>no — one event loop is concurrent</td><td>yes — something must run at the same instant</td></tr>\n</tbody></table>\n\nA single-threaded event loop is concurrent and never parallel. A `parallelStream` over an immutable array is parallel with almost no concurrency hazard, because nothing is shared for the interleaving to corrupt.\n\n**Why the distinction changes the design:** for I/O-bound work the limit is *waiting*, so raising concurrency — more threads, or non-blocking I/O with fewer threads — raises throughput.\n\nFor CPU-bound work the limit is *cores*, and threads past that number buy context switches and cache pressure instead of throughput.\n\n:::tip The one-line version\n\"Concurrency is about dealing with lots of things at once; parallelism is about doing lots of things at once\" — Rob Pike. Concurrency is how you write it; parallelism is what the hardware gets to do with it.\n:::\n\n:::deep\n**Little's Law turns it into a number.** Requests in flight `L = λ × W`: 500 rps at 200 ms latency means 100 concurrent requests, whatever the pool size says.\n\nThat figure is the concurrency you must support. A thread count is only one way to pay for it, and a blocking pool smaller than L queues the difference (15-network-i-o-models.threading-network-programming.q1).\n\n**Choosing a mechanism is then a resource question,** not a philosophical one:\n\n- blocking threads — simplest to read, debug and profile; each carries a stack, so the price of concurrency is memory\n- event loop — concurrency is cheap, but one blocking call stalls every task on the loop and stack traces stop explaining anything (15-network-i-o-models.realtime-event-driven.q3)\n- virtual threads — thread-per-request code at event-loop-shaped cost, which is what changed the default answer in Java 21 (01-java-core-jvm.concurrency.q1)\n\n**Parallelism has its own ceiling.** Amdahl caps the speed-up at the serial fraction, and the Universal Scalability Law adds a coherency term — which is why throughput can *fall* when you add threads to one contended counter (23-java-concurrency-coding.async-the-modern-toolkit.q5).\n:::"
        }
      ]
    },
    {
      "title": "Language fundamentals",
      "items": [
        {
          "id": "01-java-core-jvm.language-fundamentals.q1",
          "q": "The equals()/hashCode() contract — what happens if you violate it?",
          "a": "[[g:Contract]]: equal per `equals` ⇒ same `hashCode` (the reverse isn't required).\n\n[[r:Violating it]]: two \"equal\" objects land in different buckets in a HashMap/HashSet → wrong lookups, silent duplicates.\n\n`equals` must be: reflexive, symmetric, transitive, consistent.\n\nUse the same set of fields for both; for JPA entities, prefer a [[o:business key]] over the id (the id is null before persisting).\n\n**Violating the contract loses elements from a `HashSet`:**\n<pre><code><span class=\"k\">class</span> Booking {\n    Long id;                                  <span class=\"c\">// null until persisted</span>\n    <span class=\"k\">public</span> <span class=\"k\">boolean</span> <span class=\"f\">equals</span>(Object o) { <span class=\"c\">/* compares by id */</span> }\n    <span class=\"k\">public</span> <span class=\"k\">int</span> <span class=\"f\">hashCode</span>() { <span class=\"k\">return</span> Objects.<span class=\"f\">hash</span>(id); }\n}\n<span class=\"c\">    </span>\nSet&lt;Booking&gt; set = <span class=\"k\">new</span> HashSet&lt;&gt;();\nBooking b = <span class=\"k\">new</span> Booking();\nset.<span class=\"f\">add</span>(b);          <span class=\"c\">// hashCode computed while id = null → bucket X</span>\nem.<span class=\"f\">persist</span>(b);       <span class=\"c\">// DB assigns id = 1001 → hashCode changes → should now belong to bucket Y</span>\nset.<span class=\"f\">contains</span>(b);     <span class=\"r\">// false — the object is still in the set, just in the wrong bucket</span>\nset.<span class=\"f\">remove</span>(b);       <span class=\"r\">// removes nothing at all, the element stays stuck forever</span></code></pre>\n\n:::deep\n**A classic JPA/Hibernate bug** — using a DB-generated `id` in equals/hashCode:\n- before persisting, id=null → every entity is \"equal\"\n- after persisting, id changes → the object already in a `HashSet` ends up in the wrong bucket, lost from the set\n\n**Fix:**\n- use an immutable *business/natural key*\n- or assign a UUID at the application layer before persisting\n- or use a constant hashCode (poor distribution but correct)\n\n[[r:Be careful]] with Lombok's `@EqualsAndHashCode`, which by default pulls in every field (including lazy associations → triggers a query/LazyInitException).\n\nRule: base equals/hashCode on an *immutable* set of fields that identify the business entity, using `Objects.equals/hash`.\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q2",
          "q": "Immutability and the String pool — why does String's immutability matter?",
          "a": "String's immutability gives us:\n- [[g:thread safety]]\n- stable map keys\n- enables a *string pool* (interning) that reuses literals → saves memory\n\nImmutability in general reduces concurrency bugs and makes code easier to reason about. It's a core principle behind value objects in DDD.\n\n:::deep\n- literals are interned at compile time into the string pool (which has lived in the heap since Java 7)\n- `new String(\"x\")` creates a new object; `.intern()` puts it back into the pool ([[r:be careful]] interning large amounts of runtime strings — it holds references and bloats the pool)\n- Java 9's \"Compact Strings\" stores a Latin-1 `byte[]` instead of `char[]` → half the memory for ASCII strings\n- concatenating in a loop should use `StringBuilder` (each `+` creates a new object)\n\nThe basis for DDD *value objects* (Money, Currency) — records are the modern way to create an immutable carrier; remember to defensive-copy any mutable-type fields.\n\nTrivia: the string pool is a fixed-size hash table (`-XX:StringTableSize`) &#8594; interning too many runtime strings causes collisions &amp; bloat. G1 has `-XX:+UseStringDeduplication` to merge `char[]`/`byte[]` arrays with identical content.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q3",
          "q": "Checked vs unchecked exceptions — the modern backend philosophy for error handling?",
          "a": "- [[b:Checked]]: recoverable errors, must be declared/handled.\n- [[b:Unchecked]] (RuntimeException): programming errors or unrecoverable conditions.\n\nThe modern trend (Spring): prefer unchecked.\n- avoids `throws` spreading everywhere\n- wraps infrastructure errors as domain exceptions\n- handles them centrally at the boundary (`@ControllerAdvice`)\n\n**Wrapping correctly — keep the `cause`:**\n<pre><code><span class=\"c\">// WRONG: swallows the root cause, the real error's stack trace is gone</span>\n<span class=\"k\">try</span> {\n    gds.<span class=\"f\">issueTicket</span>(pnr);\n} <span class=\"k\">catch</span> (IOException e) {\n    <span class=\"k\">throw</span> <span class=\"k\">new</span> TicketingException(<span class=\"s\">\"issue failed\"</span>);   <span class=\"r\">// e is lost</span>\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// RIGHT: translated into a meaningful business exception but the cause is preserved</span>\n<span class=\"k\">try</span> {\n    gds.<span class=\"f\">issueTicket</span>(pnr);\n} <span class=\"k\">catch</span> (IOException e) {\n    <span class=\"k\">throw</span> <span class=\"k\">new</span> TicketingException(<span class=\"s\">\"issue failed: \"</span> + pnr, e);\n}\n<span class=\"c\">    </span>\n<span class=\"c\">// TicketingException is checked → by default it does NOT roll back, must be declared explicitly</span>\n<span class=\"k\">@Transactional</span>(rollbackFor = TicketingException.<span class=\"k\">class</span>)</code></pre>\n\n:::warn Transaction implication\nSpring by default only rolls back on unchecked exceptions. A checked exception [[r:does NOT auto-rollback]] without `rollbackFor`.\n:::\n\n:::deep\n**At the architectural level:** wrap infrastructure errors (SQLException, IOException) into a meaningful *domain exception* (e.g. `InsufficientBalanceException`) → upper layers stay decoupled from technical detail.\n- Spring's `@Repository` automatically translates JPA exceptions into `DataAccessException` (unchecked, categorized)\n- handle centrally at the boundary via `@ControllerAdvice`, returning *problem+json* (RFC 7807)\n- [[r:never swallow an exception]] (catch, log, and ignore) — fail fast\n\nRemember the rollback rule: only unchecked/Error auto-rolls back; checked exceptions must declare `rollbackFor` — a \"money didn't roll back\" bug that's very common in fintech.\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q4",
          "q": "ClassLoaders and the delegation mechanism — when do they cause problems?",
          "a": "ClassLoaders load classes using [[g:parent-first delegation]] (Bootstrap → Platform → Application).\n\n**Real-world problems:**\n- [[r:ClassLoader leaks]] on redeploy (a web app holds a static reference to an old class → Metaspace bloats)\n- version conflicts between libraries\n\nUnderstanding this helps you debug `NoClassDefFoundError`, `LinkageError`, and dependency hell.\n\n:::deep\n*parent-first delegation* is safe (prevents spoofing core classes), but SPI/JDBC/JNDI need the [[o:thread-context classloader]] to load implementations from a child classloader (reversing the delegation direction).\n\n**[[r:Metaspace leaks on hot-redeploy]]** — an old web app instance is kept alive via:\n- a static field\n- a thread that never stopped\n- a `ThreadLocal` holding a class\n- a JDBC driver still registered with the old classloader\n\n→ the whole ClassLoader can't be GC'd → Metaspace bloats until it OOMs on \"Metaspace\".\n\nTools: `-verbose:class`, heap dumps. Multiple versions of the same library → `LinkageError`/`NoSuchMethodError`, inspect with `mvn dependency:tree`. In K8s this is avoided entirely with immutable containers + restarts instead of hot-redeploy.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q5",
          "q": "What is off-heap / direct memory used for?",
          "a": "`ByteBuffer.allocateDirect` allocates outside the GC heap.\n- used for [[g:high-performance I/O]] (Netty, mmap)\n- avoids copies, reduces GC pressure\n\nIn exchange:\n- you must manage its lifecycle manually ([[r:easy to leak]] without releasing it)\n- harder to debug\n\nMany databases/brokers (Kafka) rely on the OS page cache + zero-copy following this same principle.\n\n:::deep\n**Mechanism:** a `DirectByteBuffer` allocates outside the heap, released *indirectly* via a Cleaner/PhantomReference when the GC collects the Java wrapper object.\n- if the object lives long / the GC doesn't run → native memory never gets returned → a [[r:'leak']] even though the heap has plenty of room\n- bounded by `-XX:MaxDirectMemorySize`\n\n**Applications:**\n- Netty's pooled allocator (avoids GC + copying)\n- Kafka: OS page cache + *zero-copy* `sendfile` (no copy through user space), mmap for the index/log\n- the Foreign Function &amp; Memory API (finalized in Java 22) — the modern, safer replacement for `sun.misc.Unsafe`\n\nGo deeper: off-heap trades \"manual lifecycle management\" for \"reduced GC pressure\" — worth it for latency-critical systems.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q6",
          "q": "Which java.time type belongs where, and why must LocalDateTime never store when something happened?",
          "a": "`LocalDateTime` is a wall-clock reading with no zone attached. `2026-03-29T02:30` is not a moment in time until someone says where — and in some zones that reading never existed.\n\n<table class=\"cmp\"><thead><tr><th>Type</th><th>What it is</th><th>Use it for</th><th>Column</th></tr></thead><tbody>\n<tr><td>`Instant`</td><td>a point on the UTC timeline</td><td>when something happened</td><td>`timestamptz`</td></tr>\n<tr><td>`OffsetDateTime`</td><td>instant + fixed offset</td><td>API payloads, keeping the caller's offset</td><td>`timestamptz`</td></tr>\n<tr><td>`ZonedDateTime`</td><td>instant + zone rules</td><td>future local events, DST-aware maths</td><td>`timestamptz` + zone id</td></tr>\n<tr><td>`LocalDateTime`</td><td>date + time, no zone</td><td>a reading a human typed, before interpretation</td><td>`timestamp` — rarely correct</td></tr>\n<tr><td>`LocalDate` / `LocalTime`</td><td>calendar date / clock time</td><td>birthday, opening hours</td><td>`date` / `time`</td></tr>\n<tr><td>`Duration` / `Period`</td><td>elapsed seconds / calendar amount</td><td>timeouts vs \"one month later\"</td><td>interval or numeric</td></tr>\n</tbody></table>\n\n<pre><code><span class=\"c\">// wrong: the moment depends on wherever this JVM happens to run</span>\norder.setPlacedAt(LocalDateTime.now());\n<span class=\"c\">//</span>\n<span class=\"c\">// right: store the instant, render in the user's zone at the edge</span>\norder.setPlacedAt(Instant.now(clock));\n<span class=\"k\">var</span> shown = order.placedAt().atZone(ZoneId.of(<span class=\"s\">\"Asia/Ho_Chi_Minh\"</span>));\n<span class=\"c\">//</span>\n<span class=\"c\">// DST: adding a day is not adding 24 hours, and ZonedDateTime knows that</span>\n<span class=\"k\">var</span> zdt = ZonedDateTime.of(<span class=\"n\">2026</span>, <span class=\"n\">3</span>, <span class=\"n\">28</span>, <span class=\"n\">10</span>, <span class=\"n\">0</span>, <span class=\"n\">0</span>, <span class=\"n\">0</span>, ZoneId.of(<span class=\"s\">\"Europe/Berlin\"</span>));\nzdt.plusDays(<span class=\"n\">1</span>);                <span class=\"c\">// 29 Mar 10:00 local — 23 real hours later</span>\nzdt.plus(Duration.ofDays(<span class=\"n\">1</span>));   <span class=\"c\">// 29 Mar 11:00 local — exactly 24 hours</span></code></pre>\n\n:::warn The default zone is not a decision, it is an accident\n`LocalDateTime.now()`, `Instant.toString()` in logs and `new Date()` all read the JVM default. It differs between your laptop, CI and the container, so the bug appears only in one of them.\n:::\n\n:::deep\n- store [[g:UTC]], convert at the boundary. One conversion in, one out, and the domain never asks what time zone it is in.\n- the exception is a [[o:future local commitment]]: \"the meeting is at 09:00 Berlin time next October\". Store the local time plus the zone id, because governments change DST rules and the correct instant is not knowable yet.\n- `timestamptz` in PostgreSQL does not store a zone — it stores a UTC instant and converts on the way out. `timestamp` stores the reading verbatim and is the one that loses information.\n- `Date`, `Calendar` and `SimpleDateFormat` are legacy, and `SimpleDateFormat` is [[r:not thread-safe]] — a shared static instance corrupts output under load. `DateTimeFormatter` is immutable and safe.\n- casting or wrapping a timestamp column in a function inside `WHERE` stops the index being used; compare against a computed range instead (05-db-core-index-lock.indexes-what-they-really-are.q5).\n- the zone database ships with the JDK and changes several times a year. Long-lived containers with a stale tzdata quietly disagree with reality (02-java-8-25-java-vs-go.release-timeline.q1).\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q7",
          "q": "Which clock do you read in production, and what breaks when the machine's clock moves?",
          "a": "There are two clocks and they answer different questions. Using the wrong one produces bugs that only appear when NTP corrects the machine.\n\n<table class=\"cmp\"><thead><tr><th></th><th>Wall clock</th><th>Monotonic clock</th></tr></thead><tbody>\n<tr><td>API</td><td>`Instant.now()`, `System.currentTimeMillis()`</td><td>`System.nanoTime()`</td></tr>\n<tr><td>Means</td><td>what time humans think it is</td><td>ticks since an arbitrary origin</td></tr>\n<tr><td>Can jump</td><td>yes — NTP steps, manual changes, DST for local views</td><td>no, never goes backwards</td></tr>\n<tr><td>Comparable across machines</td><td>roughly, within skew</td><td>no, meaningless outside one JVM</td></tr>\n<tr><td>Use for</td><td>timestamps, expiry, scheduling</td><td>latency, timeouts, rate limiting windows</td></tr>\n</tbody></table>\n\n<pre><code><span class=\"c\">// wrong: an NTP correction mid-request can report a negative duration</span>\n<span class=\"k\">long</span> t0 = System.currentTimeMillis();\n<span class=\"c\">// … work …</span>\nmetrics.record(System.currentTimeMillis() - t0);\n<span class=\"c\">//</span>\n<span class=\"c\">// right: elapsed time comes from the monotonic clock</span>\n<span class=\"k\">long</span> t0 = System.nanoTime();\n<span class=\"c\">// … work …</span>\nmetrics.record(Duration.ofNanos(System.nanoTime() - t0));\n<span class=\"c\">//</span>\n<span class=\"c\">// and the wall clock is a dependency, not a static call</span>\n<span class=\"k\">@Bean</span> Clock <span class=\"f\">clock</span>() { <span class=\"k\">return</span> Clock.systemUTC(); }   <span class=\"c\">// tests substitute it</span></code></pre>\n\n**What clock movement actually breaks:**\n\n- [[r:expiry checks]] — a token, lock lease or idempotency window judged against a clock a few seconds ahead of the issuer's (13-security-oauth2.jwt-tokens.q1)\n- [[r:ordering by timestamp]] — two services writing \"the latest wins\" cannot agree when their clocks differ by more than the gap between events\n- [[r:scheduling]] — a job pinned to local time runs twice or not at all on the DST changeover night\n\n:::tip The habit that makes time testable\nInject `Clock` and never call `Instant.now()` inside domain code. It costs one constructor parameter and turns \"wait until midnight\" into a one-line test (26-testing-strategy.testing-what-usually-breaks.q1).\n:::\n\n:::deep\n- give every expiry a [[g:skew allowance]] and validate on the side that owns the truth. A few seconds of leeway is standard for token validation; ordering guarantees need a version or sequence number instead (09-distributed-tx-fintech.correctness-where-money-is-involved.q5).\n- in PostgreSQL, `now()` returns the [[o:transaction start time]] and is identical for every row in that transaction; `clock_timestamp()` advances. Choosing wrongly makes rows written seconds apart look simultaneous.\n- application time and database time are two clocks. Pick one authority per field and write it down, otherwise `created_at` ends up populated by whichever layer happened to touch it.\n- `fixedRate` schedules from the previous start time and can pile up if a run overruns; `fixedDelay` schedules from the previous finish. Cron with a zone is a third behaviour again.\n- never compare `nanoTime` values from two JVMs, and never persist one — the origin is arbitrary and resets with the process.\n:::",
          "difficulty": "advanced"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q8",
          "difficulty": "advanced",
          "q": "Why can `com.acme.Order cannot be cast to com.acme.Order` happen, and how do you prove which loader is at fault?",
          "a": "At runtime a class is identified by [[b:its binary name plus the loader that defined it]], never by the name alone. Load the same bytes through two loaders and the JVM holds two unrelated types: `instanceof` is false, the cast throws, and the message prints the same name twice.\n\nModern JDKs print the diagnosis rather than hiding it — each side carries its module and defining loader:\n\n<pre><code><span class=\"c\">java.lang.ClassCastException:</span>\n<span class=\"c\">  class com.acme.Order cannot be cast to class com.acme.Order</span>\n<span class=\"c\">  (com.acme.Order is in unnamed module of loader &#39;app&#39;;</span>\n<span class=\"c\">   com.acme.Order is in unnamed module of loader com.acme.PluginLoader @1b6d3586)</span></code></pre>\n\n**Where the second copy comes from:**\n\n- the same jar sits on the parent classpath *and* inside a child loader — a plugin, `WEB-INF/lib` next to the container's shared `lib`, an OSGi bundle\n- Spring Boot DevTools reloads application classes in a fresh restart loader while a library singleton still holds an instance built by the previous one\n- a static registry, cache or `ThreadLocal` outlives a redeploy and hands the old type back\n- two artifacts publish the same package because a shade/relocate step was skipped\n\n**Four lines settle it** — compare identity first, then the loaders and the jars:\n\n<pre><code>Object row = registry.<span class=\"f\">lookup</span>(<span class=\"s\">\"order\"</span>);\nClass&lt;?&gt; found = row.<span class=\"f\">getClass</span>(), expected = Order.<span class=\"k\">class</span>;\nSystem.out.<span class=\"f\">println</span>(found == expected);                          <span class=\"r\">// false</span>\nSystem.out.<span class=\"f\">println</span>(found.<span class=\"f\">getName</span>().<span class=\"f\">equals</span>(expected.<span class=\"f\">getName</span>()));   <span class=\"c\">// true</span>\nSystem.out.<span class=\"f\">println</span>(found.<span class=\"f\">getClassLoader</span>());                    <span class=\"c\">// who defined it</span>\nSystem.out.<span class=\"f\">println</span>(found.<span class=\"f\">getProtectionDomain</span>().<span class=\"f\">getCodeSource</span>()); <span class=\"c\">// from which jar</span></code></pre>\n\n:::warn The same rule duplicates static state\nTwo loaders mean two copies of every `static` field, so a \"singleton\" initialises twice and the halves of the app disagree about a cache or a counter — with no cast anywhere to make it visible.\n:::\n\n:::deep\n**The fix is ownership, never a cast.** Decide which loader owns the shared type and let parent-first delegation do the rest (01-java-core-jvm.language-fundamentals.q4):\n\n- the shared API or DTO lives in the parent only; the child ships an implementation, not a second copy of the contract\n- across a plugin or restart boundary, talk through an interface the common parent loaded — or a serialized form (JSON, bytes) that carries no class identity at all\n- never cache instances of application classes in a class that outlives the restart loader; with DevTools, exclude the package or move the cache inside the restarted graph\n- `mvn dependency:tree` / `gradle dependencies` finds the duplicate artifact; relocate it rather than shipping one package twice\n\n**Reading the evidence.** `-Xlog:class+load=info` prints every loaded class with the `source:` it came from, so a duplicate shows up as two sources for one name.\n\nA heap dump answers the other half: how many `java.lang.Class` instances exist for that name, and which loader keeps each one alive — the same question a Metaspace leak asks.\n:::"
        },
        {
          "id": "01-java-core-jvm.language-fundamentals.q9",
          "difficulty": "core",
          "q": "The four OOP pillars — which Java mechanism actually implements each, and where does each one leak?",
          "a": "[[g:Encapsulation]] is not \"private field + getter/setter\". It is: the object never becomes visible in a state where its invariant is broken. Validate in the constructor, hand back copies of mutable fields, and delete the setter that lets a caller reach an invalid state.\n\n[[g:Abstraction]] names *what* a caller may ask for and hides *how*. `interface` for the contract; `abstract class` only when subtypes genuinely share state or a fixed sequence of steps.\n\n[[g:Inheritance]] is the strongest coupling Java has — a subclass depends on the superclass's *implementation*, not just its signature, so a change upstream can break it silently. Prefer composition; use `sealed` when the set of subtypes is closed and you want the compiler to check every `switch`.\n\n[[g:Polymorphism]] is one call site, many implementations, picked by the JVM from the object's [[o:runtime]] class.\n\n**`interface` vs `abstract class` — the split that decides most designs:**\n<table>\n<tr><th></th><th>interface</th><th>abstract class</th></tr>\n<tr><td><b>Instance state</b></td><td>none — only <code>static final</code> constants</td><td>fields, and a constructor that runs before the subclass body</td></tr>\n<tr><td><b>How many</b></td><td>a class implements many</td><td><code>extends</code> exactly one</td></tr>\n<tr><td><b>Bodies</b></td><td><code>default</code>, <code>static</code>, <code>private</code> methods (Java 8/9+)</td><td>any method, any access modifier</td></tr>\n<tr><td><b>Reach for it when</b></td><td>a capability unrelated types can offer (<code>Comparable</code>, a port)</td><td>shared state + a template of steps subtypes fill in</td></tr>\n</table>\n\n**Overload is compile-time, override is run-time — and mixing them up is the classic trap:**\n<pre><code><span class=\"k\">class</span> Repo      { <span class=\"k\">void</span> <span class=\"f\">save</span>(Object o) { <span class=\"c\">/* generic path */</span> } }\n<span class=\"k\">class</span> UserRepo <span class=\"k\">extends</span> Repo { <span class=\"k\">void</span> <span class=\"f\">save</span>(User u) { <span class=\"c\">/* never reached below */</span> } }\n<span class=\"c\">    </span>\nRepo r = <span class=\"k\">new</span> UserRepo();\nr.<span class=\"f\">save</span>(<span class=\"k\">new</span> User());   <span class=\"r\">// Repo.save(Object) — overload resolved from the STATIC type Repo</span>\n<span class=\"c\">    </span>\n<span class=\"c\">// override, by contrast, is resolved from the object:</span>\n<span class=\"k\">class</span> Base  { Base() { <span class=\"f\">init</span>(); }  <span class=\"k\">void</span> <span class=\"f\">init</span>() {} }\n<span class=\"k\">class</span> Child <span class=\"k\">extends</span> Base {\n    <span class=\"k\">private</span> <span class=\"k\">final</span> List&lt;String&gt; items = <span class=\"k\">new</span> ArrayList&lt;&gt;();\n    <span class=\"k\">@Override</span> <span class=\"k\">void</span> <span class=\"f\">init</span>() { items.<span class=\"f\">add</span>(<span class=\"s\">\"x\"</span>); }  <span class=\"r\">// NPE: Base's ctor runs first, items is still null</span>\n}</code></pre>\n\n:::tip The one-line version\nEncapsulation protects the invariant, abstraction names the contract, inheritance shares the implementation (at a price), polymorphism defers the choice to run time. SOLID is what you get when the last two are applied with discipline.\n:::\n\n:::deep\n- an overridable call **from a constructor** is the bug above: the JVM dispatches to `Child.init()` while `Child`'s field initialisers have not run. Make such a method `private`, `static` or `final`, or move the work out of the constructor.\n- `@Override` is not decoration. It is the only compile-time proof that you overrode rather than accidentally overloaded — the `UserRepo` above would not compile with it.\n- **only instance methods dispatch.** `static` methods are *hidden*, not overridden, and fields are *shadowed*: both resolve from the static type, so neither is polymorphic. A `private` method compiles to `invokespecial` and a `static` one to `invokestatic` — no dispatch instruction at all.\n- `final` is the one people get wrong: a `final` method still compiles to [[o:`invokevirtual`]]. Nothing can override it, so the JVM resolves it to a direct call — but that happens at run time, not in the bytecode, which is why `final` is a correctness contract rather than a speed trick.\n- what dispatch costs: `invokevirtual` reads the object's class vtable, but the JIT profiles each call site. Monomorphic (one type seen) inlines to a direct call; bimorphic inlines both behind a type check.\n- megamorphic (three or more types) falls back to a real vtable lookup and stops inlining — which is why a hot loop over a single implementation runs as fast as a static call, and why an over-abstracted one does not.\n- covariant return types are allowed on an override (a narrower return), parameters are not — widening a parameter silently turns it into an overload.\n- the pillars are the *mechanism*; the principles that say when to reach for them are (12-architecture-patterns.patterns-principles.q2), and how to cite them in an interview without reciting is (22-low-level-design-ood.patterns-in-interview-code.q1).\n:::"
        }
      ]
    },
    {
      "title": "Collections & data structures",
      "items": [
        {
          "id": "01-java-core-jvm.collections-data-structures.q1",
          "q": "How does HashMap work? Hashing, buckets, collisions, treeify, resize?",
          "a": "HashMap = an array of buckets + a hash function.\n\n**The put(k, v) flow:**\n- compute `hash(key)` → pick a bucket (`index = hash & (n-1)`)\n- bucket empty → place the node there\n- [[o:collision]] (same bucket) → append to a linked list; if the key `equals`, overwrite instead\n\n**Treeify:** when a bucket has ≥ 8 elements *and* the table has ≥ 64 buckets → converts the list to a [[g:red-black tree]] (O(log n) instead of O(n)).\n\n**Resize:** when size exceeds capacity × [[b:load factor]] (default 0.75) → capacity doubles and everything is rehashed.\n\n**What a single `put` walks through:**\n<pre><code><span class=\"k\">static</span> <span class=\"k\">int</span> <span class=\"f\">spread</span>(<span class=\"k\">int</span> h) {\n    <span class=\"k\">return</span> h ^ (h &gt;&gt;&gt; <span class=\"n\">16</span>);        <span class=\"c\">// mixes high bits into low bits, reducing collisions</span>\n}\n<span class=\"c\">    </span>\nmap.<span class=\"f\">put</span>(<span class=\"s\">\"VN8H2K\"</span>, booking);\n<span class=\"c\">// 1. h = spread(key.hashCode()); i = (n - 1) &amp; h   → n = 16, so i falls in 0..15</span>\n<span class=\"c\">// 2. bucket i empty      → place a new Node there, size++</span>\n<span class=\"c\">// 3. bucket i has a key  → compare hash then equals: if it matches, overwrite the value, size unchanged</span>\n<span class=\"c\">// 4. different key       → append to the end of the list; if the list is ≥ 8 AND the table ≥ 64, treeify</span>\n<span class=\"c\">// 5. size &gt; 16 * 0.75    → resize doubles capacity; each node moves to i or i + 16, no full rehash</span></code></pre>\n\n:::warn Trap\na broken `hashCode`/`equals` \"loses\" a key inside the map (see 1.11). Using a [[r:mutable]] key and changing a field after inserting it means you can no longer find it.\n:::\n\n:::deep\nWhy capacity is a power of 2: it lets `hash & (n-1)` replace modulo (faster). HashMap also *spreads* the hash (`h ^ (h >>> 16)`) to mix high bits into low bits, reducing collisions.\n\n- [[r:not thread-safe]] → concurrent puts can lose data (Java 7 even had an infinite loop during resize); use `ConcurrentHashMap` instead (01-java-core-jvm.concurrency.q3).\n- allows one `null` key (bucket 0); `Hashtable` doesn't.\n- iteration order is [[r:not guaranteed]] → use `LinkedHashMap` for insertion order, `TreeMap` for sorted order.\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.collections-data-structures.q2",
          "q": "ArrayList vs LinkedList — how do they differ and when do you choose which?",
          "a": "<table class=\"cmp\"><thead><tr><th></th><th>ArrayList</th><th>LinkedList</th></tr></thead><tbody>\n<tr><td>Structure</td><td>dynamic array</td><td>doubly-linked list</td></tr>\n<tr><td>get(i)</td><td>O(1)</td><td>O(n)</td></tr>\n<tr><td>add at end</td><td>O(1) amortized</td><td>O(1)</td></tr>\n<tr><td>add/remove in the middle</td><td>O(n) — shifts the array</td><td>O(1)* if already at the node</td></tr>\n<tr><td>Memory</td><td>compact (just the array)</td><td>costly (2 pointers/node)</td></tr>\n</tbody></table>\n\n**In practice:** almost always use [[g:ArrayList]].\n- good cache locality (contiguous array), fast index access\n- LinkedList only wins when adding/removing a lot at [[o:both ends]] — and even then `ArrayDeque` should be used instead\n\n:::tip Takeaway\nLinkedList's O(1)* assumes you're already at the right node; getting there still costs O(n). That's why LinkedList rarely wins in practice.\n:::\n\n:::deep\n`ArrayList.add` is amortized O(1): when full it grows ~1.5× (`newCap = old + (old >> 1)`) and copies into a new array → occasionally O(n) but O(1) on average. If you know the size in advance, use `new ArrayList<>(capacity)` to avoid resizing.\n\n- removing from the middle requires `System.arraycopy` to shift the tail → O(n).\n- neither is [[r:thread-safe]] → use `CopyOnWriteArrayList` (read-heavy) or `Collections.synchronizedList`.\n- iterating while removing → use `Iterator.remove()`, otherwise you get [[r:ConcurrentModificationException]] (fail-fast).\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.collections-data-structures.q3",
          "q": "How do HashSet / LinkedHashSet / TreeSet differ?",
          "a": "All three store [[g:no duplicate]] elements; they differ in ordering and cost:\n\n- [[b:HashSet]]: no order, O(1) add/contains — backed by HashMap\n- [[b:LinkedHashSet]]: preserves insertion order, O(1) — backed by LinkedHashMap\n- [[b:TreeSet]]: sorted by Comparator/natural order, O(log n) — backed by a red-black tree (TreeMap)\n\n**Choosing:**\n- just need fast dedup → HashSet\n- need stable insertion order → LinkedHashSet\n- need sorted order / range queries (`headSet`, `subSet`) → TreeSet\n\n<table class=\"cmp\"><thead><tr><th></th><th>HashSet</th><th>LinkedHashSet</th><th>TreeSet</th></tr></thead><tbody>\n<tr><td>Iteration order</td><td>not guaranteed</td><td>insertion order</td><td>sorted</td></tr>\n<tr><td>add / contains</td><td>O(1)</td><td>O(1)</td><td>O(log n)</td></tr>\n<tr><td>Memory per element</td><td>lowest</td><td>plus 2 pointers</td><td>plus 3 pointers and a color bit</td></tr>\n<tr><td>Allows null</td><td>one null</td><td>one null</td><td>no</td></tr>\n<tr><td>Requires of elements</td><td>equals + hashCode</td><td>equals + hashCode</td><td>Comparable/Comparator</td></tr>\n<tr><td>Range queries</td><td>no</td><td>no</td><td>headSet, tailSet, subSet</td></tr>\n</tbody></table>\n\n:::deep\nHashSet is just a HashMap with a dummy value → every property (hashing, collisions, load factor) matches HashMap's.\n\n- TreeSet [[r:doesn't allow `null`]] (elements must be comparable); HashSet allows one `null`.\n- broken `equals`/`hashCode` causes HashSet to hold \"silent duplicates\" (two equal objects in two buckets) — same root cause as item 1.11.\n- `TreeSet` dedupes using `compareTo`, [[r:not]] `equals` — if two objects aren't `equals()` but `compareTo` returns 0, TreeSet silently drops one of them. A Comparator inconsistent with equals is a silent bug.\n:::",
          "difficulty": "core"
        },
        {
          "id": "01-java-core-jvm.collections-data-structures.q4",
          "q": "Cheat sheet: which collection for which situation?",
          "a": "<table class=\"cmp\"><thead><tr><th>Need</th><th>Use</th><th>Because</th></tr></thead><tbody>\n<tr><td>Index-accessed list</td><td>ArrayList</td><td>get is O(1), cache-friendly</td></tr>\n<tr><td>Queue / Stack / Deque</td><td>ArrayDeque</td><td>faster than the old LinkedList/Stack</td></tr>\n<tr><td>Fast key→value lookup</td><td>HashMap</td><td>O(1)</td></tr>\n<tr><td>Map that keeps insertion order</td><td>LinkedHashMap</td><td>+ can double as an LRU cache</td></tr>\n<tr><td>Sorted map/set</td><td>TreeMap / TreeSet</td><td>O(log n), range queries</td></tr>\n<tr><td>Concurrent map</td><td>ConcurrentHashMap</td><td>bin-level locking (01-java-core-jvm.concurrency.q3)</td></tr>\n</tbody></table>\n\n**Rule of thumb:** default to [[g:ArrayList]] + [[g:HashMap]]; only switch when you have a measurable reason (ordering, sorting, concurrency).\n\n:::deep\nA few points senior interviewers like to ask:\n\n- [[o:LinkedHashMap]] with `accessOrder=true` + overriding `removeEldestEntry` gives you a few-line [[g:LRU cache]].\n- `Arrays.asList()` returns a [[r:fixed-size]] list (add/remove throw `UnsupportedOperationException`).\n- a plain `array` (`T[]`) is the fastest and most compact but fixed-size and not generic-friendly → only use it on hot paths/primitives (`int[]` avoids boxing).\n- pre-sizing a large HashMap/ArrayList's initial capacity avoids repeated rehashing/resizing.\n:::",
          "difficulty": "extra"
        },
        {
          "id": "01-java-core-jvm.collections-data-structures.q5",
          "difficulty": "advanced",
          "q": "HashMap vs ConcurrentHashMap — and what do initial capacity, load factor and threshold actually control?",
          "a": "Both are the same skeleton: an array of bins, a hash function, and the same two knobs (16 and 0.75). They differ only in how that array is *guarded* — and that one difference leaks into the constructor, `size()`, iteration and `null` handling.\n\n<table class=\"cmp\"><thead><tr><th>Aspect</th><th>HashMap</th><th>ConcurrentHashMap</th></tr></thead><tbody>\n<tr><td>Thread safety</td><td>none — two concurrent puts can lose an entry</td><td>per-bin; safe for any number of writers</td></tr>\n<tr><td>Write path</td><td>plain array write</td><td>CAS into an empty bin, otherwise <code>synchronized</code> on the bin's head node</td></tr>\n<tr><td>Read path</td><td>plain field reads</td><td><code>volatile</code> reads, no lock at all</td></tr>\n<tr><td>null key / value</td><td>one null key, null values allowed</td><td>neither — both throw <code>NullPointerException</code></td></tr>\n<tr><td>Iterator</td><td>fail-fast — <code>ConcurrentModificationException</code></td><td>weakly consistent — never throws, may miss concurrent writes</td></tr>\n<tr><td><code>size()</code></td><td>exact, one <code>int</code> field</td><td>an estimate summed from <code>baseCount</code> + <code>CounterCell[]</code></td></tr>\n<tr><td>Resize</td><td>one thread rebuilds the whole table</td><td>threads that find a resize running help transfer bins</td></tr>\n<tr><td>Resize threshold</td><td><code>capacity × loadFactor</code>, your value</td><td>always 0.75 — <code>sizeCtl = n - (n &gt;&gt;&gt; 2)</code></td></tr>\n<tr><td>Custom load factor</td><td>honoured for the map's whole life</td><td>only sizes the first table, then ignored</td></tr>\n<tr><td><code>new …(16)</code> gives you</td><td>16 bins, grows at 12 entries</td><td>32 bins, grows at 24 entries</td></tr>\n</tbody></table>\n\n**The three numbers, precisely:**\n- [[b:initial capacity]] `= 16` — `table.length`, the number of bins. Always rounded up to a power of two, and the array is allocated lazily on the first `put`, not in the constructor.\n- [[b:load factor]] `= 0.75` — how full the table may get before it grows.\n- [[b:threshold]] `= 16 × 0.75 = 12` — the **entry count for the whole map** that triggers `resize()`. Cross the twelfth entry and capacity doubles to 32 and the threshold to 24.\n\nThreshold counts entries in the map, not entries in [[o:one bin]]. Twelve entries all chained into a single bin still resize the table at 12 — and resizing does not help them, because they collided on their hash, not because the table was full.\n\n**Do these decide the bucket index?** [[g:Only the capacity does.]]\n\n<pre><code><span class=\"c\">// 1. spread — fold the high 16 bits down, so the mask cannot throw them away</span>\n<span class=\"k\">static</span> <span class=\"k\">int</span> <span class=\"f\">hash</span>(Object key) {\n    <span class=\"k\">int</span> h = key.<span class=\"f\">hashCode</span>();\n    <span class=\"k\">return</span> h ^ (h &gt;&gt;&gt; <span class=\"n\">16</span>);\n}\n<span class=\"c\">//</span>\n<span class=\"c\">// 2. index — a mask, not a modulo: n is a power of two, so (n-1) is all-ones</span>\n<span class=\"c\">//    n = 16 -&gt; n-1 = 0b0000_1111 -&gt; only the low 4 hash bits survive</span>\n<span class=\"c\">//    n = 64 -&gt; n-1 = 0b0011_1111 -&gt; the low 6 bits survive</span>\ni = (n - <span class=\"n\">1</span>) &amp; hash;</code></pre>\n\nLoad factor and threshold never appear in that formula. What they decide is [[o:when `n` changes]] — and `n` *is* in the formula. So they steer the index one step removed: every growth widens the mask by one bit, and one more hash bit starts participating.\n\n<pre><code><span class=\"c\">// \"VN8H2K\".hashCode() = -1759086559  -&gt; spread 0x9726EF07 -&gt; low bits ...0000_0111</span>\n<span class=\"c\">// \"VN9J4L\".hashCode() = -1759054783  -&gt; spread 0x97266367 -&gt; low bits ...0110_0111</span>\n<span class=\"c\">//</span>\n<span class=\"c\">//   n = 16   mask 0b001111   both -&gt; bin 7      collision</span>\n<span class=\"c\">//   n = 32   mask 0b011111   both -&gt; bin 7      one extra bit was not enough</span>\n<span class=\"c\">//   n = 64   mask 0b111111   bin 7 and bin 39   they finally split</span>\n<span class=\"c\">//</span>\n<span class=\"c\">// Resize never recomputes a hash. (hash &amp; oldCap) == 0 keeps the node at i,</span>\n<span class=\"c\">// anything else moves it to i + oldCap:  0x67 &amp; 32 != 0  -&gt;  7 + 32 = 39</span></code></pre>\n\nTwo consequences worth naming. A power-of-two capacity exists so `&amp;` replaces `%` — one instruction instead of a division, and no negative index from a negative `hashCode`.\n\nAnd because the mask only ever reads the **low** bits, keys differing only high up would collide forever; `h ^ (h >>> 16)` is what brings those bits into reach.\n\n**Tuning the load factor:**\n\n<table class=\"cmp\"><thead><tr><th>Load factor</th><th>Memory</th><th>Collisions</th><th>When it makes sense</th></tr></thead><tbody>\n<tr><td>low — 0.5</td><td>worse: resizes earlier, so the array is larger and emptier</td><td>fewer; chains stay at one or two nodes</td><td>a small, lookup-dominated hot map where the footprint is noise</td></tr>\n<tr><td>default — 0.75</td><td>balanced</td><td>~61% of bins empty, ~30% hold exactly one node</td><td>leave it alone unless you measured a reason</td></tr>\n<tr><td>high — 1.0 and up</td><td>better: a smaller array holds the same entries</td><td>more, and they worsen fast; above 1.0 collisions are guaranteed by pigeonhole</td><td>very large, rarely-read maps where footprint beats lookup latency</td></tr>\n</tbody></table>\n\n\"High saves memory\" is true and usually still a bad trade, because the array is the cheap half of the map:\n\n<pre><code><span class=\"c\">// 1000 entries, 64-bit HotSpot with compressed oops</span>\n<span class=\"c\">//   lf 0.75 -&gt; need cap x 0.75 &gt;= 1000 -&gt; cap = 2048 bins</span>\n<span class=\"c\">//   lf 1.00 -&gt; need cap x 1.00 &gt;= 1000 -&gt; cap = 1024 bins</span>\n<span class=\"c\">//</span>\n<span class=\"c\">// the array holds references, 4 bytes each:</span>\n<span class=\"c\">//   2048 x 4 = 8 KB        vs   1024 x 4 = 4 KB      -&gt; saved 4 KB</span>\n<span class=\"c\">// the Node objects are the real cost, and they do not change:</span>\n<span class=\"c\">//   1000 x 32 B = 32 KB    vs   1000 x 32 B = 32 KB  -&gt; saved 0</span>\n<span class=\"c\">//</span>\n<span class=\"c\">// 4 KB out of 40 KB = 10%, paid for by doubling average bin occupancy</span>\n<span class=\"c\">// from 0.49 to 0.98 entries per bin. Rarely worth it.</span></code></pre>\n\n**The physical layer** — what those numbers look like in memory:\n\n<figure class='dgm'><svg viewBox='0 0 680 320' xmlns='http://www.w3.org/2000/svg' font-family='IBM Plex Mono, monospace' font-size='11' text-anchor='middle'>\n<text x='340' y='16' fill='#085239' font-weight='600'>Node&lt;K,V&gt;[] table — capacity 16: one contiguous array of 4-byte references</text>\n<rect x='40' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='66' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='92' y='28' width='26' height='24' fill='#E4F1EB' stroke='#0B6E4F'/><rect x='118' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='144' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='170' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='196' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='222' y='28' width='26' height='24' fill='#E4F1EB' stroke='#0B6E4F' stroke-width='1.8'/><rect x='248' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='274' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='300' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='326' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='352' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='378' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='404' y='28' width='26' height='24' fill='#fff' stroke='#C7D0DE'/><rect x='430' y='28' width='26' height='24' fill='#E4F1EB' stroke='#0B6E4F'/>\n<text x='53' y='64' fill='#8A94A6' font-size='8'>0</text><text x='105' y='64' fill='#42536C' font-size='8'>2</text><text x='235' y='64' fill='#0B6E4F' font-size='8' font-weight='600'>7</text><text x='443' y='64' fill='#42536C' font-size='8'>15</text>\n<text x='560' y='44' fill='#42536C' font-size='9'>index = (16-1) &amp; hash</text>\n<path d='M235 66 L235 88' stroke='#3D4E8F' stroke-width='1.6' marker-end='url(#ar_hmchm)'/>\n<rect x='150' y='90' width='170' height='28' rx='4' fill='#fff' stroke='#42536C'/><text x='235' y='108' fill='#14233A' font-size='10'>Node \"VN8H2K\"</text>\n<rect x='360' y='90' width='170' height='28' rx='4' fill='#fff' stroke='#42536C'/><text x='445' y='108' fill='#14233A' font-size='10'>Node \"VN9J4L\"</text>\n<path d='M322 104 L356 104' stroke='#3D4E8F' stroke-width='1.6' marker-end='url(#ar_hmchm)'/><text x='339' y='98' fill='#3D4E8F' font-size='8'>next</text>\n<text x='340' y='140' fill='#7d2f24' font-size='10'>both keys masked to bin 7 — the chain is the collision, made of objects scattered across the heap</text>\n<text x='340' y='170' fill='#085239' font-weight='600'>one Node on a 64-bit JVM with compressed oops = 32 bytes</text>\n<rect x='180' y='180' width='80' height='26' fill='#EEF1F6' stroke='#42536C'/><rect x='260' y='180' width='40' height='26' fill='#EEF1F6' stroke='#42536C'/><rect x='300' y='180' width='40' height='26' fill='#E4F1EB' stroke='#0B6E4F'/><rect x='340' y='180' width='40' height='26' fill='#E4F1EB' stroke='#0B6E4F'/><rect x='380' y='180' width='40' height='26' fill='#E4F1EB' stroke='#0B6E4F'/><rect x='420' y='180' width='40' height='26' fill='#E4F1EB' stroke='#0B6E4F'/><rect x='460' y='180' width='40' height='26' fill='#F7F9FC' stroke='#C7D0DE' stroke-dasharray='3 2'/>\n<text x='220' y='197' fill='#42536C' font-size='9'>mark 8B</text><text x='280' y='197' fill='#42536C' font-size='9'>klass 4</text><text x='320' y='197' fill='#085239' font-size='9'>hash 4</text><text x='360' y='197' fill='#085239' font-size='9'>key 4</text><text x='400' y='197' fill='#085239' font-size='9'>val 4</text><text x='440' y='197' fill='#085239' font-size='9'>next 4</text><text x='480' y='197' fill='#8A94A6' font-size='9'>pad 4</text>\n<text x='240' y='222' fill='#42536C' font-size='9'>object header 12 B</text><text x='380' y='222' fill='#085239' font-size='9'>fields 16 B</text>\n<text x='340' y='256' fill='#42536C' font-size='10'>Scanning the array is sequential and prefetch-friendly. Following `next` is not:</text>\n<text x='340' y='274' fill='#42536C' font-size='10'>the address is only known once the previous Node has arrived — a dependent load,</text>\n<text x='340' y='292' fill='#42536C' font-size='10'>frequently a cache miss. That is the real price of a collision, not the extra equals().</text>\n<defs><marker id='ar_hmchm' markerWidth='8' markerHeight='8' refX='6' refY='3' orient='auto'><path d='M0,0 L7,3 L0,6 z' fill='#3D4E8F'/></marker></defs>\n</svg></figure>\n\n:::warn Trap\n`new HashMap&lt;&gt;(1000)` does not reserve room for 1000 entries: it rounds to 1024 bins and the threshold is still 1024 × 0.75 = 768, so the map resizes at the 769th entry anyway.\n:::\n\nSizing takes a *capacity*, not a count — use `HashMap.newHashMap(1000)` (Java 19+) or `new HashMap&lt;&gt;((int) (1000 / 0.75f) + 1)`, both of which land on 2048 bins and never resize.\n\n:::deep\n**Why 8 is the treeify threshold.** With a decent `hashCode` bin occupancy follows a Poisson distribution — at load factor 0.75 the mean is about 0.5 entries per bin.\n\nThe table in HashMap's own source: a bin is empty 60.65% of the time, holds one node 30.33%, two nodes 7.58%, and eight nodes about 6 times in 100 million.\n\nTreeifying is not a normal event; a bin that reaches 8 is evidence of a weak hash or a deliberate collision attack, not of a table that is merely full.\n\nIt also only treeifies once the table is ≥ 64 bins — below that HashMap resizes instead, because a tiny table's collisions are crowding rather than clustering.\n\n**ConcurrentHashMap computes 0.75 without touching a float.** `sizeCtl = n - (n >>> 2)` is n minus a quarter of n. That is exactly why a custom load factor handed to CHM's constructor only sizes the *first* table and is then forgotten — after the first resize the density is hard-wired.\n\n**`new ConcurrentHashMap&lt;&gt;(16)` allocates 32 bins, not 16.** The constructor evaluates `tableSizeFor(1 + 16 / 0.75)` = `tableSizeFor(22)` = 32, so the map genuinely holds 16 entries before growing.\n\nHashMap given the same argument gives 16 bins and resizes at 12. Same number, opposite meaning — CHM pre-divides by the load factor for you.\n\n**A HashMap's `threshold` field means two different things over its life.** `new HashMap&lt;&gt;(64)` stores `threshold = 64` — the *capacity* to allocate — and only when the first `put` triggers `resize()` does it become `64 × 0.75 = 48`.\n\nOne field, reused, which is why reading a fresh map's threshold in a debugger is confusing.\n\n**Chains are a cache problem before they are a complexity problem.** Eight nodes is eight dependent loads that cannot be prefetched, and a main-memory miss costs on the order of 100× an L1 hit. That is why treeify pays despite a `TreeNode` being ~56 B, roughly two Nodes.\n\n- The put/treeify/resize walkthrough lives in (01-java-core-jvm.collections-data-structures.q1); the bin-locking detail and the `computeIfAbsent` race are in (01-java-core-jvm.concurrency.q3).\n:::"
        },
        {
          "id": "01-java-core-jvm.collections-data-structures.q6",
          "difficulty": "core",
          "q": "A class is used as a HashMap key — why must it override both `equals` and `hashCode`, and what exactly breaks when only one of them is there?",
          "a": "`get` reaches your key in [[b:three steps]], and each step uses a different half of the override:\n\n1. `hash(key)` — take `key.hashCode()` and spread it with `h ^ (h >>> 16)`\n2. `index = (n - 1) & hash` — which bucket to walk\n3. inside the bin — `e.hash == hash && (e.key == k || key.equals(e.key))`\n\nStep 2 decides *where* to look; step 3 decides *whether it matched*. Miss either override and the map still answers, with the wrong answer and no exception anywhere.\n\n<table class=\"cmp\"><thead><tr><th>Overridden</th><th>What the lookup does</th><th>Symptom</th></tr></thead><tbody>\n<tr><td>neither</td><td>identity hash, identity comparison</td><td>only the exact instance is ever found — correct if that is what you meant</td></tr>\n<tr><td>only `equals`</td><td>two equal keys hash to different buckets</td><td>step 3 is never reached: `get` returns null and `put` adds a duplicate</td></tr>\n<tr><td>only `hashCode`</td><td>right bucket, then identity comparison</td><td>works with the original instance, fails with an equal copy — the \"passes locally\" bug</td></tr>\n<tr><td>both</td><td>bucket, then value comparison</td><td>correct — as long as the fields it reads cannot change</td></tr>\n</tbody></table>\n\n**A key that mutates is worse than a missing override:**\n<pre><code>Map&lt;Point, String&gt; map = <span class=\"k\">new</span> HashMap&lt;&gt;();\nPoint p = <span class=\"k\">new</span> Point(<span class=\"n\">1</span>, <span class=\"n\">2</span>);\nmap.<span class=\"f\">put</span>(p, <span class=\"s\">\"here\"</span>);      <span class=\"c\">// hash of (1,2) chose bucket X</span>\np.<span class=\"f\">setX</span>(<span class=\"n\">99</span>);              <span class=\"c\">// entry stays in X, the hash now points at Y</span>\nmap.<span class=\"f\">get</span>(p);               <span class=\"r\">// null — it looked in Y</span>\nmap.<span class=\"f\">remove</span>(p);            <span class=\"r\">// removes nothing; the entry is stuck for the map&#39;s lifetime</span></code></pre>\n\n:::tip What to use as a key\nPrefer a `record`, `String`, `enum` or boxed number: a record derives both methods from its components and cannot mutate, which deletes the whole class of bug instead of documenting it.\n:::\n\n:::deep\n**A constant `hashCode` is correct and still wrong.** The contract only demands equal ⇒ same hash, so `return 1` never loses an entry — but every key lands in one bin, and lookups degrade to a scan or to a tree once the bin holds eight entries in a table of at least 64.\n\nCorrectness and distribution are separate properties, and the contract covers only the first (01-java-core-jvm.collections-data-structures.q1).\n\n**`==` before `equals` is why the bug hides.** Step 3 tries reference equality first, so a test that reuses the same instance passes while production — where the key is rebuilt from a request or a database row — does not.\n\n**Not every map cares.** `TreeMap` and `TreeSet` ignore both methods and order by `compareTo`/`Comparator`, which brings its own surprise when the ordering disagrees with equals (01-java-core-jvm.collections-data-structures.q3). `IdentityHashMap` uses `==` on purpose.\n\n**JPA entities are where this is lost most often** — a database-generated id inside `hashCode` changes at `persist` time. The contract item works that case through in full (01-java-core-jvm.language-fundamentals.q1).\n:::"
        }
      ]
    }
  ]
}
